# Frozen project compatibility fixture

`0.1.0-dev-schema-1-assets.ocfproj.base64` freezes the historically
representative SequenceSlate shape (stable legacy id `all-aboard`): a heading, an `ImageReference`, an
ordered task, the actual `agency.help.v1` SVG, and its complete provenance
record. It decodes to 2,138 bytes with SHA-256
`9B592AA00C2CCB31C5678D82C1685DBE99E023FA482AE25F103AE0D50D9A13FD`.
The asset bytes and provenance are the blobs recorded at commit `1c41984`.
The package is deliberately synthetic: it contains no classroom, learner,
staff, district, or other identifying data. Freezing the bytes rather than
regenerating them under the current writer means the test detects a real
compatibility regression instead of merely agreeing with itself.

The asset-bearing fixture's snapshot is the exact learner HTML emitted by the
renderer at commit `1c41984`, not a hand-written approximation. ~~Managed
preparation first checks those exact legacy bytes, then rewrites the snapshot
through the current canonical renderer alongside the compatibility context.~~
Corrected 6 September 2026: preparation verifies and preserves every original
member's bytes, including the historical snapshot, while adding the data-only
validation and render context. It does not regenerate the snapshot or change
the recorded writer identity. The asset-bearing upgrade regression explicitly
checks all original members and their timestamps. ~~Its new assertions await
the candidate's next verification run.~~ Verified 6 September 2026: the combined
upgrade and versioned-persistence selection passed 50/50 twice after a
zero-warning Release build, format fix/verification and post-format build.
The actual TRX hashes are
`5D3137E634FC70E7EEAA9D8C938CBECE67F7EABA73F1043C38042478A33271BD`
and `3175A25236F9A11322B683609CA8B809844ABF17ECC4FA5D0020B30DFA77E687`.
These targeted checks do not replace full closing verification.

No historical `.ocfproj` blob was committed with the `0.1.0-dev` build. This
fixture was therefore constructed from that build's recorded schema-1 contract;
it does not claim to be an archived teacher package. Git commit `1c41984`
records `EngineVersion = "0.1.0-dev"` and `ProjectSchemaVersion = "1"`; commit
`4843359` changes only the engine identity here to `0.7.0-alpha`, retaining
schema 1.

`0.7.0-alpha-prior-main-task-strip.ocfproj.base64` is an untouched 3,323-byte
package emitted by `SampleGenerator` from prior main commit `1bded59`, before
the portable-snapshot verifier was added; its relevant renderer, store,
generator, and symbol trees are byte-identical to `32d8407`. Its SHA-256 is
`AE0C140FC4FCB1F9A4DF10FBDC32B2FE09384A5481016016B6223EB6DAAF0D5A`.
It freezes the important compatibility case where the engine identity did not
change while package validation became stricter; the suite proves this real
prior-main output still loads and receives a side-by-side prepared copy.

`c1-first-admission-task-strip.ocfproj.base64` separately freezes the exact
3,338-byte canonical sample from first-admission C1
`5cae09dcb40628265d51912aea98304557abfda6`. It was reconstructed on
6 September 2026 UTC by the unchanged C1 generator, twice, and matched the
original 40-member `recipe-first-admission-samples.sha256` baseline. Its SHA-256
is `9014BFFB477FC9F470E3901393AF1B6D34ACF697B5E0CD174A12BF8D76A74C82`.
This is the synthetic bilingual task strip and its two provenance-backed
symbols, not a teacher or learner package. The base64 conversion preserves
the original ZIP bytes; it is not regenerated by the candidate writer.

The C1-specific controls address the `0.7.0-alpha` reader under candidate
engine `0.8.0-alpha`, exact outgoing recipe selection, original member and
source-byte preservation, deterministic context-enriched copies, and batch
failure rollback. The prepared copy retains the historical manifest and
snapshot and adds validation/render context. Its receipt identifies the
candidate engine performing preparation, not a new original writer. These
controls are not a schema-2 route, recipe admission, or release authorization.
