# Sightings register

**Opened:** 1 September 2026 (fifth forge menu, item 1)

**Authority:** the current handover's standing instruction to keep every recorded sighting and its distinct signature open until measurement earns a diagnosis; [P-07, documented known failure modes](../release/release-requirement-test-traceability.md); the [forge closeout's recorded debts and sightings](../handover/2026-08-30-forge-closeout.md).

**Scope:** every test failure this repository has recorded as a sighting: a failure observed under load or in a hosted run that passed alone afterwards, whose cause is not established. Before this register opened, those rows lived across four handovers and the traceability matrix. This is now the one place a sighting is named, dated, and kept open. The handovers keep their narrative; this file keeps the inventory.

## The rules of the register

- These are **sightings, not diagnoses**. A row records what was measured, never a cause.
- **A passing rerun is a non-reproduction.** It is recorded, and it never closes a row. Only a measured diagnosis, recorded in a dated note that names its evidence, changes a status.
- **Rows are never deleted.** A row that earns a diagnosis is struck in place with a dated note and keeps its number.
- **Raising a timeout is not a fix.** The last flake that was chased with a longer wait turned out to be a real seam. Instruments reproduce load; they do not extend deadlines.
- **The guard:** [SightingsRegisterTests](../../tests/Unit/SightingsRegisterTests.cs) fails whenever any governing record under `docs/` names a test identifier on the same line as the word *sighting* and this register does not carry that identifier, whenever a register identifier names no real test, and whenever the row tables lose their order or vocabulary.

## Status vocabulary

| Status | Meaning |
|---|---|
| Open sighting | Observed, retained, not diagnosed. Every rerun since is a non-reproduction. |
| Deterministic, repaired | Named on a sighting line in a record, but measured as a deterministic contract failure and repaired. Listed only so the guard stays exhaustive and a reader does not mistake it for a sighting. |
| Deterministic, context-bound, undiagnosed | Fails every time in one named process context and passes in another on the same machine and tree. Not a load sighting, because it does not pass alone in the context that fails; not repaired. Listed so the next hand starts from the measurement rather than from a hunch. |

## Open sightings

| ID | Signature | Class | First observed | Retained observations and instruments | Status |
|---|---|---|---|---|---|
| S-01 | `Foundry.Tests.UiAutomation.HeadedUiaWalkTests.Part3_Steps9to12_move_edit_and_approve_operate_through_uia_patterns` | Headed UI Automation element wait timed out while the machine was contended | 29 Aug 2026, local full-solution run: failed at 22 s and passed alone in 685 ms ([the moved calendar](../handover/2026-08-29-the-moved-calendar.md)) | Never reproduced in hosted CI. A ten-repetition isolated replay passed 20/20 on 30 Aug; a source-bound headed batch passed 6/6 on 31 Aug; both are non-reproductions ([forge closeout](../handover/2026-08-30-forge-closeout.md)). Instrument: [run-headed-uia-load-repro.ps1](../../tools/run-headed-uia-load-repro.ps1), which leaves the shared 20-second probe timeout unchanged. | Open sighting |
| S-02 | `Foundry.Tests.UiAutomation.HeadedUiaWalkTests.PilotDay_dress_rehearsal_cold_start_to_reopened_booklet_and_low_ink_over_real_uia` | Headed UI Automation element wait timed out while the machine was contended; message retained: `System.TimeoutException : The UI Automation element did not appear in time`, thrown from the status wait | 29 Aug 2026, local full-solution run ([the moved calendar](../handover/2026-08-29-the-moved-calendar.md)) | Never reproduced in hosted CI. Same isolated replay and source-bound batch as S-01, both non-reproductions. Instrument: [run-headed-uia-load-repro.ps1](../../tools/run-headed-uia-load-repro.ps1). | Open sighting |
| S-03 | `Foundry.Tests.Integration.ImageNormalizerTests.A_burned_region_destroys_the_pixels_beneath_it` | Under-load assertion failure whose message was lost to the output filter | 29 Aug 2026, local full-solution run ([the moved calendar](../handover/2026-08-29-the-moved-calendar.md)) | A separate .NET 10 encoder-inventory hazard was measured on 30 Aug at 2/20 fresh processes before an explicit-codec cache and 0/50 after it; the raw logs were not retained and nothing binds that hazard to this failure, so the earlier "diagnosed" note was corrected the same day ([forge closeout](../handover/2026-08-30-forge-closeout.md)). Instrument: [run-image-load-repro.ps1](../../tools/run-image-load-repro.ps1), a fresh-process probe that cannot recover the lost message. | Open sighting |
| S-04 | `Foundry.Tests.Integration.ProjectUpgradeOperatorHostTests.Real_console_ctrl_c_cancels_the_process_and_cleans_the_synthetic_batch` reporting `console-signal.lock-observation-timeout` | The test-support sender attached to the isolated console and installed its handler but did not observe the exclusive batch lock within the unchanged 15-second budget while the target stayed alive; the signal was never sent | 1 Sep 2026, hosted pull-request run `33497314978`; again in run `33504105140` ([forge integration handover](../handover/2026-09-01-forge-integration-handover.md)) | Both failures lasted 23.9 to 28.8 s and overlapped the two-export Edge PDF exercise with up to four Integration tests running concurrently. The second run's Integration TRX is retained at SHA-256 `04DDB040A9C8BA4F620B65EE02CE95F0FA3B16EF7EA891992B9EAD3681C1D262` in receipt `20260901T114837Z-73edf7b6ab8d42ec8ee3d26fac1c7bda`. Ten isolated local passes took 0.56 to 0.60 s. C4 and C5 moved the class into the non-parallel `BoundedNativeLifecycleTestGroup` collection and added content-free probe diagnostics without changing any deadline; every later hosted pass is a non-reproduction. Instrument: [run-console-signal-load-repro.ps1](../../tools/run-console-signal-load-repro.ps1), which restores the measured overlap across two fresh test processes. | Open sighting |
| S-05 | `Foundry.Tests.Integration.ProjectUpgradeOperatorHostTests.Real_console_ctrl_c_cancels_the_process_and_cleans_the_synthetic_batch` reporting `console-signal.attach-failed` | `AttachConsole` returned false before handler installation, lock observation, or signal delivery; a distinct attachment-phase signature, not a recurrence of S-04 | 1 Sep 2026, hosted exact-`main` run `33517101369` after the documentation-only pull request 6 merged ([forge integration handover](../handover/2026-09-01-forge-integration-handover.md)) | Receipt `20260901T140524Z-8ff4cc52be4044b3a1021b7165ad5e95` retained Integration TRX SHA-256 `E85F112C74467AE09DEB1B020D158477046ED626F282B53007E4EE119B0B0AAD`, a 0.63 s duration, and the content-free snapshot `senderExit=3; hostExited=False; candidateEntries=0; lockExists=False; hostStdoutEmpty=True; hostStderrEmpty=True`. The receipt kept no native error code. Pull request 7 made the sender record attach and target-watch results separately and retry only `ERROR_INVALID_HANDLE` inside the same 15-second budget; that correction does not claim the observed error was that code. Pull-request run `33522404031` and exact-`main` runs `33523542939` and `33528622567` are non-reproductions. Instrument: [run-console-signal-load-repro.ps1](../../tools/run-console-signal-load-repro.ps1). | Open sighting |
| S-06 | `Foundry.Tests.Integration.FlashCapCameraSourceTests.A_late_successful_start_is_stopped_and_disposed_again_after_immediate_cleanup` | Expected `OperationCanceledException`; the outer five-second observation produced `TimeoutException` instead | 1 Sep 2026, hosted pull-request run `33507671099` at the C4 checkpoint ([forge integration handover](../handover/2026-09-01-forge-integration-handover.md)) | Receipt `20260901T122927Z-9ced1d172cc9405ba728cd2ea3fa7cb7` retained the byte-identical raw and curated Integration TRX at SHA-256 `6D9C9163DA96762AFD63319F828B5321FED64855580510235437DD3A5CAFD519`. The 11.67 s case overlapped a 38.3 s real-PDF test, a 37.4 s floor test, and an 11.6 s capture test. It then passed 20/20 in fresh isolated processes and inside two complete local receipts. C5 placed the class in `BoundedNativeLifecycleTestGroup`; later hosted passes are non-reproductions. Instrument: [run-console-signal-load-repro.ps1](../../tools/run-console-signal-load-repro.ps1). | Open sighting |
| S-07 | `Foundry.Tests.Integration.FlashCapCameraSourceTests.A_shared_lifecycle_lock_can_prevent_confirmed_shutdown_but_capture_still_settles_bounded` | Expected exact `InvalidOperationException`; the outer five-second bound observed `TimeoutException: The operation has timed out.` | 31 Aug 2026, attempt 1 of hosted `main` run `33378979000` at 1,327/1,328 ([kiosk-plan evaluation](../handover/2026-08-30-kiosk-plan-evaluation.md)) | The test then passed alone, alone with coverage, and inside a fresh local 1,328/1,328 run; the record names it "a new named sighting". No later hosted failure of this case is recorded. Instrument: [run-console-signal-load-repro.ps1](../../tools/run-console-signal-load-repro.ps1), which now includes it as a third named case because it shares the bounded native-lifecycle collection. | Open sighting |
| S-08 | Complete UI Automation assembly produced no TRX and an idle testhost for more than five minutes in two plain minimal-output executions | Local same-process suite stall with no active test name, stack, or dump captured | 31 Aug 2026, local ([kiosk-plan evaluation](../handover/2026-08-30-kiosk-plan-evaluation.md); [UIA harness traceability](../accessibility/uia-harness-traceability.md)) | The same binary passed 243/243 with ordinary output, with blame-hang, and with diagnostic logging, and passed as separate headed and non-headed partitions. Independent harness lifecycle defects were repaired that day and must not be read as the cause. No instrument exists; a diagnosis requires a stalled-run process inventory and native or full dumps, which no run has yet captured. | Open sighting |
| S-09 | Hosted UI Automation produced no TRX before the six-hour job limit and the run was cancelled | Hosted same-process suite stall; six other suites had completed 1,103/1,103 | 31 Aug 2026, attempt 2 of hosted `main` run `33378979000` ([kiosk-plan evaluation](../handover/2026-08-30-kiosk-plan-evaluation.md); [traceability matrix](../release/release-requirement-test-traceability.md)) | The run has no overall result and its dependent gates were skipped, not inferred. The bounded runner in [run-ci-tests.ps1](../../tools/run-ci-tests.ps1) now caps the single test process at 15 minutes and retains its streams and receipt; that is an evidence boundary, not a diagnosis. Related to S-08 by signature, recorded separately by environment. | Open sighting |
| S-10 | Part3 approval `Invoke` returned `0x8000FFFF` once while approval closed the final window | Provider-return sighting inside the `Foundry.Tests.UiAutomation.HeadedUiaWalkTests.Part3_Steps9to12_move_edit_and_approve_operate_through_uia_patterns` walk, distinct from S-01's element-wait timeout | 30 Aug 2026, local: five identical nine-test focused batches measured 8/9, 9/9, 9/9, 9/9, and 8/9 ([forge closeout](../handover/2026-08-30-forge-closeout.md)) | The other red in those batches, a Gate B window whose UIA modal flag never arrived, was diagnosed as a missing native owner and repaired; that repair does not explain this return. The harness now accepts `0x8000FFFF` only when exit code 0 independently proves completion and otherwise retains the HRESULT and process state. Post-repair batches passed 5/5 and 40/40, which are non-reproductions. Instrument: [run-headed-uia-load-repro.ps1](../../tools/run-headed-uia-load-repro.ps1) through its Part3 case. | Open sighting |
| S-11 | `Foundry.Tests.UiAutomation.HeadedUiaWalkTests.Part3_Steps9to12_move_edit_and_approve_operate_through_uia_patterns` reporting `System.Windows.Automation.ElementNotAvailableException : Unrecognized error.` | A headed UI Automation element became unavailable mid-walk while the full solution ran; a distinct message from S-01's element-wait timeout and S-10's `0x8000FFFF` return | 1 Sep 2026, local bounded receipt `20260901T202153Z-a8f63cde12b6455aad54179b2834b9f5` at 2,011/2,013 ([fifth forge menu](../handover/2026-09-01-fifth-forge-menu.md)) | The retained UI Automation TRX records a 1.52 s duration and a stack that ends in the UI Automation core's `FindAll` query rather than in a wait. The case then passed alone 2/2 in fresh processes at about 1 s each and passed inside the two complete bounded receipts that followed; those are non-reproductions. That red receipt also carries the context-bound console failure recorded as N-02, which is a separate matter. Instrument: [run-headed-uia-load-repro.ps1](../../tools/run-headed-uia-load-repro.ps1) through its Part3 case. | Open sighting |

## Named beside sightings, not sightings

| ID | Signature | Disposition |
|---|---|---|
| N-01 | `Foundry.Tests.Unit.LocalizationRulesTests.The_winforms_app_carries_no_user_facing_string_outside_the_catalog` | Deterministic, repaired 30 Aug 2026. The [forge closeout](../handover/2026-08-30-forge-closeout.md) names it on the line that also states no sighting reproduced: it reported twelve HTTP/IPC grammar literals in the print-view handoff, a private machine protocol rather than chrome; the source guard gained an exact machine-protocol inventory and the rule passed. Not a sighting. |
| N-02 | `Foundry.Tests.Integration.ProjectUpgradeOperatorHostTests.Real_console_ctrl_c_cancels_the_process_and_cleans_the_synthetic_batch` with the host's standard output not empty: the sender exited 0 with empty streams, yet the host completed its 512-package batch and printed its preparation receipt instead of the `upgrade.canceled` refusal | Deterministic, context-bound, undiagnosed. Measured 1 Sep 2026 on one machine and one tree ([fifth forge menu](../handover/2026-09-01-fifth-forge-menu.md)): 3.50 s to fail inside the red bounded receipt `20260901T202153Z-a8f63cde12b6455aad54179b2834b9f5` and 3/3 fresh-process failures at about 2 s each, all with the test process launched from the session's Git Bash tool context; 1/1 pass at 0.59 s when launched from a Windows PowerShell context, and passes inside the two green PowerShell-context bounded receipts that followed. The test is a bounded observation followed by delivery, not a held rendezvous, and in that one context the signal never reaches the batch before it completes. What differs between the two contexts is not measured. Not a sighting, because it does not pass alone in the context that fails; not repaired; a candidate for the next forge menu. |

## The guard

[SightingsRegisterTests](../../tests/Unit/SightingsRegisterTests.cs) reads every Markdown file under `docs/` except this one. On every line that contains the word *sighting*, it extracts each test identifier written as a class name ending in *Tests*, a dot, and a method name containing an underscore, and each `console-signal.` token, and fails if this register does not carry it. It also fails if a register identifier names no method in a matching test source file, if a token is absent from the console-control sender, if the S and N rows are not sequential, complete, and in their closed vocabulary, or if a relative link here does not resolve. The guard cannot judge whether a row is honest; a hand still reads the record it cites.

## Adding a row

1. Retain the evidence first: run identifier, receipt identifier, TRX hash, duration, and the exact failure name and message.
2. Add the next S row with the signature, class, first observation, retained observations, and the instrument that can seek an equivalent-load reproduction. If none exists, say so.
3. Cite the run in the [evidence ledger](evidence-ledger.json) rather than restating it in a handover.
4. Never raise a deadline as part of the same change. If a later measurement earns a diagnosis, strike the row in place with a dated note and its evidence.
