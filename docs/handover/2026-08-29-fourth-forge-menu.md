# The fourth forge menu — the vein thins, and the product widens it

**Date:** 29 August 2026 · **Status:** PROPOSED — drafted by the generator from atlas 2.0 against the repository at `42de318` (menus one through three fully struck); **ratification is the typist's act.** Adopting, striking, or reordering is done by reply; the first "Proceed with …" against an item adopts that item. Until then this document binds nothing.

The standing laws carry forward unchanged from the [first menu](2026-08-29-waiting-window.md): presses take parameters, never prose; nothing renders, prints, exports, or persists without a typed `ApprovedArtifact`; lanes escalate only and Amber never persists; model self-ratings are not release evidence; the AAC/SLP seat and the district's written instrument gate what they gate, and neither is waivable from the keyboard.

## I. What the window looks like now — an honest accounting

Eighteen recipes, forty-eight engines, 597 green tests, the full teacher loop working end to end. And a fact that outranks everything else on this menu: **the release artifact in `out/` was built at 10:48 this morning — before any menu existed.** A pilot machine provisioned from `honest-ink-win-x64.zip` opens an app whose main window is the empty placeholder that was retired eleven commits ago. Item 1 exists because of that zip.

Beyond it, this menu is honest about its own character: the ATLAS's zero-gate vein has thinned — three menus took its dense seams, and what remains there is smaller-grained: pilot-week readiness, worthy stragglers, machine-evidence hardening. But the typist named the deeper truth as this menu was being drawn: **the atlas is not the only source of possibilities — the art of the possible is widened by the enriched product itself.** The atlas was written before the vector PDF writer, before the seeded PRNG, before the press catalog became data, before the imposition transform existed. Those capabilities now COMPOSE into artifacts no atlas entry could have named, and section II carries two of them (items 9 and 10) alongside the atlas work. Every item below is real; none is the size of a Press Room; and the letters may still preempt this menu entirely.

## II. The menu, ranked by value-per-risk in this window

### 1. ~~The pilot build — a fresh distributable and a dress rehearsal~~ — **done 29 Aug 2026**
Two halves of one readiness claim. First, a **repeatable publish script** (a tool beside the SampleGenerator) that produces `out/publish`, `honest-ink-win-x64.zip`, and `SHA256SUMS.txt` from the current tree with one command — then RUN it, so the artifact on disk contains the app that actually exists tonight; installing it on the pilot machine, tagging, and distributing remain the typist's acts, stated in the script's own output. Second, a **dress-rehearsal scenario test**: one headed run of the complete pilot-day loop — cold start, forge a press, Gate B, approve, save to library, reopen, export the booklet PDF, low-ink variant — as a single test that fails if any seam of the whole grammar slips before September 8. **DoD:** fresh zip and sums on disk, script committed; the rehearsal green headed. *Medium.*

*Struck 29 Aug 2026.* `tools/publish.ps1` already existed from the 10:48 build; it gained the typist's-acts statement in its own output and was re-run — the zip and sums in `out/` now carry the forty-eight-engine app (verified by launching the published exe itself), not the retired placeholder. The dress rehearsal is `PilotDay_dress_rehearsal_cold_start_to_reopened_booklet_and_low_ink_over_real_uia` in `HeadedUiaWalkTests.PilotRehearsal.cs`: the full loop headed and green in ~4 s, with the print step asserting the gate and the speaking status, never paper. It earned its keep before it ever passed: it found Export's modal still click-synchronous (the finding-6 wedge, undeferred) and proved the shell Save As dialog undrivable by cross-process automation — four distinct walls, recorded as findings 7–8 in the [traceability register](../accessibility/uia-harness-traceability.md) — so Export gained the same ctor seam the library picker already had (`exportPicker`, harness switch `--export-to`), while the Open dialog leg runs against the real dialog, which commits honestly. The harness also gained `--library-root`, so the rehearsal saves and reopens real `.ocfproj` projects in a disposable directory and the teacher's Documents are never touched. 598 tests green, twice.

### 2. ~~The Chart Press (the deterministic heart of atlas #88)~~ — **done 29 Aug 2026**
Bar charts from a teacher's own data — "label | value" lines, values printed verbatim, bars **proportionally true** with the same exactness discipline as the timeline (a value twice as large is a bar exactly twice as long, asserted as arithmetic), gridlines at clean intervals, horizontal or vertical, landscape-friendly. Data Story Maker's inquiry prompts stay with the teacher; the press draws the picture. Serves math, science (#99's graph-to-claim needs a graph), and the whole data-literacy seam with one engine. **DoD:** the usual press bar; bar proportionality asserted exactly. *Small-medium.*

*Struck 29 Aug 2026.* `ChartPress` in the module, `bar-chart` in the catalog (forty-nine engines), `press.charts` as the nineteenth recipe with the honest prohibition: computing, aggregating, correcting, or interpreting the teacher's data — the numbers are the teacher's claim, drawn exactly as typed. Proportionality is asserted as arithmetic against the catalog's own defaults, whose data carries the invariant on its face (Sun 18 is exactly twice Shade 9); horizontal bars mirror the same arithmetic and the longest bar meets its gridline exactly; the clean-interval step (1, 2, or 5 × 10ⁿ, at most eight lines) has its own theory. Three recipe-count assertions bumped, not the two the handover promised — `WholeBookVariantTests` held a third. The samples run gained `press-bar-chart`, double-run byte-identical (the one standing mismatch remains `task-strip-bilingual.ocfproj`, the documented writer-nondeterminism awaiting its unmerged parallel task). The pilot zip was re-pressed so the artifact carries the new engine. 612 tests green, twice.

### 3. ~~Bell-to-Bell (atlas #44) — the schedule that does its own arithmetic~~ — **done 29 Aug 2026**
Teacher-typed rows of "minutes | activity" plus a start time; the press computes each activity's clock time cumulatively, prints the minute-by-minute plan, and **refuses loudly when the minutes overrun the declared period** — the schedule arithmetic teachers do in the margin, done exactly. A transitions column and a protected-closure row honor the atlas's intent. **DoD:** the usual press bar; cumulative clock arithmetic asserted; overrun refused. *Small.*

*Struck 29 Aug 2026.* `BellToBell` in the module, `bell-to-bell` in the catalog (fifty engines), `press.schedules` as the twentieth recipe — prohibition: planning, suggesting, reordering, or trimming activities; the press does only the clock arithmetic. It is a semantic document on the booklet guide's precedent (Heading, TableNode, summary Paragraph), not a vector sheet — the arithmetic IS the artifact. Transitions ride as a column and count in the cumulative clock; the closure row is protected at the period's end, so open minutes appear between the last transition and the closure and are named in the summary line; an overrun refuses with the full arithmetic in the message ("needs 55 but the period holds 50; trim 5"). The catalog defaults meet the bell exactly — 48 activity + 4 transitions + 3 closure = 55 of 55 — and the tests assert every clock cell against them. Honest smudge, recorded in the handover: a UIA test flaked once during the stability run and its NAME was lost to a summary-only output filter; green on the immediate rerun and a third full run. 618 tests green; samples gained `press-bell-to-bell`, double-run identical; the pilot zip re-pressed.

### 4. ~~The verbatim-text kin — Bug Zoo and Fluency Rehearsal (atlas #214, #77)~~ — **done 29 Aug 2026**
**Bug Zoo**, teacher-authored bugs ONLY: a buggy program the teacher wrote, printed verbatim with the trace-table discipline (indentation as geometry, code never interpreted), above diagnose → repair → explain sections; the teacher's note on the intended misconception rides as a teacher-only notice. **Fluency Rehearsal Builder**: teacher text with phrase marks, printed large with the marks become visible breath-breaks, repeated-reading tally boxes, and a reflection line — the text preserved exactly, the marks the teacher's own. **DoD:** the usual press bar; verbatim invariants asserted. *Small; splits.*

*Struck 29 Aug 2026.* `BugZoo` rides in the computational studio under `press.computational` — its "code is cargo" prohibition IS Bug Zoo's law — with the trace table's numbered-gutter indent geometry, three teacher-editable section labels, and a REQUIRED misconception note as `TeacherOnlyNotice` (a blank note refuses: the note is the teacher's authorship in ink; the catalog default is a real misconception — assignment where accumulation was meant). `FluencyRehearsal` under `press.fluency`, the twenty-first recipe: passage lines in 7 mm print, the teacher's `|` marks rendered as breath-break slashes, numbered tally boxes, reflection line; both engines' verbatim invariants asserted by reconstruction, the Parsons-key way. Fifty-two engines. The rehearsal flake was finally CAUGHT BY NAME this item and killed at its root: no lookup on the shell Open dialog earned trust under load (traceability finding 9), so reopen now goes through the `libraryPicker` seam the in-process tests always used — the rehearsal drives no shell dialog at all, and the UIA suite dropped from ~27 s to ~7 s. 628 tests green across three consecutive full runs; samples gained both presses, double-run identical; the pilot zip re-pressed.

### 5. ~~The card and protocol trio (atlas #58, #136, #68)~~ — **done 29 Aug 2026**
On deck machinery that already exists: **Concept Sort Studio** — examples, nonexamples, and ambiguous cards from three teacher lists, each kind border-distinguished by shape, with a teacher-only key; **Discussion Role Wheel** — role cards of "role | accountable action" pairs, plus a rotation note so no learner is permanently passive; **Peer Feedback Builder** — a bounded protocol sheet: teacher sentence stems, evidence rules, and the author-decision box that keeps the author in charge. **DoD:** the usual press bar. *Small.*

*Struck 29 Aug 2026.* Three engines in `ProtocolPresses.cs` under `press.protocols`, the twenty-second recipe — prohibition: inventing examples, roles, stems, or feedback content; the sorting judgment, the discussion, and the feedback remain the learners' and the teacher's. The deck loop follows the algorithm-cards pattern rather than widening its private helper (the handover's own fork, taken on the two-kinds-versus-three grounds), and the border language gained a third shape: single border = example, double border = nonexample (the control-card precedent), **dashed frame** = deliberately ambiguous — the card that is MEANT to start an argument, honoring the ambiguity-registry lineage. The concept word itself never prints on any card, only on the `TeacherOnlyNotice` key — asserted by a DoesNotContain sweep over every card label. The rotation note rides every role-card page in the teacher's own words; the author-decision box holds the sheet's bottom quarter with the heaviest border on the page. Fifty-five engines. 638 tests green, twice; samples gained all three presses, double-run identical; the pilot zip re-pressed.

### 6. ~~Glossary Garden (atlas #147)~~ — **done 29 Aug 2026**
An illustrated-by-structure bilingual unit glossary: teacher entries of "term | meaning" or "term | meaning | translation," rendered through the engine's existing bilingual nodes with correct `lang` semantics — consistent terminology in print, the translations the teacher's own and verbatim. Honest note attached: like every bilingual output, its rendering falls under the multilingual seat's week-3 review; building the press is zero-gate, blessing its typography is not ours. **DoD:** the usual press bar; bilingual pairs verbatim with correct language tags. *Small-medium.*

*Struck 29 Aug 2026.* `GlossaryGarden` under `press.glossary`, the twenty-third recipe — prohibition: translating, correcting, or leveling the teacher's entries; the translations are the teacher's own, placed verbatim. A semantic document: each entry is a term heading and its meaning, and bilingual entries add a `BilingualPair`, so the `lang` and `dir="auto"` semantics come from the tested renderer, never from the press — verified down to the rendered sample's ink (`lang="es"` on every translation, diacritics intact and escaped). The document's own `Language` is the source tag; language tags are validated loudly ("'e n' is not a language tag; write it like en or es-MX"); mixed monolingual/bilingual lists are honest (no pair where no translation). **The standing honest note, restated as the menu requires: building this press was zero-gate, but its bilingual typography answers to the multilingual seat's week-3 review — that blessing is not ours to give from a keyboard.** Fifty-six engines. 643 tests green, twice; samples gained `press-glossary-garden`, double-run identical; the pilot zip re-pressed.

### 7. ~~Machine evidence, extended — determinism as a CI gate, coverage breadth~~ — **done 29 Aug 2026**
The byte-identical claim is currently proven on this machine and taken on faith in CI: add a **CI step that runs the SampleGenerator twice and diffs the hashes**, so every push re-proves determinism on a machine nobody tuned. And the coverage breadth clause once more: measure honest, then add `Foundry.Modules.DeterministicPress` (last measured 96.6%) and `Foundry.Modules.BuiltIn` (88.7%) to the threshold assemblies. **DoD:** both CI steps green; numbers recorded in the workflow checklist. *Small.*

*Struck 29 Aug 2026.* The determinism gate runs the SampleGenerator twice on the runner and hash-compares every byte of all 38 sample files, with exactly ONE named exclusion — the known `.ocfproj` writer nondeterminism, flagged in the log but not failing, its exclusion line marked for deletion when the writer task lands (and the gate says so out loud if the file starts matching). The script was rehearsed locally verbatim before it was trusted to a push. Coverage was re-measured honest-first: `Foundry.Modules.DeterministicPress` 96.6%, `Foundry.Modules.BuiltIn` 88.8% (a tenth above the menu's stale figure), combined threshold set 93.0% — both assemblies joined the filter and the numbers are recorded in the workflow checklist per house habit. Both steps verified green on the actual CI run for this commit, not assumed from the local rehearsal.

### 8. ~~The samples gallery for honest-ink.org~~ — **done 29 Aug 2026**
A deterministic gallery page joining the site generator's six: a curated set of press samples embedded as inline SVG — the gift made visible to a district or teacher before they ever install anything. Same rules as the site itself: no timestamps, byte-identical, self-contained; **publishing remains the typist's act.** **DoD:** gallery page in the deterministic build; byte-identical across runs. *Small-medium.*

*Struck 29 Aug 2026.* `SampleGallery` in the SiteGenerator: eight curated single-sheet catalog entries — graph paper, hundred chart, timeline, bar chart, trace table, fluency rehearsal, one-point rubric, peer feedback — each built at its own defaults, approved at the SampleGenerator's fixed instant, and rendered as inline SVG by the same `AccessibleHtmlRenderer` that prints them; the captions are the presses' own accessible descriptions, the same words a screen reader hears in the app. The page joins the nav as the site's seventh, and the tests assert what the menu asked: eight figures, eight inline SVGs, no `<img>`, no `<script>`, byte-identical across doubled builds — proven in-suite AND by running the tool twice and hashing all seven site files. The pilot zip was NOT re-pressed: no app byte changed (the last re-press, item 6's, still matches the tree — stated so the freshness claim stays honest). **Publishing the site remains the typist's act; this item only made the artifact.**

### 9. ~~The Studio Sampler — the forge prints its own catalog~~ *(not in the atlas; born of the enriched product)* — **done 29 Aug 2026**
Possible only now, because the press catalog is DATA and the booklet imposer is real: one deterministic booklet, one representative page per engine at its defaults, imposed saddle-stitch, cover page listing all forty-eight with their recipe lanes — the entire studio as a printed, foldable object a pilot teacher holds in their hands on day one. The catalog builds it, the imposer binds it, the PDF press inks it; no single atlas entry describes it because no single capability could have made it. **DoD:** `press-studio-sampler.pdf` in the samples run, byte-identical; every catalog engine represented exactly once, asserted against the catalog itself so a new engine can never be silently missing. *Small-medium.*

*Struck 29 Aug 2026.* `StudioSampler` in the module — a composer over the catalog, like Big Print Shop never a catalog entry. `Plan()` partitions all fifty-six engines against the booklet's one uniform page: fifty-one bound as their default first sheets, five named on the cover with honest markers — timeline and bar chart (landscape, `*`), booklet guide, bell-to-bell, and glossary garden (prose, `†`) — and the partition is asserted against `PressRoomCatalog.All` itself, so a future engine can never go silently missing. Composition honors the layering wall: the module builds the document, the SampleGenerator approves and imposes it via `PdfSides` + `WriteImposed` into `press-studio-sampler.pdf` (52 content pages, 359 KB), double-run byte-identical — and the CI determinism gate now re-proves the whole booklet on every push for free. **The sampler earned its keep the way the rehearsal did — by being the first thing to push every default sheet through the native PDF press:** it found the estimation-first default carrying U+2212 (no WinAnsi encoding — a default that could not reach native PDF is a broken default; now the ASCII hyphen), and the bingo free-center `★` glyph likewise unprintable — now a DRAWN five-chord star from exact constants, geometry instead of type, truer to the house than the glyph ever was. One unrelated flake noted by name: `ImageNormalizerTests.A_burned_region_destroys_the_pixels_beneath_it` failed once under full-suite load, green in isolation and on both following runs. 649 tests green; the pilot zip re-pressed.

### 10. ~~Class Sets — the seeded engines, multiplied~~ *(not in the atlas; born of the enriched product)* — **done 29 Aug 2026**
The seeded presses (word search, bingo, retrieval grids, grouping, Parsons) already promise "same seed, same page." Compose that with a roster-free count: **one document, N variants of the same seeded artifact** — thirty word searches of the same teacher words, each grid different, each footer naming its variant seed, so neighboring desks can't copy and any lost sheet is reprintable by its number. A generic composer over any seeded catalog engine, not a new press. **DoD:** variant seeds derived deterministically from the teacher's base seed; every variant carries its number and seed in ink; determinism asserted across the whole set. *Small-medium.*

*Struck 29 Aug 2026.* `ClassSets.Compose` — a composer over any seeded catalog entry, never a press of its own. Variant seeds are `baseSeed + variantIndex`, exactly as the handover prescribed; every sheet carries "Variant v of n · seed s" in ink AND in its accessible description; teacher-only notices ride along named by their variant (two Parsons variants arrive with two answer keys, each naming its seed); an unseeded press refuses loudly ("class sets multiply seeded presses only"), as do out-of-range counts and seeds. The load-bearing test is the reprint claim itself, proven byte for byte: **variant 2 of a base-100 set, with its stamp set aside, serializes identically to the single press at seed 101** — a lost sheet really is reprintable by the number printed on it. No two grids in a set match, asserted. The composed document passes Gate B as itself; the gate is structural, not hereditary. 653 tests green, twice; samples gained `class-set-word-search` (three variants, puzzles and keys), double-run identical and CI-gated; the pilot zip re-pressed.*

---

*With item 10, every item on this menu is struck — the fourth forge menu is exhausted. Fifty-six catalog engines, twenty-three recipes, two composers born of the product, 653 green tests, the pilot artifact fresh, the determinism gate standing watch in CI, and the site carrying the studio's own portraits. The letters go out September 1–4; the pilot opens September 8; none of that is forge work. Per this menu's own resuming words: the honest next act may not be a fifth menu at all — it may be rest, until the world writes back.*

## III. What must NOT be built, carried forward and extended

- Everything the three prior menus refused, verbatim: the seat's territory, the instrument's territory, Inference.Local, human checklist rows, PNG raster export, faded glyphs before font provenance, Notation Bench before its reviewer, inference modules before their roadmap stage, printing without approval, presets before the council shapes the format surface.
- **Tagging, versioning, installing, and distributing are the typist's acts.** Item 1 produces the artifact and its checksums; it does not bump `EngineVersion`, cut a tag, touch the pilot machine, or upload anything anywhere.
- **No auto-approving CLI runner.** A headless command that approves its own output would make the machine the signer of Gate B; the SampleGenerator's evidence fixtures are the only sanctioned non-teacher approvals, and they stay clearly labeled as such.

Bench stock (worthy, unranked): Focus Beacon (#25), Plan B Press (#26), Morning Launch and Closing Loop (#134–135), Help Protocol Builder (#138), Vocabulary Ladder (#73), Print Queue Planner (#175), a Print-and-Play game-board press (#166), and lifting `Foundry.App.WinForms` coverage (72.9%) toward the threshold.

## IV. Resuming

Unchanged: **"Proceed with …"** adopts and starts an item; completed items are struck with a dated note, never deleted. And the typist's principle is now part of the divination rite itself: future menus draw from TWO sources — the atlas, and the compositions the enriched product newly affords — and must say which each item came from. If this menu exhausts before the letters answer, the honest next act may not be a fifth menu at all — it may be rest, until the world writes back.

**Any hand enacting this menu — a future session included — reads the [enactment handover](2026-08-29-forge-enactment-handover.md) first:** it carries the repository state, the craft idioms and sprung traps of the three exhausted menus, per-item build guidance, and the closing rites that every item must pass through.
