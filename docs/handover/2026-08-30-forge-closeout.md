# Forge closeout — doors opened, floor held, human gates kept

**Date:** 30 August 2026 · **Audience:** the typist, council, District IT, the next maintainer, and any automated contributor. Read [AGENTS.md](../../AGENTS.md) before acting.

> **Historical repository state.** The later [Audit D enactment handover](2026-08-30-audit-D-enactment.md) is the explicit current-state marker. This closeout remains the inherited full forge record.

This was the repository state at the commit that first contained this file. It closes every forge-owned part of the standing directives in [the moved calendar](2026-08-29-the-moved-calendar.md), records what measurement changed, and names every remaining human, release, and compatibility hold that code must not counterfeit as complete. The engine remains `0.7.0-alpha`. No version, tag, signature, installation, distribution, publication, filing, correspondence, district decision, GitHub protection-setting change, or protected-seat acceptance was performed.

## I. Exact measured state

- The exact `global.json` SDK, **10.0.302**, resolved for the final closing sequence. The Release solution built with warnings as errors: **0 warnings, 0 errors**.
- Full `dotnet format` fix completed and the following `--verify-no-changes` exited **0**.
- The first complete coverage-enabled final run was red at **1233/1234**, with zero skipped or unexecuted tests. `LocalizationRulesTests.The_winforms_app_carries_no_user_facing_string_outside_the_catalog` reported all twelve HTTP/IPC grammar literals in `AppServices.PrintViewHandoff.cs`, beginning with `READY ` and the exact tokenized `GET` and ending with the complete 404 response. Those strings are a private machine protocol rather than translated chrome. The source guard now carries an exact file-and-literal machine-protocol inventory, refuses any unrecorded spaced literal, and refuses stale inventory entries; the focused rule then passed **1/1**. Only after that measured correction did two complete coverage-enabled stability runs pass **1234/1234** each, with no failed, skipped, or unexecuted test. All **213** UI-automation tests, including the headed walks, executed in both runs; none of the three historical under-load sightings reproduced:

  | Suite | Tests |
  |---|---:|
  | Unit | 532 |
  | Integration | 226 |
  | UI automation, including headed UIA | 213 |
  | Contract | 100 |
  | Rendering | 80 |
  | Instructional evaluations | 75 |
  | Accessibility minimum floor | 8 |
  | **Total** | **1234** |

- SampleGenerator ran twice on the final source tree and all **40 files** matched byte for byte, with no exclusion. The first comparison command falsely reported all 40 as mismatches because PowerShell interpreted `Where-Object RunA -ne RunB` as comparison with the literal string `RunB`; inspecting an exact pair exposed the harness error. The corrected explicit `$hashA -ne $hashB` comparison recorded zero mismatches. The SHA-256 of the sorted filename-plus-hash manifest is `22E7858ADC3239C46092ECFD971C6181D8AA97303725A7F63BF8EE5187566CDF`.
- Two final neutral UI-catalog exports were byte-identical at **996 strings**: packet SHA-256 `A7E43F7FDE26D947C3727196FC8CA341FB9BFFA4C58E78126AC233689E73F6CF`, bound to source digest `f22b6e792bc6119942897734ba2d9e70af8fc0410462bb028a18aeaccb3d984d`. This is the deterministic neutral review packet, not a reviewed second language and not an activation pin.
- A last catalog-boundary audit found that valid reviewed `&&` literal escapes were passed verbatim to window titles and the newly non-mnemonic status labels. Every literal sink now decodes only its static catalog template before raw dynamic arguments are inserted; mnemonic-capable chrome keeps the validated marker syntax. The new form proof first measured **25/26**, not green, because its synthetic Node Editor had not been shown before `PerformClick`; after showing the real control, the synchronized initial, pending, and invalid status announcements and the full catalog suite passed **26/26**.
- The first full run after combining that audit measured **896/899**, not green. Both floor cases named the now-empty Node Editor status label because it remained visible, and the built-in-studios UIA walk still searched for raw mnemonic-marker text after the explicit accessible names were correctly decoded. The empty status is now hidden until it carries synchronized text and name, and the walk asks for the exact decoded accessible names. The floor suite then passed **8/8** and the headed test passed in isolation without increasing its timeout before the complete runs restarted.
- A later second stability run measured **898/899**: the Built-in Studios walk found the real visible Gate B window while UIA's modal property was false. The first repair waited for that provider property and initially appeared sufficient. Final repetition below disproved that conclusion: the property could remain false for the full bound, and exact native evidence later isolated intermittently missing window ownership in the parameterless production `ShowDialog` route. The corrected route and proof are recorded below; the initial red remains here because measurement must not be rewritten into hindsight.
- Repetition then exposed two more one-shot standard-provider races in that same new Built-in Studios walk: the instrumented door-selection replay passed **3/4** before a `Select` request was acknowledged without changing selection, and the next batch passed **2/3** before an `Expand` request exposed no combo items. The harness now re-resolves and reissues only idempotent ListBox `Select` and ComboBox `Select`/`Expand`/`Collapse` requests inside the original bounded wait, with candidate, match, and attempt counts. It does not retry approval or any output action. The repaired exact path passed **10/10** isolated replays without increasing a timeout. These measured harness defects do not diagnose either historical timeout sighting.
- An intermediate Access-authority design first measured **888/889**, not green, because its new user-facing refusal text bypassed `UiStrings`. That route was then rejected entirely after an adversarial Spanish assessment fixture proved that typed prose cannot establish purpose across languages. The final design holds Access at the catalog and public-API boundaries; this red result and its assertion message were retained rather than rounded into an expectation.
- The first complete closing attempt was red, and its names and messages were retained. It found two real regressions introduced by hardening: old headed commands had not adopted the newly mandatory disposable library, and the supposedly frozen 0.1 snapshot renderer inherited CRLF from its source file. Both were fixed and rerun in isolation before the two complete green runs. A red measurement was not rounded into a green expectation.
- The completion audit's first full run was likewise red at **896/897**: `LocalizationRulesTests.The_winforms_app_carries_no_user_facing_string_outside_the_catalog` retained all nine internal access-key context labels that the source guard rejected as inline prose. Those diagnostics became stable space-free identifiers, the focused rule and catalog tests passed, and only then did the two 897-test green runs proceed.
- The first GitHub Actions run for commit `b32f062` was also red, in the pseudo-locale 125% floor case. Its trace proved that the hosted desktop had silently clamped the nominal 1366×728 top-level form to a 1044×728 outer window and 1028×689 client area. The exact geometry and failure reproduced locally at 1044×728; the same Press Room passed at the intended 1366×728 and at 1071×728. A first harness correction made the requested outer geometry explicit, and the next hosted run for `a3f71b5` honestly failed that assertion in both locales: WinForms applied the same maximum-track clamp even to the child Form. The final harness never scales, creates, or shows a Form window. It measures only each Form's non-client extent, moves its complete live control tree with z-order, inherited chrome, and mirrored RTL window flags preserved into an unclamped logical client canvas, lays it out at physical-client ÷ scale, scales that canvas, and corrects rounding to the exact physical client. Client plus measured frame must equal the requested outer floor. A local comparison put the key pseudo Press Room text box at the same rectangle as a real 1366×728 window, with only one-pixel internal rounding differences elsewhere; Module Studio also lost an erroneous over-tall list left by the old clamped path. This is a client-layout test-harness repair, not an application-layout diagnosis; headed UIA separately retains real-window coverage.
- The continuation audit also retained its own red-to-green trail. Catalog/access-key work first measured **22/32**, then **32/32**; a combined application slice measured **53/56** before routine-name, refusal-type, and malformed-browser corrections, then **56/56**. The first broadened full run was **1022/1023** because four new capture messages bypassed the localization catalog. An image concurrency assertion then failed with `Expected: not 30; Actual: 30`; the test was replaced by a synchronization proof of work leaving the caller thread rather than a scheduler-id guess. The rights boundary first measured **5/10**, then **10/10 twice** and **93/93** across the broader package slice. Capture UI work moved from **8/10** to **11/11**, then exposed four compile errors and a **9/14** hidden-retry-selector mismatch before reaching **14/14**. These were deterministic findings, not reclassifications of the three under-load sightings.
- The final export audit found that holding a junction entry without delete sharing did not prevent an already-open write handle from changing its reparse metadata, while the original absolute rename would resolve that mutable path again. The first repair used a non-null `FILE_RENAME_INFO.RootDirectory`; three focused runs retained its deterministic `ERROR_INVALID_PARAMETER`, because Windows requires a null root for a same-directory simple-name rename. The corrected native `FileRenameInformation` buffer carries only a validated leaf and a null root, so the already-open stage is renamed inside its current physical parent without another path resolution. The same audit then found that cancellation cleanup still reopened the random stage pathname; cleanup now marks the exact still-open stage handle with `FileDispositionInfo` before disposal and aggregates a failed disposition with the original failure. The focused transactional-export slice then passed **12/12**, including exact buffer/ABI shape, alternate-data-stream refusal before stage creation, handle-owned bytes, parent holds, cancellation, replacement, handle-only cleanup, and cleanup residue.
- The first merged run in that closing sequence was likewise red: **1088/1089**. `ReviewSurfaceContractTests.Mnemonics_are_unique_so_every_action_has_one_unambiguous_access_key` reported `Expected: 7; Actual: 6`; the improved diagnostic isolated two `p` mnemonics in `CaptureForm`. The purge-retry access key moved to `u`, the focused mnemonic/catalog slice passed **27/27**, and that historical suite then reached **1089/1089** before the final capability-boundary work and current-tree stability runs.

## II. The forge order, honestly closed

| Standing item | State | What is true now |
|---|---|---|
| Doorless modules | **Struck** | The old count was wrong: Family Bridge made **ten doors**, with **eleven modes** because Scaffold Smith has two. One generated standard-controls studio exposes all of them. Eight modes have synthetic Green starters. Access Remix is visible but has no catalog build delegate or public remixer API pending protected specialist purpose-authority review; Exit Lens and Rubric Relay remain visible and unavailable behind written district authority. |
| Real second UI chrome language | **Human hold — not struck** | The complete deterministic neutral-packet workflow, strict reviewed-catalog loader, LTR/RTL chrome, provenance contract, pseudo stress path, CLI/environment selection, and exact-file compiled allowlist exist. Runtime tests exercise every press, built-in mode, All Aboard mode, Review tab, node editor, vector primitive, and the loaded-project preflight under pseudo RTL. Activation rejects access-key collisions within each actual simultaneously visible surface using WinForms' native UTF-16 and current-culture matching semantics, while allowing reuse across separate forms and mutually exclusive editor variants. Dynamic labels and status payloads keep `&` literal; static chrome retains its validated access keys. The final neutral packet is measured above at 996 strings with exact packet and source digests. The allowlist is empty because no multilingual-seat-reviewed translation exists. JSON cannot appoint its own reviewer, and a path cannot approve its own bytes. |
| 1366×768 minimum floor | **Struck** | The contract models a 1366×768 screen with the exact 1366×728 working area left by the reference 40-pixel system bar; it is a floor, never the design target. Every production Form base scenario, all three Review views, every press and built-in mode, every admitted node-editor variant, and every vector primitive layout is exercised in neutral 100% and stretched pseudo 125%. Capture's normal, normalization-retry, pending-operation, and purge-retry states are included; the Press Room and All Aboard exercise cancellable export and their busy-state gates. A reflection inventory fails if a future production Form lacks a deliberate scenario. Interactive and non-interactive chrome must remain unclipped; long standard-list items must either fit or expose a horizontal reading path. Reachability through an owning scroll container is accepted only when a real `ScrollControlIntoView` exposes the target and preserves inspection state. The design surface remains 1180×720 and ordinary larger displays remain the intended experience. |
| Atlas, council-first | **Prepared — session hold** | The needs-first [priority-session packet](../council/atlas-priority-session.md) is ready and explicitly unrun. Need capture precedes candidate names; absent protected seats create holds; withdrawal and compensation confirmations, the cohort's still-unenacted decision/quorum record, participant review, and council-record freeze are explicit fields. Feasibility and product-owner disposition remain separate. No atlas rank or newly divined product goal was selected by automation. |

No authorized seat-supplied catalog or council evidence was presented to this session. No protected Drive material was inspected.

## III. What the teachers can actually reach

The running application now has three first-class doors:

1. **Press Room** — 56 deterministic engines across 23 recipes.
2. **All Aboard** — the complete visual-support studio.
3. **Built-in Studios** — Board to Brief; Access Remix; Directions Duet; Scaffold Smith packet and task-entry modes; Talk Moves Studio; Lesson Loom; Exit Lens; Rubric Relay; Source Lens; and Family Bridge.

The shared studio is catalog-driven, not ten copied forms. Stable submitted values are independent from translated labels. Every available mode owns typed field readers, its recipe validator and warning inventory, fresh Gate B review, audience/scale/language-order render profile, print, print view, declared export targets, and atomic project save. Any field, option, mode, or source change revokes approval and every sink.

Access Remix's deterministic layout engine remains internally testable, but the shipped type, result, and methods are non-public and the catalog mode has no build delegate. All Aboard approvals unlock only All Aboard outputs and remain purpose `Unknown`, including assessment-shaped English and Spanish fixtures. No raw document, draft, generic module approval, keyboard declaration, mutable package purpose field, saved package, or typed claim can enter Access through the application or its public engine surface. The hold remains until protected specialist review establishes a purpose-authority route that cannot be granted from typed content.

## IV. Gate B and package trust were made exact

Gate B now has three keyboard-reachable views: semantic elements, exact source comparison when source exists, and a visibly watermarked unapproved visual derivative made by the same HTML/SVG semantic core as final output. Assigning `WebBrowser.DocumentText` is not readiness. Approval remains disabled until `DocumentCompleted` exposes a browser-only SHA-256 marker binding the exact revision, render request, derivative bytes, and load generation, and every required DOM image is complete with positive natural and rendered bounds. Malformed or unready images fail closed. Edit, refresh, navigation, error, marker mismatch, and stale completion revoke readiness. The marker enters neither approved output nor the semantic derivative.

The `.ocfproj` reader now treats manifest, semantic artifact, snapshot, render profile, validation envelope, assets, provenance, entry inventory, and exact renderer identity as one hostile package. A safe but unrelated snapshot, active content, unknown renderer, forged warning text, metadata refresh, duplicate entry, traversal, size/inflation excess, asset mismatch, or half-context is refused. Saved validation and render profiles bind to the exact document SHA-256. Admitted package assets reopen through a validated read-only catalog, travel into Foundry rendering, and survive exact resave only after their filenames, media structure, hashes, provenance, and document references agree. Package saves stage to one unique sibling, flush through the file handle, fully revalidate, then atomically replace or move; failure and cancellation preserve the prior destination and remove the bounded stage.

The compatibility boundary keeps a frozen exact renderer for the admitted `0.1.0-dev` package and the current renderer for current packages. The first closing run proved why “frozen” must include source-line-ending independence: a raw CSS literal acquired CRLF under the repository checkout policy and no longer matched the historical LF bytes. The legacy writer now spells its newlines explicitly. The earlier focused measurement was 26 package-upgrade plus 10 operator-host cases, **36/36**; the two complete final current-tree passes above supersede that slice for the closing tree. The separate admitted 0.7 standalone snapshot-visual gap remains named below.

## V. The repository compatibility-preparation seam is complete; a managed upgrade path is not

[ADR-007](../adr/ADR-007-managed-pilot-upgrades-are-side-by-side.md) remains **Proposed**. The [runbook](../release/managed-pilot-upgrade-runbook.md), strict plan schema, and `Foundry.Tools.ProjectUpgradeHost` provide two operator-controlled operations:

- `review` canonicalizes and validates an exact closed plan and reports its SHA-256;
- `prepare` requires that unchanged digest and creates a new candidate library without changing the source.

Each source is addressed by root, relative path, engine, schema, and SHA-256 and held open read-only across address, hash, full validation, copy/compatible transform, and final source check. Source and candidate roots must exist, be canonical, non-reparse, separate and non-overlapping; the candidate begins empty. A batch lock excludes another engine batch, every partial is bounded, completed output is fully validated before atomic promotion, one batch failure removes every output created by that batch, and cleanup residue reports a content-free stable code.

`tools/publish.ps1` is now an **unsigned pre-sign builder**, not a release publisher. It accepts Release only, requires a clean exact commit, performs a repository-configured locked `win-x64` restore followed by `--no-restore` publish, verifies compiled engine/product/dependency identity, separates PDB evidence from payload, rejects local/superseded SourceLink paths, records signing roles and pre-sign hashes, and promotes only an explicitly named `unsigned-pre-sign` bundle under one cooperative lock. `tools/finalize-signed-package.ps1` is a separate typist-invoked, non-signing verifier: it requires a clean signed annotated tag at the exact manifest commit, an authorized timestamped Authenticode signature on every signable first-party file, byte-identical unchanged files, and exact inventory identity before it regenerates signed checksums and hashes the final signed zip. Both routes have rollback attempts and preserve recovery data when rollback is incomplete. Their parsers and **11/11** structural/algorithmic contracts passed; neither script was run.

The current tree deliberately cannot pass that release route: `EngineIdentity` is `0.7.0-alpha`, compiled assembly/dependency metadata still resolves as `1.0.0`, and the existing unsigned `v0.7.0-alpha` tag identifies an earlier commit rather than this tree. No version or tag was changed. Cohering those identities, replacing or superseding the tag, signing, installing, distributing, or publishing is a typist act. No separately authorized operator package for the compatibility host exists. The runbook gives an explicit source-tree evaluation invocation without representing that command as an installed tool. This code does **not** choose a candidate application, ratify the ADR, install or uninstall, assign Intune policy, sign, version, launch, replace the live library, retire the prior library, or decide rollback. Those remain typist and District IT acts, and a managed-device upgrade and rollback remain unratified and unproven.

## VI. Recorded debts and sightings

- **Accessibility suite:** empty-suite debt struck. It now holds eight machine tests for the exact minimum-floor contract and passed twice. The eighth catches over-wide standard-list text without a real horizontal reading path; the Access hold warning measured 1,363 px against 1,006 px available at the neutral floor, and the repaired live control exposed native `WS_HSCROLL`. This is not NVDA/Narrator, contrast or high-contrast, 200% zoom, human reading-order, or ACR/VPAT evidence; those remain owned human accessibility work.
- **Deprecated Node 20 actions and SDK drift:** debt struck. Every workflow action is exact-commit pinned to its Node 24 generation. A completion audit then found that `global.json` named 10.0.302 but allowed `latestFeature`, and the successful hosted run had in fact selected 10.0.400. Roll-forward is now disabled and CI requires `dotnet --version` to equal the exact JSON version before restore. Repository tools are manifest-pinned; all 24 portable project graphs and the 11 applicable `win-x64` graphs carry committed lock files; CI, CodeQL, site generation, tools, and pre-sign packaging use the explicit repository NuGet configuration and locked restores. CI is configured with read-only contents permission, direct/transitive vulnerability audit, full-history secret scanning, and retention of two distinct CycloneDX 1.7 inventories. Local validation measured the 34-package repository/build graph and the 18-component restored WinForms `win-x64` project-dependency graph. The latter proves license evidence, exact dependency edges, and application-root reachability for that NuGet graph; it is not an OS/framework inventory or a promoted release-artifact SBOM, and artifact correspondence is explicitly not claimed while engine and assembly versions disagree. Exact hosted conclusions and retained artifacts remain post-push evidence. The gitleaks binary is pinned to SHA-256 `551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb`, independently matched to the official v8.30.1 checksum without execution. CI and read-only CodeQL are release-policy requirements, not protected-branch enforcement; no branch protection or ruleset exists. The site remains manual-dispatch only, and no site dispatch occurred during this continuation.
- **Standalone 0.7 snapshot visual continuity:** open. A reopened `.ocfproj` now recovers its validated asset bytes for Foundry rendering and exact resave, but the admitted 0.7 `snapshot.html` renderer is frozen to its semantic symbol placeholders. The package therefore does not yet carry the same symbol visuals when that HTML is opened without Foundry. Changing those bytes under the same renderer/version identity would break the frozen compatibility route; closure requires a new documented renderer/version route and a typist-authorized version change. No version or frozen snapshot byte is changed by this finding.
- **Image flake — correction recorded 30 Aug 2026:** ~~diagnosed and fixed, no longer merely a sighting.~~ Official .NET 10 source supports the existence of a separate unsynchronized encoder-inventory hazard: `Image.Save(stream, ImageFormat.*)` could observe a default encoder GUID and throw `ArgumentNullException (encoder)`. A fresh-process probe measured **2/20** before the explicit-codec cache and **0/50** after it, and the current focused regression later passed **20/20**. Those raw per-process logs were not retained, the original `ImageNormalizerTests.A_burned_region_destroys_the_pixels_beneath_it` assertion message had already been lost, and no evidence binds the repaired encoder hazard to that under-load failure. The previously proposed 64-way current-code probe remains unrun; it could characterize the guard, not retroactively recover the original failure signature. The original image failure therefore remains a sighting, not a diagnosis, alongside the two historical headed UIA timeouts.
- **Two historical headed UIA timeouts:** still sightings, not diagnoses. Both passed in isolation when recorded, never reproduced in the 57-run CI history inspected, and did not recur in the earlier complete closing runs. A later ten-repetition isolated replay of each test added **20/20** passing executions; that is still not an under-load reproduction or a diagnosis. Their names remain in the moved calendar; the `PilotDay` timeout message was captured, while the historical `Part3` message was not retained. Every named-control discovery and state wait in `Part3`, and every state wait in `PilotDay`, now records its stable phase, process state, expected control kind, candidate and match counts, elapsed time, and probe count in a content-free timeout message. Its diagnostic snapshot formats counters cached by the last completed probe plus a direct read of the owning local process state; it performs no post-timeout UIA or filesystem traversal, and a formatter failure retains only the exception type. The shared probe timeout remains 20 seconds and was not increased.

The final adversarial pass found three different deterministic UIA-harness defects, which must not be mislabeled as explanations for those historical sightings:

1. storage-capable harness modes could inherit the real default teacher library when the disposable root was omitted;
2. missing or misspelled harness modes could fall through into the production Press Room; and
3. `--export-to` could bypass Save As and overwrite an arbitrary PDF path.

All three now fail closed. Press Room, All Aboard, and module harnesses require an exact empty `%TEMP%\ocf-rehearsal-{GUID}\0.7.0-alpha\prepared-library`; one exact known harness mode is mandatory; and direct export accepts only one new fully qualified `.pdf` directly inside that validated empty root. Diagnostics contain only the exception type, never a message, path, stack, or authored text.

A final independent UIA review then found four additional harness weaknesses before commit: Gate B used one-shot acknowledgement and approval discovery while browser readiness was still asynchronous; Part3's final process-exit check bypassed the diagnostic wait; a failed combo inspection could begin a second throwing cleanup wait and mask the first failure; and PilotDay passed disposable paths through the mode label used by startup diagnostics. Gate B now polls readiness read-only, toggles a required acknowledgement at most once, verifies that state without retry, waits for enabled approval, and invokes approval exactly once. Part3's exit uses the same bounded diagnostic wait, combo cleanup begins only after successful inspection, and the stable mode label is separate from raw process arguments. The review also exposed a production defect: `ReviewForm` derived `Enabled` from a child's effective `Visible` value before its hidden parent Form was shown, so a required acknowledgement could open visibly disabled for a teacher even though UIA could still mask the error. Both properties now derive from the required-acknowledgement predicate, and the shown-form contract asserts that the required checkbox is enabled. The initial focused readiness and headed set passed **9/9**.

Independent repetition refused that single green as closure. Five identical nine-test batches measured **8/9, 9/9, 9/9, 9/9, and 8/9**. The first red was the Built-in Studios visible Gate B whose UIA modal property remained false for 20 seconds; the last was Part3's one-time `Invoke` return of `0x8000FFFF` while approval closed the final window. Native per-invariant evidence then reproduced the modal class in Pilot replay four: dialog and expected Press Room both existed, remained visible, shared the exact process and UI thread, the dialog was enabled, and the Press Room was disabled, but `GW_OWNER(dialog) == expectedOwner` was the sole missing condition. The three identical ReviewForm routes had used parameterless `ShowDialog`; each now supplies its exact calling form as owner. The harness requires the exact visible/enabled dialog, exact visible/disabled owner, process, and UI-thread relationship and includes positive and wrong-owner native contract cases; UIA's independently stale modal flag no longer substitutes for that proof. Post-owner-fix Pilot passed **5/5**, and the ten-test combined path passed **40/40** across four batches without increasing a timeout. A final tightening then made clean exit code 0 mandatory and passed Part3 **1/1** before the two final complete runs. The separate Part3 provider return remains a sighting: approval is still invoked once, never retried, and only a clean observed process exit can establish completion. If no exit arrives, the bounded timeout retains the HRESULT and process state; a nonzero observed exit reports the HRESULT and exit code. None of these later findings diagnoses either historical under-load timeout.

## VII. Other hardening earned during the forge

- Every `ArtifactDocument` recursively freezes its node collections; edits create new revisions and cannot mutate an approved graph by alias.
- Language-bearing documents, bilingual pairs, StepRows, and builders share a bounded deterministic BCP-47 grammar covering language, extlang, script, region, unique variants, unique extensions, and terminal private use. Malformed keyboard tags block before Gate B.
- Directions Duet, Family Bridge, and Glossary Garden say **working glossary — not approved** and **not yet language-reviewed**. No text field manufactures review status.
- Gate A freezes each request before preview and execution, and the policy wrapper re-reads the current district policy after the provider's asynchronous capability report. Admission and its receipt bind the provider, deployment, pinned model, and provider-reported HTTPS origin; the actual normalized origin must be on the current allowlist. The Azure provider owns its HTTP transport with automatic redirects disabled, and every 3xx response fails closed instead of replaying an approved body to another origin. Restricted and undefined lanes fail closed even when a district maximum would otherwise appear broad enough.
- Reopened projects default to Amber until the teacher completes a three-part exact-document Green preflight. Package lane, recipe, module, notice, and purpose claims are not trusted provenance; origin warnings are engine-owned and require acknowledgement.
- Edge PDF export uses an isolated local profile, bounded stable-file completion, process-family cleanup, and retrying residue cleanup. It refuses remote or active content and preserves render options.
- Every renderer route now shares finite 100–200 text-scale admission, document/render-unit/text/output budgets, defensive catalog-byte ownership, and cancellation checkpoints. Raster admission caps cumulative referenced pixels at 100 million and each decoded PNG stream at 128 MiB. PNG validation checks the exact zlib scanline count, filter bytes, CRCs, and permitted chunk structure before decoder proof; JPEG validation checks required quantization and Huffman tables plus scan selectors and bounds before decoder proof. SVG admission carries the caller's cancellation token through its bounded XML walk and passive element/attribute validation, with checkpoints around the owned parse. Mixed or multi-node standalone SVG and mixed native/imposed PDF refuse instead of dropping semantic nodes. SVG wrapping is rune-safe and linear; native PDF caps pages, work, semantic text, and output at 32 MiB. The exact raster asset → Edge PDF test is named only for what it proves—asset resolution and a valid PDF—not unmeasured visual appearance.
- Press Room, All Aboard, and Built-in Studios export asynchronously through one transactional writer. A new sibling stage is created and retained by handle; the writer flushes through that handle while handles pin every mutable destination-directory component and the followed parent against deletion or replacement. The native `FileRenameInformation` promotion carries only a validated destination leaf and a null root, atomically renaming the already-open stage inside its current physical parent without re-resolving a mutable absolute path; separator, volume-separator/alternate-data-stream, and invalid leaf forms refuse before directory or stage creation. Cancellation and promotion share one compare-and-swap boundary, so a cancelled operation cannot later report completion. Failure and cancellation mark that same owned stage handle for deletion before disposal instead of reopening its pathname; a failed disposition remains explicit residue and is aggregated with the original failure. Every surface keeps mutation and output gates closed while busy and offers a keyboard-reachable Cancel action. Cooperative writers drain and remove their stage during close; the Module Studio waits at most two seconds, so stalled storage may outlive the surface and cleanup is then explicitly unconfirmed.
- Print view creates each approved HTML leaf through an exclusive `CreateNew` handle, flushes it to disk, reopens it read-only with `FileShare.Read`, and rechecks its physical identity, reparse status, and exact bytes while a separate exclusive active-marker handle holds the job. The process admits at most eight live file leases and, separately, at most eight live child helpers; a ninth request at either boundary is refused, never admitted by retiring unacknowledged work. While the lease still denies mutation, a hidden same-executable child revalidates the temporary and physical path, copies at most 32 MiB, verifies SHA-256 against the parent-approved bytes, and generates its own random 32-hex UUID loopback path token. Browser shell launch occurs inside that bounded child. The child tolerates nuisance connections but accepts only the exact tokenized `GET`, writes and flushes the approved HTML once, and exits 0; the parent treats that exit as proof that the response write and flush completed, not proof that the browser received, rendered, or retained it. One absolute 10-second child deadline covers shell launch, accept, request, and response, and the parent observes it for 12 seconds. On disposal, a still-running helper is killed alone with `Kill(false)`, never with its browser descendants; helper capacity is returned only after exit is positively observed. The parent zeros its handoff byte array and the child zeros its owned byte array; the parent releases the lease and attempts job deletion or quarantine in `finally`, and startup/shutdown cleanup retries residue. Press Room, All Aboard, and Built-in Studios run the handoff away from the WinForms message loop, announce a localized Opening state, and keep mutation plus every duplicate output sink disabled while it is pending. After the response write, their status says it was sent and requires the teacher to verify that it is visible before printing at 100 percent; expected launch/path failures instead produce one localized refusal without revoking the exact approval or escaping the UI.
- Loose symbol manifests are bounded at 2 MiB and 512 records, strict UTF-8 JSON with exact properties and depth, and reject duplicate, case-confusable, unknown, commented, or trailing-comma forms before binding. Asset files are bounded before whole-file allocation. Required provenance and the two export dispositions must contain substantive Unicode text rather than whitespace, joiners, or script marks alone; optional fields remain length- and control-checked. Open-pack export requires explicit attribution and modification dispositions and emits each raw, control-free value on one line inside exactly one fenced `text` block, so URL or HTML-looking text remains literal. The current catalog and teacher shelf remain blocked pending rights-seat wording; no keyboard text fabricates that review. Local teacher-symbol content temporaries and manifest byte buffers used to compute fingerprints are zeroed after use. Known-open license policy, the redistributable claim, media structure, filename, and hash must agree before lookup or export.
- The teacher symbol shelf now accepts only a publicly non-mintable, non-rewritable capability with privately owned normalized bytes. Its trusted R2-8 preflight requires teacher-confirmed Green, a fresh distinct metadata-stripped PNG, stable source envelope fields, and whole-session purge; public API and adversarial-normalizer contracts fail if a raw byte route or alternate factory appears.
- Capture now owns one store per session instead of exposing a root singleton. Import bytes are read under a 16 MiB encoded bound and zeroed after the store makes its defensive copy; decode dimensions are capped at 16,384 per side and 25 million pixels before canvas allocation; normalization input/output temporaries are zeroed. Cancellation, Gate C, completion, failure, replacement, and late races release the correct old or tentative references. A failed purge enters an explicit `PurgeIncomplete` state whose only available recovery is a speaking, keyboard-reachable retry; no pending capture or normalization operation can be waived into another transition.
- The FlashCap camera source owns an explicit open, start, frame, stop, and dispose lifecycle. Borrowed frame segments are zeroed before release on every acquired path, and owned or late-arriving frame buffers are likewise zeroed. Raw/automatic modes are refused when their conservative BMP bound exceeds the 16 MiB envelope; accepted JPEG, PNG, and BMP payloads report matching MIME types. Late open success and late faults are observed. If `StartAsync` succeeds only after cancellation and immediate cleanup has finished without pending serialized lifecycle work, a second bounded stop/dispose closes that late activation; if serialized cleanup itself remains pending, it is observed rather than raced by a duplicate lifecycle call. A driver operation that ignores cancellation and never returns cannot be killed in-process, so shutdown and cleanup remain unconfirmed in that case.
- The three recorded under-load sightings now have a headed reproduction harness for the two UIA targets that preserves names and full assertion messages, applies bounded contention, and performs no diagnosis by expectation. Its first local minimal-load exercise (one CPU worker, 64 MiB, one repetition per headed target) exposed a TRX-parser `PropertyNotFoundException` even though both child processes exited 0. After the parser repair, the same configuration recorded **2/2 passed** and did not reproduce either timeout. *Measured again 30 Aug 2026:* the harness now refuses dirty trees and binds its content-free summary to the exact commit, SDK, build state, host facts, and load settings. From clean commit `d54a2d2ec65fdad209c020cefc346617438c95f8`, three repetitions per target under two CPU workers and 128 MiB contention recorded **6/6 passed**, zero process timeouts, and no reproduction. The stronger result is still a non-reproduction, not a diagnosis; all three historical sightings remain sightings.
- The UI Automation harness now expects the Review tab control, selected tab, and all three tab items, and its content-free failures retain the test name and assertion message in the runner evidence.
- A real out-of-process UIA walk now enters Built-in Studios through the production Press Room modal, enumerates all ten doors and eleven modes, proves Access/Exit/Rubric cannot grant themselves authority, and opens real Gate B before the four output sinks unlock. No timeout was increased and no sink was invoked.
- GitHub's current organization membership API reports two active admin/owners, `honest-ink-edu` and `Spacejunk-io`; the stale one-owner handover statement is struck in place. Both accounts belong to the typist, so this corrects hosting redundancy without seating a second maintainer or closing R2-13.

## VIII. What remains, and whose act it is

1. **Multilingual seat:** supply, provenance, review in context, and approve an actual second chrome catalog. Only then may an authorized source review add its exact SHA-256 to the build allowlist.
2. **Council and product owner:** run the needs-first Atlas session, record present and absent seats, then create a separate feasibility record and written disposition. The template is not the meeting.
3. **District IT / product owner:** evaluate and ratify ADR-007 for an exact managed deployment; test installation, rollback, detection, signatures, and device policy outside this repository.
4. **Typist:** choose one coherent engine/assembly/dependency version, replace or supersede the stale earlier unsigned tag with an authorized signed tag route, then sign, install, distribute, publish, file, send correspondence, and perform every outward-facing release act. The pre-sign builder and signed-package finalizer remain unrun until that authority exists.
5. **Accessibility/AAC, multilingual, district, privacy/records, safeguarding, curriculum, and rights seats:** retain every ordinary gate. Time does not waive territory.
6. **Project:** seat the second maintainer, run the pilot, and keep all three historical under-load sightings named until measurement earns a diagnosis. The source-bound **6/6** broader UIA non-reproduction does not close either UIA sighting, and the image sighting retains no source-bound reproduction record of its lost historical signature.
7. **Future code/version route:** after the typist authorizes a new version route, carry validated symbols into standalone package HTML without rewriting the frozen 0.7 snapshot identity. *Hardened 30 Aug 2026 without changing schema 1:* open-pack export now refuses non-substantive attribution/modification dispositions and its inert draft emits source plus both dispositions inside one fenced literal block. The current 13-asset catalog and open-license teacher submissions therefore remain blocked instead of receiving invented rights text. Complete the OER/license-steward dispositions, then design a versioned ledger topology for license text, commercial-use status, consent/release semantics, and license-specific proof before claiming rights completeness; the rights seat retains review.

~~The forge has no remaining code-owned standing item hidden behind those human holds.~~ *Continuation correction and close, 30 Aug 2026:* the adversarial continuation found and corrected code-owned renderer, export, capture, asset, package, CI, supply-chain, and release-evidence defects; it also preserved the snapshot, ledger, protected-seat, managed-device, exact-version/tag, and outward-act gaps recorded above. The final current-tree closing rites are recorded in §I without changing version or tag: two complete **1234/1234** green runs, exact sample determinism, and an exact neutral-catalog export followed the retained red measurements. Honest Ink is more capable, but its strongest improvement is that it now has fewer ways to claim capability, review, provenance, readiness, release identity, or authority that it has not actually earned.
