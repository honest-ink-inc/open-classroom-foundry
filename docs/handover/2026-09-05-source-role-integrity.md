# Source-role integrity — engineering continuation

**Prepared:** 5 September 2026. **Branch:** `codex/source-role-integrity`.
**Starting source:** `8857a6474cc17cdfc7357dc56665b45dcced4fac`.

This supersedes the same-day
[fixture-disposal handover](2026-09-05-fixture-disposal-observations.md) for current
navigation only. Earlier frozen evidence and manifests are unchanged.
The [complete work register](../governance/accepted-improvement-register.md)
retains all 40 proposals, 227 atlas dispositions and the full release envelope;
the objective is not complete.

SourceLens review now checks the supplied title, source-card field/value set,
excerpt role/section and formatted citation exactly. A literal copied elsewhere
cannot substitute for its source role. Notes, row reordering and intact
excerpt-section movement remain possible. Two unchanged-production runs retain
11 typed-approval counterexamples each; two first-candidate runs retain two
additional boundary defects each. Final focused runs passed 93/93 twice:
26 role controls plus 67 existing SourceLens/catalog/review controls.
~~Full closing is pending at this preparation cutoff.~~ **Later 5 September
2026:** both complete local runs passed 2,609/2,609 after build, format and
rebuild gates exited 0. All 40 default sample files matched twice and the
frozen baseline. These are engineering checks, not visual QA or admission. The
[continuation evidence](../governance/source-role-integrity.md) separates those
stages and the original immutable nine-case baseline.

The separate test-instrument corrections passed 91/91 Unit and 16/16 Integration
checks twice after Release build/format/rebuild gates exited 0. The first common
build's two analyzer errors remain retained. Zero-time observations now use a
known-unstarted adapter; PDF checkpoint diagnostics preserve terminal faults/
cancellation and do not turn a pending observation into a test pass. Production
exporter/helper policy and all deadlines are unchanged. New owned-waiter cleanup
preserves the exception caught by the outer test; the pre-existing nested
replacement-helper exception-precedence residual remains outside that claim.

The [ledger](../evidence/evidence-ledger.json) records PR21's own failed CI and
successful CodeQL. The [sightings register](../evidence/sightings-register.md)
retains the new S-15 first caller and its 24 refusals, separate synthetic capture
and PDF-checkpoint signatures, and three isolated local non-reproductions.
No scheduler, OS or historical waiter outcome is invented. Local passes do not
replace the hosted verdict or supply equivalent load.

SourceLens is a sixth direct changed recipe identity in the scoped inventory;
the exact `source-lens@0.1.0` compatibility disposition remains held with the
five earlier recipe changes and separately classified shared semantics.
This is not an exhaustive C1-to-present equivalence proof. Calibration visual
QA still fails; the native-console matrix is unadmitted.

CC BY-SA 4.0 remains the selected educational-content license proposal, not an
operative grant. Exact ownership/covered scope and contributor assent remain
unsupplied. ADR-007 and ADR-010 remain Proposed pending separate exact answers.
No engine/recipe version, schema writer, migration or routing choice is inferred.

Real consent, custody/withdrawal, appointments/quorum/recusal and protected-seat
authority remain human work. H0–H7 remain NOT BEGUN. No real council or review is
invented. No main merge, site dispatch, version/tag, signing, installation,
distribution, filing or correspondence occurred. Push authority does not
supply the missing decisions or frozen human records; publication and release
remain held.
