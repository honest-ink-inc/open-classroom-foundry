# Kiosk-plan evaluation — useful residue admitted, mistaken provenance corrected

**Current repository state · Date:** 30 August 2026 · **Comparison base:** `5f424057ff9051264bdde1e4fad9494ac1cfda7b` · **Enactment:** `fc160f9`, `fe142f6`, `33be421`, `9311e82`, and `1a5c6f6`

This handover supersedes the [Audit D enactment](2026-08-30-audit-D-enactment.md) only as the current-state marker. It inherits that record's engine, UI, minimum-floor, Atlas, release, protected-seat, typist-act, burned-study, and historical-flake boundaries unless a row below expressly changes one.

## What the attachment actually is

The supplied working set describes itself as a review and enactment plan for `honest-ink-inc/open-classroom-foundry`. It expressly distinguishes Writer's Kiosk as a separate ancestor and its provenance says that ancestor was not cloned or read. It is therefore stale external evidence about Honest Ink at earlier commits, not evidence of improvements implemented in Writer's Kiosk and awaiting transfer.

The attachment was read as evidence, not instruction. Its signed disposition and work orders do not supersede this repository's ADR → plan → atlas → review hierarchy, the current handover, protected-seat ownership, or the typist-only boundary. Nothing from the attachment or its scratch clone was copied into this repository.

## Complete proposal disposition at current HEAD

| Attachment items | Current disposition |
|---|---|
| A1–A7 review conventions | **Review-repository work, not imported.** Namespaced errata, dated measurements, shipped governance, shape-based probes, stop-ship routing, manifests, and packet checks are sound review practices. Honest Ink already has current-state indexing, native stop-ship traceability, fail-closed validators, package manifests, and closing rites. The absent parent-review repository is not reconstructed here. |
| B1–B4 instruments | **External instruments, not imported.** Their useful lesson—required anchors and inventories fail closed—already governs native project, package, architecture, release-matrix, and source-boundary tests. The packet's 145-string result remains an inventory floor, never a translation queue. |
| C1–C4 record repairs | **External record work.** Arithmetic errata, packet manifests, and prediction scoring do not become product files. |
| D1 Windows SampleGenerator | **Already satisfied.** The current generator runs deterministically on Windows. |
| D2 Ubuntu SampleGenerator | **Strengthened, measured, repaired, and hosted green.** The prior workflow proved two Windows runs agreed and two Linux runs agreed, but never compared the operating systems. The new recursive comparison found a real `.ocfproj` difference rather than conferring a green claim. A retained Windows/Linux pair proved the JSON newline and ZIP host-metadata causes; the writer now fixes both inputs while preserving established Windows bytes. At `33be421`, the dependent Ubuntu job proved all 40 Windows and Linux files matched byte for byte. |
| D3 version/assembly/tag identity | **Real and unchanged typist hold.** Engine `0.7.0-alpha`, assembly `1.0.0.0`, and the earlier unsigned tag remain deliberately incoherent until the typist performs the coordinated version/tag act. No version or tag changed here. |
| D4 accessibility-directory signpost | **Enacted; count superseded below.** `tests/Accessibility/README.md` points a directory reader to the cross-suite traceability matrix without freezing a case count and states exactly what the current practical-floor suite does not prove. |
| D5 document-locale threading | **Not enacted.** The safe artifact-furniture metadata residue is already present. Resolving the null-document `en` compatibility fallback, real translations, descriptions, fonts, and reading-order effects remains accessibility/multilingual-seat territory. |
| D6 five observed architecture rules | **Not adopted wholesale.** Native reference, BCL-side, and build-property guards already own the useful bounded residue. The one-file egress cap remains rejected; multiple governed outbound seams and the loopback print-view boundary make it the wrong invariant. |
| E1–E5 licences, fonts, seats, council | **Human/protected holds unchanged.** In this audit-D continuation, no printable or symbol-system licence was chosen, no font added, no simulated seat counted, no correspondence sent, and no council session invented. Council-first governs Atlas selection rather than imposing a moratorium on ordinary authorized engineering. |
| F1–F2 missing review inputs | **External and deferred.** No parent review or unofficial Atlas 3.1 is reconstructed. Honest Ink's canonical Atlas remains 2.0 and its needs-first session remains unrun. |
| F3 egress re-derivation | **Already superseded by current native evidence.** Provider egress, policy gating, redirects, print/export sinks, and the governed loopback boundary are covered by the trust model and tests; the packet's one-file proxy remains rejected. |
| G1 locale fallback | **Same protected hold as D5.** Strong evidence is not seat authority. |
| G2 missing packet tool | **External packet defect.** No product change. |
| G3 rapidly stale pins | **Admitted as documentation discipline.** Current-state markers, comparison commits, dates, and re-measurement remain mandatory. |
| G4 generator timing estimate | **No product defect.** A corrected cost estimate is not code. |
| G5 artifact determinism | **Guarded; one defect found, repaired, and proved.** Both operating systems self-compare. The cross-operating-system gate closed an observability gap by first finding the defect recorded below and then proving equality at exact repair commit `33be421`. |
| G6 calibration cross-renderer probe | **Already superseded.** Native rendering tests verify the same calibration source's exact SVG/HTML millimetre geometry, exact PDF point geometry, and preview routes. A sample-file parser would duplicate stronger source-level contracts and invite format brittleness. |

## Additional current-state corrections

The attachment's rights review repeated an old claim that the repository had no third-party dependencies. Current project files and locked manifests disprove that claim. `NOTICE.md` now says dependencies are present, points to the commit-scoped CI inventories and SBOMs, and preserves the requirement that any distribution-required notice be summarized before release. A unit contract first proves package references exist, then refuses a return to the false “None yet” statement or loss of those evidence pointers.

The root README's test-evidence pointer now follows the current-state handover rather than the historical forge closeout. This changes no test result; it prevents a current-tree claim from pointing at a superseded count.

The typist's 30 August note that OpenSymbols may be used for AAC features is now recorded as a candidate in the [trademark and ecosystem screen](../trademark-screening.md#opensymbols-candidate-note--30-august-2026). The catalog is an index of sources with distinct licences, not a blanket licence; per-result rights and attribution metadata must flow into the existing ledger. No asset, API key, account, network path, licence disposition, or AAC meaning was admitted here. Curated local assets and identifier interoperability remain possible only after the protected AAC/SLP and rights seats act.

## Mulberry + OpenMoji candidate continuation — 30 August 2026

The product owner proposed a complementary source strategy: Mulberry for action, routine, communication, and sequencing candidates; OpenMoji for object, curriculum, activity, technology, place, and general-interface candidates; and Honest Ink originals for unmet agency or safety-critical concepts. The accurate proposed claim is “symbol-supported, AAC-aware directions,” never automatic AAC conversion. The exact authoring invariants and review fields now live in the [proposed/held source packet](../council/symbol-source-candidate-packet.md).

That direction did not waive the seats. The packet calls every role a hypothesis, does not call the current 13 originals council-approved, and admits no artwork, mapping, family default, fallback, schema change, production descriptor, API, credential, or runtime network path. Exact release reconnaissance found Mulberry `v3.6.1` at 3,436 English SVGs and OpenMoji `17.0.0` at 4,495 colour plus 4,495 black SVGs. Both complete collections exceed the 512-record loose-manifest boundary, and representative files from each fail the current narrow SVG policy. Their exact pins and hashes are review inputs, not an allowlist.

Three source-neutral defects were closed without activating either candidate. `CompositeAssetCatalog` now snapshots each explicitly supplied inventory once and refuses exact or portable case-confusable `AssetId` collisions instead of resolving the first source silently. SequenceSlate (stable legacy module ID `all-aboard`) now binds the displayed symbol choice and the authored document to one typed inventory snapshot, preventing an unstable catalog order from changing the selected asset. The shipped-symbol orphan guard now scans the same recursive tree the application project copies, so nested unprovenanced bytes cannot hide below the top-level manifest.

Focused evidence passed **25/25** selected integration cases across `LocalSymbolStoreTests` and `RightsMetadataTests`, including three new composition guards, and **17/17** cases in the legacy-named `AllAboardContractTests` class, including the new changing-order selection regression. These machine guards prepare safe composition only. AAC/SLP mappings and fallback rules, rights-ledger topology and exact attribution, source curation, new interaction patterns, recognizability, AT evidence, and any schema/version route remain held by their named owners.

## Public module naming decision — 30–31 August 2026

The product owner directed a researched decision and enactment for the public
module names that had developed direct or unacceptably close uses, especially
Lesson Loom. [ADR-008](../adr/ADR-008-public-module-display-names.md) records the
30 August selection, which initially used GridLesson. A fresh 31 August screen
found [LessonGrid](https://lessongrid.app/), an active visual curriculum and
lesson-planning product for United States teachers. Reversing the same two words
does not create adequate distance in the same software category.

[ADR-009](../adr/ADR-009-strandplan-display-name.md) therefore supersedes only
that display row. The current provisional displays are **SequenceSlate — Visual
Support Studio**, **StrandPlan — Lesson Design Studio**, **Forumwright —
Discussion Design**, **ReteachSignal — Formative Evidence**, **Inquirywright —
Source & Inquiry**, and **KinDispatch — Bilingual & Family Press**. Both Lesson
Loom and GridLesson are retired from current UI chrome.

This is a display-name change, not a data migration. The stable legacy IDs
`all-aboard`, `lesson-loom`, `talk-moves`/`talk-moves-studio`, `exit-lens`,
`source-lens`, and `family-bridge`, together with their recipe, schema,
localization, diagnostic, and project bindings, remain exact compatibility
contracts. Current product prose calls the ADR-005 feature the Scaffold Smith
task-entry scaffold; TaskDock remains only where a historical Atlas entry,
ratified ADR, old evidence, or stable record needs its then-current title.

The screen is not trademark clearance and makes no domain or handle claim.
Counsel review remains a pre-release checkpoint. The AAC/SLP, accessibility,
multilingual, rights, district, and council-first holds are unchanged; names do
not transfer seat authority or select an Atlas priority. Exact local naming
verification is recorded below. Commit, push, and hosted conclusions remain to
be read rather than predicted.

## Pseudo-locale glyph clarification — 30 August 2026

The repeated accented `ẋ` characters occasionally visible in a window title
during the local close are expected pseudo-locale padding, not corrupt text.
`UiStrings.Pseudoize` appends U+1E8B until each chrome string expands by at least
40 percent, then the harness mirrors the window so truncation and direction
defects are visible. The screenshot timestamp coincided with the headed UI test
run. Activation requires exact `--pseudo-locale`, inherited
`OCF_PSEUDO_LOCALE=1`, or friend-assembly test setup; OS language and culture
cannot activate it. Process, user, and machine values for that environment
variable were all unset after the run, and no repository launcher or workflow
supplies it outside tests. No current-state leak was found. A later containment
audit did find that the startup configurator and explicitly test-only setter
were public, so outside code could force process-global pseudo mode without a
real selector. Both mutators are now assembly-only and a reflection contract
guards that boundary. That repair is not a diagnosis of the screenshot, whose
timing remains consistent with headed tests. Appearance in an ordinary non-test
launch would instead be a configuration defect requiring command-line and
inherited-environment inspection.

## Board-to-Brief source intake — 30 August 2026

The measured pre-change Board-to-Brief door began at a table labeled for already verified lines and roles. `TranscriptSession` and the capture/OCR seams existed, but production navigation did not connect a captured board, source comparison, uncertainty review, reading-order editing, or semantic-role assignment to that table; the only complete capture-to-transcript exercise used a fake OCR service in an integration test. That was a direct gap against implementation plan §10.2, not merely a request for more fixtures.

The Board-to-Brief door now exposes **Import and verify board**. One owner-retained session reuses the standard capture, normalization, and lane-confirmation surface, refuses anything other than teacher-confirmed Green, and displays the exact normalized image beside candidate and teacher-verified text. Production reads the installed district policy once per process through `JsonDistrictPolicyProvider`; a missing or corrupt file retains that provider's fail-closed Offline fallback, while tests alone inject a policy. The recognizer reads the opaque session reference only, accepts one metadata-stripped normalized PNG, and calls the local Windows `Windows.Media.Ocr` capability through the current user's installed recognition languages. The teacher-chosen import path dies at the existing capture boundary and does not cross the OCR seam. The intake adds no cloud endpoint, HTTP transport, credential, autosave, artifact, approval, print, or export path.

The platform boundary is deliberately less confident than a simulated OCR score. Microsoft's [`OcrEngine`](https://learn.microsoft.com/en-us/uwp/api/windows.media.ocr.ocrengine?view=winrt-26100) returns lines and words, while [`OcrWord`](https://learn.microsoft.com/en-us/uwp/api/windows.media.ocr.ocrword?view=winrt-28000) exposes text and a bounding rectangle but no word-confidence property. Honest Ink therefore marks every Windows OCR word `ConfidenceAvailable = false` and uncertain, rather than translating a numeric placeholder into certainty. The installed-language set and profile selection remain Windows-owned through [`AvailableRecognizerLanguages`](https://learn.microsoft.com/en-us/uwp/api/windows.media.ocr.ocrengine.availablerecognizerlanguages?view=winrt-28000) and `TryCreateFromUserProfileLanguages`. If Windows OCR or a matching language is unavailable, the surface offers an honestly labeled manual literal-transcript fallback; each manually entered source line is also uncertain until the teacher acts.

Literal does not mean space-joining a platform word list. The Windows adapter now aligns each word ordinally and forward-only against the platform's exact line text, stores the exact leading/inter-word/trailing separators, and uses one projection for candidate, partial-verification, and completed views. Focused cases preserve `x=2`, `25 mL`, terminal punctuation, and arrows while allowing only the verified word itself to change. If the platform's line text and words do not align exactly, OCR is refused and the existing manual path is offered; no guessed separator reaches the teacher as a faithful transcript.

The standard-control grammar is explicit: next uncertain, accept the visible candidate, retype exactly, or mark illegible. Only after every uncertainty is resolved does the role table become active; it lets the teacher edit verified line text, requires one teacher-selected role for each line and exactly one Title, and provides keyboard-operable up/down reading-order changes. Successful handoff is deliberately weaker than approval: verified `BriefLine` values populate the existing module table, then Green confirmation and any prior Gate B approval are cleared. Existing schema validation, Gate B review, and approved-only sinks remain the only route to HTML, print, save, or export.

Byte ownership also remains explicit. Normalization releases the superseded capture reference; the preview and OCR services each zero their owned PNG copy after use; closing the preview disposes its decoded image. Success, cancel, refusal, and the always-visible Safety Pause all terminate through the owning `CaptureSession`. Forced `Dispose()` likewise cancels an active OCR operation, waits asynchronously for that borrower to settle, and only then cancels/purges the capture session; a focused regression observed one held reference at OCR settlement, then zero after cleanup, with the retained buffer zeroed and no result. An unexpected non-cancellation OCR fault during terminal cancel cannot suppress purge, and a capture-surface exception cancels/purges before OCR is called. No verified lines are exposed until the normalized session bytes reach `TransientSourcesPurged`; if purge fails, the working surface is disabled, `ResultLines` remains null, and **Retry secure purge** is the only enabled action. Managed OCR/transcript strings are ordinary process memory and are not claimed byte-zeroed; pagefile and crash-dump residue remain outside the application's purge claim.

A new 36-case, wholly synthetic Board-to-Brief corpus covers more than 20 subject labels, exact formulas/units/numbering/arrows/citations/rights strings, teacher-only notes, four language/reading-direction inputs, deterministic rendering, and four fail-closed title/locked-field boundaries. It is deterministic structural evidence only. It is not a field-photo corpus and confers no multilingual, accessibility, AAC/SLP, curriculum, rights, or usability-seat approval.

The exact minimum-floor inventory now includes the intake and exercises its unresolved review, completed transcript and role table, Move down/Move up reading-order actions, `PurgeIncomplete` state, and Retry secure purge at neutral 100 percent and pseudo 125 percent on the hermetic 1366×728 working area within the 1366×768 floor. Its first focused run found two genuine clipping defects in the lower-right work area. A scroll-owned virtual layout repaired both; the fresh focused floor batch then passed **8/8**. That is machine geometry and reachable-control evidence, not a physical-device, keyboard, or screen-reader observation.

Bounded focused verification on the combined working tree passed **23/23** `TranscriptSessionTests`, **11/11** `WindowsOcrServiceTests`, **9/9** `BoardToBriefIntakeContractTests`, the floor's **8/8**, and the synthetic Board corpus's **37/37** tests (one corpus-inventory fact plus the 36 fixture cases). A Release build of the WinForms application project completed at **0 warnings, 0 errors**. These are focused working-tree receipts only: no full-solution test, stability rerun, implementation commit, push, CI, or CodeQL conclusion is inferred from them.

The Board intake added 52 reviewed-chrome candidates. The current neutral catalog therefore measures **1,048 ids**. Two fresh exports were byte-identical at **158,789 bytes** and SHA-256 `1AC439213B5E22A985754F0C7774EBBDFB06C250EACB0362A80FBAE85AC1E094`, bound to source digest `cdaabf3e91e9f81ed85056a085850e534b12d9c6807ba6f92bd5ae5eb91343a6`; `ReviewedUiCatalogTests` passed **26/26**. This is a neutral mechanical packet, not a second language, seat review, or activation pin. The production allowlist remains empty.

The slice does not complete Board to Brief or the release. One normalized image at a time is the current format boundary; low-light, fluorescent glare, marker color, real end-of-day photography, perspective/glare correction, multi-page/PDF/slide input, and complex layout remain open. Human keyboard plus NVDA/Narrator, 200 percent zoom, grayscale/high contrast, physical minimum-floor observation, the three-minute/80-percent teacher measure, qualified language review, source-rights review, accessible DOCX/ODT, and native print-PDF evidence also remain open. The existing module exports are accessible HTML and print HTML. The ordered local closing receipt and exact hosted conclusions are not claimed in this working-tree entry; they must be appended only after they are measured on the final combined tree.

## Cross-platform project-package defect: measurement before repair

The first strengthened run, [CI 33339699165](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33339699165) at `fc160f9`, concluded `failure`. Its Windows build, full test pass, sample self-comparison, retained baseline, and secret scan succeeded. Ubuntu then compared all **40** files within Linux, but the fail-fast cross-platform gate reported `task-strip-bilingual.ocfproj` as its first mismatch. [CodeQL 33339699185](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33339699185) concluded `success`.

Commit `fe142f6` retained the Linux candidate without weakening that gate. [CI 33340135629](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33340135629) again concluded `failure`; `build-and-test` and `secret-scan` succeeded, `portable-samples` failed, and the following always-run upload retained artifact `linux-sample-candidate` (artifact ID `9740350192`). [CodeQL 33340135625](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33340135625) concluded `success`.

The retained pair made the diagnosis exhaustive for that sample set: inventories agreed; **39/40** top-level files were byte-identical; the `.ocfproj` was Windows **3,338 bytes / SHA-256 `9014BFFB477FC9F470E3901393AF1B6D34ACF697B5E0CD174A12BF8D76A74C82`** and Linux **3,326 bytes / `F83B1881852F4B30868F8C1B9B62B0B82F48BEAEBB9641A19876F84E5ED28FF1`**. All seven entry names, order, timestamps, and three non-JSON payloads agreed. Four JSON payloads differed only by **107** platform newline sequences: Windows CRLF versus Linux LF. Every central-directory record also carried .NET's host defaults: Windows version-made-by `0x0014` and attributes `0`; Linux `0x0314` and `0x81A40000`. Rebuilding with canonical newlines and changing those **21** differing metadata bytes produced an exact match; compression bytes were not an additional cause.

The repair makes `StorageJson` use explicit CRLF, preserving current Windows and frozen-fixture conventions while removing the same latent dependency from other storage JSON. Every produced project entry explicitly carries zero external attributes. A bounded, fail-closed, non-ZIP64 central-directory pass runs only on unpublished new or transformed staging files after `ZipArchive` closes, validates the complete layout, and changes only .NET's Unix host byte `3` to canonical `0`. The already-compatible upgrade path remains untouched and byte-preserving. New integration tests cover both production paths, raw central metadata, canonical storage newlines, the Unix-to-canonical byte change, idempotence, and refusal-before-mutation. No schema, engine version, fixture, tag, or release act changed.

The exact repair commit is `33be4214361e97c5ce410f52659d861d2a4c9c0b`. [CI 33341621839](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33341621839) concluded `success`: `build-and-test`, `secret-scan`, and `portable-samples` all succeeded. Ubuntu reported **40 files compared byte-for-byte on Linux** and **40 Windows and Linux sample files matched byte-for-byte**; the retained Linux artifact is `9740786630`. [CodeQL 33341621871](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33341621871) also concluded `success`, reported a clean SARIF across one file, and retained artifact `9740804405`.

## Verification and unchanged boundaries

Focused repair evidence passed: all **230** integration tests, including four new package-canonicalization tests, passed. The repaired Release SampleGenerator produced two fresh 40-file inventories with zero mismatches, and both were also byte-identical to the retained pre-repair Windows baseline; the `.ocfproj` retained SHA-256 `9014BFFB…A74C82`. Full local closing, the implementation commit, push, and its exact hosted conclusions are recorded below.

Protected and typist work remains unchanged: a real second chrome language; complete artifact-furniture translation and font evidence; AAC/SLP, accessibility, multilingual, safeguarding, rights, district, and council judgments; coherent version/tag/sign/package/install/distribution/publication; pilot evidence; and a second maintainer. The three historical under-load failures remain sightings, not diagnoses.

## Closing evidence

The initial enactment's ordered local closing sequence resolved the exact `global.json` SDK **10.0.302**. The Release solution built at **0 warnings, 0 errors**. `dotnet format` fix completed and `--verify-no-changes` exited 0. Two complete independently recorded stability runs each passed **1,239/1,239**, with zero failed or skipped: Unit 536, Integration 226, UI automation 213, Contract 100, Rendering 81, Instructional Evals 75, and Accessibility 8. All headed UIA cases executed; none of the three retained historical under-load sightings reproduced. Each run's seven TRX files were read directly; there were no non-passing result records or failure messages to suppress.

The repair then repeated the complete closing sequence on SDK **10.0.302**. Release build again completed at **0 warnings, 0 errors**; format fix and verification both exited 0. Two fresh full runs each passed **1,243/1,243**, zero failed and zero skipped: Unit 536, Integration 230, UI automation 213, Contract 100, Rendering 81, Instructional Evals 75, and Accessibility 8. Their fourteen TRX files contained no result other than `Passed`. Exact CI-style coverage collection and filtering reported **92.7%** line coverage for the core and nine module assemblies, above the required 80% floor.

The original Release SampleGenerator receipt ran twice into fresh directories. Both recursive inventories contained **40 files**, and every corresponding SHA-256 matched; there were zero byte mismatches. After the measured repair, two more fresh 40-file runs again matched each other and the retained pre-repair Windows baseline byte for byte, including `.ocfproj` SHA-256 `9014BFFB477FC9F470E3901393AF1B6D34ACF697B5E0CD174A12BF8D76A74C82`. The Windows host could not prove cross-operating-system equality; the dependent Ubuntu job did so for exact commit `33be421` by matching all 40 Windows and Linux files byte for byte.

The CI YAML parsed successfully, focused supply-chain contracts passed 6/6, and `git diff --check` found no whitespace errors. The repository hook was installed from the tracked source; the repair's real staged hook exited 0 after gitleaks scanned **33.36 KB** and found no leaks. The two initial CodeQL runs succeeded and both strengthened diagnostic CI runs honestly remained red at the portable comparison as recorded above. At `33be421`, hosted Windows tests passed **1,243/1,243**, the coverage gate reported **92.7%**, the same-platform and cross-platform sample gates passed 40/40, secret scan passed, CI concluded `success`, and CodeQL concluded `success` with a clean retained SARIF.

### Mulberry/OpenMoji continuation closing

The candidate-preparation continuation resolved SDK **10.0.302**. Its Release solution build completed at **0 warnings, 0 errors**; `dotnet format` fix and `--verify-no-changes` both exited 0. Two independently recorded full runs each passed **1,247/1,247**, zero failed and zero skipped: Unit 536, Integration 233, UI automation 214, Contract 100, Rendering 81, Instructional Evals 75, and Accessibility 8. All fourteen TRX files were read directly and contained no outcome other than `Passed`; the three historical under-load sightings did not reproduce. Presses and sample generation did not change, so the conditional two-run SampleGenerator hash rite did not apply. The staged repository hook, push, and exact hosted conclusions are recorded immediately below.

Commit `9311e82f78d8fb45f9b6195eb98849ee32ee6681` pushed the candidate-preparation continuation. [CI 33344337697](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33344337697) concluded `success`: `secret-scan`, `build-and-test`, and `portable-samples` all succeeded, including the hosted full suite and Linux/Windows sample equality. [CodeQL 33344337664](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33344337664) concluded `success`; its SARIF gate was clean. Neither run admits a symbol source or changes a protected-seat hold.

### Public-module naming closing

The naming continuation also resolved SDK **10.0.302**. Its final Release build completed at **0 warnings, 0 errors**; `dotnet format` fix and `--verify-no-changes` both exited 0. An initial complete run looked green at the wrapper but direct TRX inspection found **1,249/1,250 passed** and retained the exact failure: `LocalizationRulesTests.The_winforms_app_carries_no_user_facing_string_outside_the_catalog` rejected two suggested-filename interpolations in WinForms. The repair moved those public file-stem rules into `ModulePublicIdentity`; the focused localization and catalog batch then passed **17/17**.

Two fresh, independently recorded complete runs on the naming commit each passed **1,250/1,250**, zero failed and zero skipped: Unit 537, Integration 233, UI automation 216, Contract 100, Rendering 81, Instructional Evals 75, and Accessibility 8. Before push, `origin/main` advanced by three disjoint CI/publish-boundary commits through `52ea37e`; the naming commit rebased without conflict. The complete ordered close was repeated on that combined tree. Its final two runs each passed **1,252/1,252**, zero failed and zero skipped: Unit 539, Integration 233, UI automation 216, Contract 100, Rendering 81, Instructional Evals 75, and Accessibility 8. The final fourteen TRX files contained no outcome other than `Passed`; every headed UIA case executed, and none of the three historical under-load sightings reproduced. Before the full close, the affected focused UI contracts passed **68/68**, the exact 1366×768 floor suite passed **8/8** at neutral 100 percent and pseudo 125 percent, and the two affected real headed UIA walks passed **2/2**.

The final 996-id neutral UI packet exported byte-identically twice at SHA-256 `2C803158A5BDCA3B71EA77E893DCCFF3F8F2D646859C4A3524961FFAE15997DE`, bound to source digest `c9bb2685b57824a3feec14eb79cd51038d4a1f64632e1f9538df9d954e193b0a`. This remains mechanical neutral-packet evidence, not multilingual-seat review or an activation pin; the allowlist is still empty. Press definitions and generation did not change, so the conditional local SampleGenerator rite did not apply.

The real staged hook scanned about **58.44 KB** and found no leaks. Implementation commit `1a5c6f60855962194593fa59b224929b9b221152` pushed to `main`. [CI 33347263918](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33347263918) concluded `success`: all seven hosted suites passed the same **1,252/1,252** cases, core-and-module line coverage measured **92.8%**, build and format gates passed, no known vulnerable direct or transitive NuGet dependency was reported, 40 Windows sample files matched byte for byte, 40 Linux files matched byte for byte, and all 40 Windows/Linux counterparts matched. `secret-scan`, `build-and-test`, and `portable-samples` each concluded `success`. [CodeQL 33347263911](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33347263911) concluded `success`; the required SARIF gate reported clean across one file and retained artifact `9742480141`. These are implementation-commit receipts, not a tag, release, signature, package, installation, distribution, publication, trademark clearance, or protected-seat approval.

### Board-to-Brief source-intake closing — 31 August 2026

The Board source-intake continuation resolved SDK **10.0.302**. Its Release solution build completed at **0 warnings, 0 errors**; `dotnet format` fix completed and `--verify-no-changes` exited 0. Two independently recorded complete runs each passed **1,326/1,326**, with zero failed and zero skipped: Unit 556, Integration 244, UI automation 225, Contract 100, Rendering 81, Instructional Evals 112, and Accessibility 8. All fourteen TRX files were read directly and contained no result other than `Passed`; every headed UIA case executed, and none of the three retained historical under-load sightings reproduced.

Before staging, `origin/main` advanced by two disjoint architecture-map commits through `002a03c`. The continuation rebased without conflict, and the widened portable-engine rule passed **17/17** focused architecture cases against the new Windows adapter and UI path. The complete ordered close was then repeated on that combined tree: Release build **0 warnings, 0 errors**; format fix and verification both exit 0; and two fresh full runs each **1,328/1,328**, zero failed and zero skipped — Unit 558, Integration 244, UI automation 225, Contract 100, Rendering 81, Instructional Evals 112, and Accessibility 8. The final fourteen TRX files again contained only `Passed`, including every headed UIA case, and none of the three historical under-load sightings reproduced.

Before the full close, the final affected batches passed **23/23** transcript-state tests, **15/15** combined Windows-OCR and capture-flow integration tests, **9/9** Board intake UI contracts, **37/37** synthetic Board corpus tests, **8/8** minimum-floor tests, and **26/26** reviewed-catalog contracts. Two independent neutral-catalog exports each contained **1,048 ids** and **158,789 bytes** and were byte-identical at SHA-256 `1AC439213B5E22A985754F0C7774EBBDFB06C250EACB0362A80FBAE85AC1E094`, bound to source digest `cdaabf3e91e9f81ed85056a085850e534b12d9c6807ba6f92bd5ae5eb91343a6`. This is mechanical neutral-packet evidence only; the allowlist remains empty and no real second language or protected-seat review is claimed.

Press definitions and sample generation did not change, so the conditional two-run SampleGenerator hash rite did not apply. The staged hook, implementation commit, push, and exact hosted conclusions are recorded only after they occur; no version, tag, signature, package, installation, distribution, publication, filing, correspondence, or protected-seat act is part of this continuation.

The real staged hook scanned about **162.45 KB** and found no leaks. Implementation commit `0ad2a333a2fd4555d7f0075b9c306045b1849f8f` pushed to `main`. [CI attempt 1](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33378979000/attempts/1) concluded `failure` at **1,327/1,328**, zero skipped. Contract 100/100, Instructional Evals 112/112, Rendering 81/81, Unit 558/558, Accessibility 8/8, and UI automation 225/225 passed; Integration was 243/244. The retained failure was `FlashCapCameraSourceTests.A_shared_lifecycle_lock_can_prevent_confirmed_shutdown_but_capture_still_settles_bounded`: the assertion expected exact `InvalidOperationException`, but its outer five-second bound observed `TimeoutException: The operation has timed out.` Build, format, and secret scan succeeded. The failing test then passed alone, alone with coverage, and inside a fresh local full-coverage **1,328/1,328** run. That is non-reproduction, not diagnosis; the hosted timeout remains a new named sighting. The dependent determinism, coverage-threshold, dependency, vulnerability, SBOM, and portable-sample jobs did not run on attempt 1 and are not inferred.

[CodeQL 33378978773](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33378978773) concluded `success` for that exact commit. It scanned **236/236 C# files**, required a clean SARIF across one file, and retained `codeql-sarif` artifact `9753167150` at SHA-256 `62d0d7e235a45644e8d49c96c721e02203d9fdaf73caefe14136775253a7319f`. [CI attempt 2](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33378979000/attempts/2) later concluded `cancelled` at the exact six-hour job limit. Six completed suites measured **1,103 passed, 0 failed, 0 skipped** — Instructional Evals 112, Rendering 81, Contract 100, Unit 558, Integration 244, and Accessibility 8 — so attempt 1's Integration timeout did not reproduce. UI automation started but emitted no TRX or pass/fail result before cancellation; there is therefore no valid 1,328-test conclusion. Build, format, and secret scan succeeded, while determinism, coverage-threshold, dependency, vulnerability, SBOM, and portable-sample gates were skipped. Artifact `9764992716` contains only the six completed TRX files and twelve corresponding Cobertura files. This six-hour runner timeout is a sighting, not a diagnosis, and supplies no missing downstream evidence.

### Exact teacher-controlled capture crop continuation — 31 August 2026

The capture surface now displays the exact normalized generation the teacher is being asked to classify and pass forward. Standard numeric X, Y, width, and height controls describe a half-open pixel rectangle in post-rotation coordinates; that pixel basis deliberately does not mirror in RTL. Apply and Reset are explicit, an unapplied or invalid proposal blocks rotation and confirmation, applying a crop replaces the authoritative generation and resets the lane to Amber, and Safety Pause remains reachable throughout. Crops must lie wholly inside the post-rotation image: there is no silent clipping, invalid input does not store a derivative or mutate the source, and an asymmetric crop may end exactly at an image edge.

The preview and lifecycle path is generation-bound rather than merely visually adjacent. Publication, rotation, crop, and lane confirmation all compare the exact authoritative byte reference under the capture-session lock; a stale generation fails closed and clears the preview. Preview copies decode off the WinForms message loop and are zeroed after decode. Replaced, rejected, or disposed decoded images are overwritten before disposal. Close, Safety Pause, cancellation-callback faults, late normalizer settlement, and direct disposal without a successful owner handoff all converge on settlement and purge. A real Capture → rotated asymmetric crop → Green confirmation → Board OCR contract proves the exact reference, dimensions, encoded bytes, and known pixels reach OCR and remain retained only until Board's terminal purge.

The final focused evidence passed **44/44** combined session/architecture tests, **33/33** combined capture-surface/Board intake contracts, **23/23** image-normalization tests, and **8/8** minimum-floor tests. The affected stress batch before the final stale-rotation assertion passed **140/140** across three capture-surface runs and two runs each of session and Board intake; the final capture-surface batch then passed **23/23**. Two independent neutral-catalog exports each contained **1,072 ids** and **163,309 bytes** and were byte-identical at SHA-256 `F4FFB140E8DB460C567877AD97733C1EA60EB05A1545F6FC82A1CBDB1F9ED39E`, bound to source digest `5cc0216be58608ecfcb6367524b85615be4ee6b0e0dbe64ea41b1364ea201784`; the focused reviewed-catalog contract passed **26/26**. This is still a neutral mechanical packet, not a real second chrome language, protected-seat review, or activation pin. The production allowlist remains empty.

The full local close resolved SDK **10.0.302**. The Release solution built at **0 warnings, 0 errors**; `dotnet format` fix completed and `--verify-no-changes` exited 0. Two independently recorded complete runs each passed **1,361/1,361**, zero failed and zero skipped: Unit 570, Integration 256, UI automation 234, Contract 100, Rendering 81, Instructional Evals 112, and Accessibility 8. All fourteen TRX files were read directly and contained no result other than `Passed`; every headed UIA case executed. Neither the three historical under-load sightings nor the newer hosted FlashCap timeout reproduced locally.

Press definitions and sample generation did not change, so the conditional two-run SampleGenerator hash rite does not apply. The staged hook, implementation commit and push, and exact hosted conclusions are recorded only after they occur. No version, tag, signature, package, installation, distribution, publication, filing, correspondence, or protected-seat act is part of this continuation.

The real staged hook scanned about **89.29 KB** and found no leaks. Implementation commit `96e37e9854861cc2a6098a9fbf05add708732e03` pushed to `main`. [CI 33390149083](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33390149083) concluded `success`: all seven retained TRX files measured **1,361/1,361 passed**, zero failed or skipped, with the same suite totals as the local close; Release build and format passed; core-and-module line coverage measured **92.8%**; 40 Windows files matched across two runs, 40 Linux files matched across two runs, and all 40 Windows/Linux counterparts matched. The dependency, vulnerability, repository/application inventory, CycloneDX 1.7 SBOM, build-evidence, and full-history secret-scan gates passed; gitleaks scanned 101 commits / about 8.74 MB and found no leaks.

[CodeQL 33390149028](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33390149028) concluded `success` for the same exact commit. It scanned **236/236 C# files** with the `security-extended` suite; direct inspection of the retained SARIF 2.1.0 found one CodeQL run and zero results. Artifact `9757301294` contained 62,441 bytes at SHA-256 `1b21b0c93300324df18218c4bcfa43e0419e9884583e395603341d1b32aefece`. These are source-commit receipts, not a version, tag, release, signature, package, installation, distribution, publication, filing, correspondence, or protected-seat approval.

### Declared bilingual exact-value preservation — 31 August 2026

The next measured module gap was not a translation-quality judgment. Directions Duet and Family Bridge/KinDispatch required each declared locked value to occur somewhere on both sides, but Directions did not require it to remain in the same aligned row. A red focused batch passed **35/39**: the four new cases showed that two locked values could be swapped between Directions rows, moved between Family Bridge paragraphs, or moved during Gate B without any blocker; a whitespace-only lock could also match ordinary sentence spaces. A separate monolingual Family Bridge regression passed **0/1** because that direct builder ignored its declared locks.

The first repair was not accepted as sufficient. Independent review measured three further mechanical defects: raw substring matching accepted `3` inside `13` and `3.5`, and accepted `$4.5` inside `$4.50`; an undeclared source `3` could become target `9` when the lock inventory was silently empty; and applying Directions' row-parity rule to Family Bridge blocked harmless target repetition or rearrangement that implementation-plan §10.10 does not forbid. These were measured implementation findings, not linguistic or specialist judgments.

The corrected contract is deliberately module-specific. The shared ordinal matcher rejects blank declarations and semantic-token continuations across Unicode letters, combining marks, numbers, units, dates, punctuation/whitespace chains, paired accounting brackets, percent/per-mille compatibility forms, leading numeric list markers, and URI continuation punctuation: numeric changes such as `3` → `13`, `3.5`, `3..5`, `3 000`, `3  000`, `( 3 )`, or `3％`, and `$4.5` → `$4.50`, do not satisfy the lock. Separate document nodes keep separate boundaries. CJK counters, covered Bopomofo/Katakana/Han ranges, compatible corner quotation forms, sentence punctuation, and prose en/em dashes retain their valid exact-value boundaries. Directions Duet requires every declaration in the same aligned source and target rows, preserving its one-to-one action alignment. Bilingual Family Bridge/KinDispatch permits reordering and repetition only among message paragraphs. Requested action, deadline when present, and contact are explicit bilingual role pairs, and a declaration in one of those source roles must remain in that role's target. A body fact cannot migrate into contact or action. The teacher-entered title is still source-only: a title-only declaration fails closed in bilingual mode until a semantic bilingual-heading model exists. Monolingual Family Bridge validates only the teacher-authored title, message, action, deadline, and contact; generated card titles and notices cannot satisfy a declaration. The original public builder signatures remain reachable but fail closed because they cannot attest the new source-inventory act. Initial construction and Gate B both reapply the applicable rule; a violation blocks approval and keeps every output sink disabled.

Both modules now require a false-default teacher confirmation that the source content was reviewed and the exact values which must remain unchanged were declared. A reviewed-empty inventory remains valid, but it must be explicit rather than accidental. The confirmation is bound to an ordered, role-aware projection of the source-bearing content, semantic heading levels, root document language, and source locale metadata captured at construction: changing a module input resets it; changing source text, its semantic role/order, heading level, or source language metadata during Gate B makes it stale and requires return and reconfirmation. Layout-only page breaks do not stale it. A target-text edit does not stale it, although every target locale remains pinned by a separate blocker and all other validation still applies. The artifact carries a teacher-only fact-lock summary with a declaration count, indexed entries, UTF-16 lengths, and escaped quoted values so delimiters, line breaks, BMP format controls, or supplementary-plane invisible format scalars cannot fabricate or visually hide entries; neither that summary nor a durable file field claims that the checkbox act occurred. This is the bounded source-inventory act already assigned to the teacher by implementation-plan §§10.5 and 10.10. It is not language or specialist review, does not authenticate the truth or completeness of the declaration, and grants no multilingual, AAC/SLP, accessibility, safeguarding, curriculum, or rights-seat authority.

Directions Duet also has **36 wholly synthetic cases** across all ten `LockedFieldKind` values, valid and refusal strata, glossary conflicts, long content, Arabic/Hebrew/Japanese/mixed-script structure, an explicitly reviewed-empty case, an unreviewed-inventory refusal, and byte-deterministic rendering. The target strings are explicitly test tokens, not translations or reviewer evidence. The corpus measured **37/37** including its inventory assertion; the focused matcher/Family/Catalog batch measured **185/185**. An independent rebuilt-DLL audit matched 57/57 adversarial boundaries plus 7/7 extended CJK-range probes and found no material remainder.

The first complete close usefully stopped at **1,562/1,563** because the stricter spaced-operator guard rejected the already self-delimited synthetic Board coordinate `(−3, 4)` after `A = `. The repair recognizes a declaration's own opening/closing punctuation without weakening the accounting, numeric-range, list-marker, or shorter-number refusals; the full Instructional Evals then measured **149/149** and independent probes remained clean. This red measurement is retained rather than overwritten by the green reruns.

The final Release solution build completed with **0 warnings and 0 errors**. `dotnet format` fix completed and `--verify-no-changes` exited 0. After the whitespace-only target-locale visibility repair, two new complete solution runs produced fourteen directly read TRX files; each run measured **1,566/1,566 passed**, zero failed and zero skipped: Unit 729, Integration 256, UI automation 243, Contract 100, Rendering 81, Instructional Evals 149, and Accessibility 8. The focused Module Studio plus pseudo-locale UI batch measured 39/39, and the 1366×768/accessibility floor measured 8/8. The then-current 1,078-entry neutral UI catalog exported byte-identically twice at 164,925 bytes, packet SHA-256 `0402F82D6BC42F892218F130F96FA1B2AAA6CF1A9B0F759223B577947C6A96BA`, and source digest `9a8ed1cd9dc7a534bd6104d106f50f6308392058746d55283fcee7fd6f9b3929`; its contract measured 26/26. That packet is localization mechanics, not a second-language claim. Press definitions did not change in that continuation, so SampleGenerator's conditional two-run hash rite did not apply.

Staging is deliberately held. ADR-001 requires versioned recipes and side-by-side retention for pinned projects, but this tree changes Directions and Family behavior/output while their recipe versions remain `0.1.0`, output schemas remain `schema.directions-duet.v1` and `schema.family-bridge.v1`, and the Directions evaluation identity remains `0.1`; Board also inherits the stricter shared matcher under its existing recipe identity. The unratified managed-upgrade and deployment boundary makes this more than a mechanical string bump: deterministic schema-1 side-by-side preparation exists and passed its synthetic tests, but ADR-007 remains proposed and no authorized managed-device version-bump/rollback proof exists. The typist must decide either that these identifiers are pre-admission identities being frozen for the first time, or authorize new recipe/schema/evaluation identities and their side-by-side treatment. No commit, push, version, tag, signature, package, installation, distribution, publication, filing, correspondence, or protected-seat act is claimed while that decision remains open.

### StrandPlan evidence and council-provenance correction — 31 August 2026

The public-name re-screen found that LessonGrid now occupies visual curriculum and lesson planning for United States teachers. [ADR-009](../adr/ADR-009-strandplan-display-name.md) retires the reversed GridLesson composite and uses **StrandPlan — Lesson Design Studio** provisionally. The stable `lesson-loom` recipe, schema, localization, source, diagnostic, and saved-project identities remain unchanged. This is an informal product correction, not legal clearance, domain availability, filing, registration, publication, or release.

StrandPlan now has 36 wholly synthetic structural fixtures across ten subject labels, Arabic/Hebrew/Japanese/Traditional-Chinese layout, 15–240-minute duration edges, declared missing-material/access warnings, multiple contingencies, long and mixed-script rendering, and ten fail-closed timing/evidence/structure cases. The corpus measured **37/37** including its breadth assertion; full Instructional Evals measured **186/186**. Focused current-tree evidence also measured **46/46** across catalog/traceability/hygiene unit tests, **59/59** across reviewed-catalog and Module Studio UI contracts, and **41/41** across asset, rights, and teacher-shelf integration tests.

The Atlas audit found that two explicitly fictional rehearsal records later mislabeled product-owner adoption as educator-council ratification. Their headings and dispositions now say exactly what occurred; active open-hand ambiguity metadata and its test commentary likewise say “product-owner-adopted rehearsal,” not council or AAC/SLP approval. No implemented behavior or protected-seat hold changed. Because the active manifest wording changed, the SampleGenerator was run twice: each run produced the same **40 files** and **773,759 bytes**, with every counterpart byte-identical.

The display correction changed the neutral catalog bytes without changing its inventory. Two fresh exports each contained **1,078 ids** and **164,925 bytes**, byte-identical at SHA-256 `ADC01D0323AD6BBF766B1CC305A409165AD3D82118FF642A9B6576FE8CFF20C1`, bound to source digest `13cab42111782b29bc1d048b75d66a45a5a195210fe284072d3323b2d63d0e3e`. This remains mechanical neutral-packet evidence only; the production allowlist is empty and no second language or protected-seat review is claimed.

The exact combined tree then completed the ordered local close again. Release build measured **0 warnings and 0 errors**; `dotnet format` fix and `--verify-no-changes` both exited 0. Two independently recorded complete runs produced fourteen directly read TRX files; each measured **1,603/1,603 passed**, zero failed and zero other outcomes: Unit 729, Integration 256, UI automation 243, Contract 100, Rendering 81, Instructional Evals 186, and Accessibility 8. The unchanged ADR-001 typist decision still precedes staging; no commit, push, version, tag, signature, package, installation, distribution, publication, filing, correspondence, or protected-seat act is claimed.

### Forumwright corpus and rehearsal-provenance continuation — 31 August 2026

Forumwright (stable `talk-moves-studio` recipe identity) now has a 36-case wholly synthetic structural corpus. Ten cases vary subject labels; four add non-English script and document-language tags; the remaining valid strata vary one-to-five question maps, three-to-eight participation modes, absent/empty/populated optional frames, long content, literal markup-like tokens, and mixed script. Ten refusal cases cover an empty question set, each blank question-map field, too few participation modes, and each of the five required facilitation-move families. Every valid case asserts the always-last wait/pass option, teacher-only purposes/evidence/moves/equity reflection, learner-visible questions/modes/optional frames, exact language metadata, approval, and byte-identical repeat rendering. The focused corpus measured **37/37** and the full Instructional Evals suite measured **223/223**.

That evidence does not invent the two unimplemented Forumwright acceptance proofs: sensitive-topic fixtures and available-time fit remain open. It is not curriculum, language, accessibility, AAC/SLP, sensitive-topic, or educator-usability review.

A second provenance sweep corrected active comments and records that still called fictional rehearsal notes council findings or binding council authority. The underlying lane, egress, symbol-privacy, adjacency, Gate C, and multilingual-preparation constraints remain intact; their source is now described as product-owner-enacted engineering, a product-owner proposal, or rehearsal-derived cross-checking as applicable. No real council, privacy, safeguarding, multilingual, AAC/SLP, accessibility, district, or rights approval is claimed.

The exact combined tree completed the ordered local close. The Release solution build measured **0 warnings and 0 errors**; `dotnet format` fix and `--verify-no-changes` both exited 0. Two independent complete runs produced fourteen TRX files read by literal path; each measured **1,640/1,640 passed**, zero failed and zero other outcomes: Unit 729, Integration 256, UI automation 243, Contract 100, Rendering 81, Instructional Evals 223, and Accessibility 8. Press definitions and sample inputs did not change, so the conditional SampleGenerator rite did not apply. The unresolved ADR-001 identity decision still precedes staging, commit, and push.

### Scaffold Smith packet corpus and Inquirywright boundary audit — 31 August 2026

The catalog audit separated Scaffold Smith's two buildable modes instead of treating the 40-case ADR-005 task-entry corpus as evidence for both. The earlier packet recipe now has its own 36-case wholly synthetic structural corpus: ten subject-label cases, four non-English script/document-language cases, support-count and optional hint/vocabulary/frame breadth, long and markup-like mixed-script rendering, and ten target/criterion/rationale/ladder refusals. Valid cases preserve exact target, criteria, supports, hints, vocabulary, and optional frame; keep evidence, barrier, preserved demand, fade criterion, and removal plan teacher-only; and render byte-identically on repeat. The focused corpus measured **37/37** and the full Instructional Evals suite measured **260/260**.

This is not answer-leak detection, target-fidelity judgment, proof that a rationale is sound, proof that a fade criterion is observable, curriculum review, accessibility evidence, or teacher-usability evidence. The planned source-versus-scaffold comparison and checkpoint-card outputs also remain absent from the packet builder.

The next-mode audit found two Inquirywright gaps that a future corpus must not bless. Exact `unknown` rights currently emits `lens.rights-unknown`, but that warning is not a rights-specific save/export refusal. Transcript review is a caller-supplied Boolean, prechecked in the synthetic Module Studio form and not bound to an exact excerpt hash or durable verification receipt; editing the excerpt clears ordinary Green confirmation but does not reset that checkbox. Post-review required-fact checking is substring-based rather than role-bound, and the planned sensitivity/trauma, omissions, authoritative-context, answerability, and rights-enforcement proofs remain absent. Test names and source commentary now describe only the blocking issue or warning actually measured. Repairing those behaviors changes the SourceLens contract and therefore remains behind the same version/side-by-side identity decision rather than being smuggled in as evidence work.

The exact combined tree then completed the ordered local close. The Release solution build measured **0 warnings and 0 errors**; `dotnet format` fix and `--verify-no-changes` both exited 0. Two independent complete runs produced fourteen TRX files read by literal path; each measured **1,677/1,677 passed**, zero failed and zero other outcomes: Unit 729, Integration 256, UI automation 243, Contract 100, Rendering 81, Instructional Evals 260, and Accessibility 8. Press definitions and sample inputs did not change, so the conditional SampleGenerator rite did not apply. The unresolved ADR-001 identity decision still precedes staging, commit, and push.

### Inquirywright structural corpus — 31 August 2026

Inquirywright (stable `source-lens` recipe identity) now has a 36-case wholly synthetic structural corpus. Ten cases vary source form; four add non-English script/document-language tags, for five language tags including English. Other valid strata cover omitted optional and explicit-unknown metadata, minimum and many prompt groups, one/eight/twenty observation rows, citation trimming, and long, markup-like, and mixed-script rendering. Ten cases block blank required metadata, an unasserted transcript-review Boolean, a blank excerpt, missing sourcing or corroboration, or a blank prompt. Of the 36 cases, 25 are renderable, ten are blocking, and one observes exact `unknown` rights without exercising approval or an output sink. The corpus repeats and byte-compares learner, teacher, and print rendering for every renderable case.

The first focused attempt stopped during compilation, before test execution, on nullable test assertion `CS8604`; the assertion was made type-explicit rather than suppressing the warning. On the corrected current tree, the focused SourceLens unit batch measured **6/6**, the focused corpus measured **37/37**, and full Instructional Evals measured **297/297**, with zero failed or skipped. The implemented copy no longer says an unknown-rights warning mechanically blocks redistribution, no longer calls a caller Boolean human verification, and labels the content as a teacher-provided excerpt rather than claiming external verbatim fidelity.

The renderable cases prove only the current source-card, citation, prompt-group, observation/inference, initial-builder audience-filtering, language/direction, approval, and deterministic-rendering contracts. The unknown-rights case observes only `lens.rights-unknown`; it is not evidence of a product rights hold, rights clearance, or save/export enforcement. Transcript review remains a caller-supplied Boolean rather than an excerpt-bound durable receipt, and post-review required-fact matching remains substring-based rather than role-bound. The corpus does not prove external quotation fidelity, authoritative context, answerability, omission handling, sensitivity/trauma safeguards, disciplinary or curriculum quality, qualified language review, accessibility review, educator usability, or protected-seat approval. KinDispatch is now the sole buildable Module Studio mode without a 30–50-case corpus; Access Remix, ReteachSignal, and Rubric Relay remain held rather than corpus-authorized.

The exact combined tree then completed the ordered local close. The Release solution build measured **0 warnings and 0 errors**; `dotnet format` fix and `--verify-no-changes` both exited 0. Two independent complete runs produced fourteen TRX files read by literal path; each measured **1,714/1,714 passed**, zero failed and zero other outcomes: Unit 729, Integration 256, UI automation 243, Contract 100, Rendering 81, Instructional Evals 297, and Accessibility 8. Neither historical headed UIA timeout nor the retained image-test sighting reproduced. Press definitions and sample inputs did not change, so the conditional SampleGenerator rite did not apply. The unresolved ADR-001 identity decision still precedes staging, commit, and push.

### KinDispatch structural corpus — 31 August 2026

KinDispatch (stable `family-bridge` recipe identity) now has a 36-case wholly synthetic structural corpus. Nine source-valid cases include an explicitly reviewed-empty fact inventory and cover every `LockedFieldKind`; nine bilingual drafts use conspicuous target test tokens across `es`, `ar`, `he`, `ja`, and `zh-Hant`, including permitted message-fact reordering/repetition and one narrow multi-ask warning; eighteen cases refuse missing inventory review, empty body, blank action/contact, source-body readability over the current limit, target content without a locale, missing target roles, an orphan target deadline, a missing working-glossary token, absent/generated-only declarations, role migration, a source-only bilingual title lock, numeric substrings, and a blank declaration. Blocking cases stop before rendering. Every nonblocking case repeats and byte-compares learner HTML, teacher HTML, and print HTML; the audience checks keep all fact-lock, delivery, and translation-status notices out of learner/default-print output.

The nine bilingual cases are deliberately named **renderable unreviewed drafts**. The current direct builder gives eight no issue and one only the nonblocking `bridge.actions` warning; `ApprovalGate` therefore approves and renders all nine. The corpus calls that generic structural approval exactly what it is, preserves the teacher-only “NOT yet language-reviewed” status, and does not recast it as language approval. Their target strings are test tokens, not translations. The focused corpus measured **37/37**, the related Family Bridge and Module Studio unit batch measured **63/63**, and full Instructional Evals measured **334/334**, all with zero failed or skipped.

This corpus proves literal fixture-token preservation, current role-aware lock behavior, semantic node shape, source/target language attributes, audience separation, and deterministic bytes only. It cannot prove translation correctness or dialect fit, qualified review, glossary approval, inventory truth/completeness, semantic equivalence, a genuine one-ask policy, a mandatory deadline, plain-language quality beyond the current average-word heuristic, high-stakes or family-deficit detection, accessibility or font/RTL/CJK legibility, interpreter adequacy, family clarity, educator usability, or any protected-seat approval. The title and interface furniture remain source-only; the builder produces accessible and print HTML, not SMS, FAQ, native PDF/DOCX, addressing, delivery, or distribution. With this corpus, all eight currently buildable Built-in Studios modes meet the 30–50-fixture numeric requirement; SequenceSlate is the ninth current authoring-mode corpus but remains a separate top-level surface. The protected Access Remix, ReteachSignal, and Rubric Relay doors remain held and unavailable.

Independent review asked that “semantic node shape” be proven as an exact graph rather than inferred from selected node families. The corpus now compares the complete ordered nonblocking node sequence, including headings, paragraphs or bilingual pairs, cards or bilingual role sections, and all notices. A scoped formatting gate then rejected the helper's interface return type with `CA1859`; using its concrete `List<DocumentNode>` resolved the measured issue without suppression, and the focused corpus returned to **37/37**.

The exact combined tree completed the ordered local close. The Release solution build measured **0 warnings and 0 errors**; `dotnet format` fix and `--verify-no-changes` both exited 0. Two independent complete runs produced fourteen TRX files read by literal path; each measured **1,751/1,751 passed**, zero failed and zero other outcomes: Unit 729, Integration 256, UI automation 243, Contract 100, Rendering 81, Instructional Evals 334, and Accessibility 8. Neither historical headed UIA timeout nor the retained image-test sighting reproduced. Press definitions and sample inputs did not change, so the conditional SampleGenerator rite did not apply. The unresolved ADR-001 identity decision still precedes staging, commit, and push.

### Second chrome language boundary revalidation — 31 August 2026

The next ordered priority remains honestly human-held. The production `UiCatalogDeployment` allowlist is exactly empty, the repository contains no completed catalog, and a narrow filename-only search of the configured Honest Ink Drive folder found no candidate catalog; no protected document content was opened. No translation, review assertion, reviewer identity, provenance record, or activation pin was invented.

The seat-neutral machinery remains current. Two fresh neutral review-packet exports each contained **1,078 ids** and **164,925 bytes**, byte-identical at SHA-256 `ADC01D0323AD6BBF766B1CC305A409165AD3D82118FF642A9B6576FE8CFF20C1`, bound to source digest `13cab42111782b29bc1d048b75d66a45a5a195210fe284072d3323b2d63d0e3e`; the reviewed-catalog contract measured **26/26**. The packet is still `und`/`draft`. These receipts prove deterministic export, strict loading, and fail-closed deployment admission only. A real second chrome language still requires the multilingual seat to supply and review the completed exact bytes, after which a separately authorized source review may pin that exact SHA-256. The hold cannot be cleared from a keyboard.

### Minimum-floor harness correction — 31 August 2026

An independent inventory confirmed that reflection covers all nine shipped
nonabstract WinForms surface types and that every press, studio door/mode,
review tab, and admitted node variant is enumerated. It also found that the
default harness silently stretched every surface to a synthetic full-floor
panel. That made a fixed 260×130 Tile, the loaded-project preflight, and the
normally smaller Node Editor look roomier than their declared windows. The
same audit found that the older closeout had attributed Capture retry/pending
states and Press Room/SequenceSlate busy export states to this floor file even
though it never entered them. Those states have other UI contracts; they are
not recast as floor evidence.

The harness now keeps the Tile, preflight, and every Node Editor variant at
their declared outer sizes while retaining full-floor scenarios for the
maximizable work surfaces. The strengthened first run measured **7/8**: the
pseudo RTL 125 percent vector editor exposed **Apply primitive edit** outside a
non-scrollable action viewport. Giving that production action strip a real
horizontal scroll path repaired the measured reachability defect, and the
focused floor suite then passed **8/8**. The test's own contract now says what
it is: hermetic live-control-tree geometry for one 1366×728 reference working
area. It is not physical `DeviceDpi`, non-client, top-level shown-window, or
hardware proof.

### Atlas council-first mechanical guard — 31 August 2026

The Atlas remains a possibility register, not a keyboard-selected queue. No
real priority session, council recommendation, feasibility record, or
product-owner disposition was supplied, so no next Atlas priority was chosen.
A new repository contract instead protects the authority boundaries already
present in the unrun packet: need capture must precede mapping; mapping must
precede the council record; participant review and record freeze must precede
feasibility; product-owner disposition remains last and cannot waive a seat or
an ADR. It also fails if absence stops meaning “not reviewed,” if the still-
unenacted decision/quorum rule is silently inferred, or if the canonical Atlas
is recast as an engineering queue. The focused contract measured **1/1**. This
is process-regression evidence, not a council meeting or decision.

The exact combined tree then completed the ordered local close. The Release
solution build measured **0 warnings and 0 errors**; full `dotnet format` fix
and `--verify-no-changes` both exited 0. Two independent complete runs each
produced seven directly read TRX files and measured **1,752/1,752 passed**, zero
failed, skipped, or other outcomes: Unit 730, Integration 256, UI automation
243, Contract 100, Rendering 81, Instructional Evals 334, and Accessibility 8.
The retained result roots are
`ocf-full-floor-atlas-run1-62f18a520c8c4d598f6b2c9775c0f42d` and
`ocf-full-floor-atlas-run2-00e190344624457b9dded89fabc3f3e3` under the local
temporary directory. Neither historical headed UIA timeout nor the image-test
sighting reproduced. Press definitions and sample inputs did not change, so
the conditional SampleGenerator rite did not apply. The unresolved ADR-001
identity decision still precedes staging, commit, and push.

### Managed-upgrade executing-build gates — 31 August 2026

The upgrade audit found two safe, source-tree gaps inside the still-proposed
ADR-007 boundary. The operator host accepted an otherwise valid candidate root
whose path did not carry the executing engine's literal version segment, even
though both the ADR and runbook require that address. It now refuses that root
before preparation. The compatibility service also had no executable proof
that a fully valid source package's exact pinned recipe ID and version exist in
the candidate build. The host now derives that inventory mechanically from the
complete deterministic-press, SequenceSlate, Built-in Studios, and portable-
semantic recipe catalogs compiled into the executing tool; the service
validates the whole held source and then refuses an absent exact identity
without substituting or upgrading it. A review caught that deriving only from
the visible Press Room list omitted the separately persisted Big Print recipe;
the authoritative deterministic catalog, a positive Big Print preparation, and
a complete-inventory guard now close that false refusal. The portable-semantic
identity moved to the shared contracts assembly so the application and operator
host consume one definition. The service and its request records are internal,
with the operator host as the sole production friend; a caller cannot invent an
inventory or skip the host's version-address boundary. Review also prints a
deterministic SHA-256 of the sorted executing inventory, and prepare requires
that exact digest back alongside the plan digest so two source-tree invocations
cannot silently cross different sorted recipe ID/version inventories under one
engine identity. Schema, validator, renderer, warning, and evaluation changes
under unchanged IDs and versions remain outside that digest and behind the
ADR-001 identity decision.

The focused operator/service/schema boundary measured **47/47**. It includes
content-free refusals for an unversioned candidate root and for a synthetically
valid package whose pinned recipe is absent from the executing inventory,
case/version exactness and inventory-digest checks, and positive Big Print
preparation. A real NTFS hard-link regression addresses the same source bytes
twice under different plan paths, observes the stable alias refusal, and proves
the earlier prepared output and created directories are removed. Refused
sources remain unchanged and the candidate root empty. The portable JSON schema
now shares the host's engine/schema token shapes, accepts mixed-case package
suffixes, and rejects expressible absolute, traversal, backslash, glob, invalid,
duplicate-separator, and trailing-dot/space item paths; platform filesystem,
normalization, existence, identity, containment, and reparse checks remain
explicitly host-authoritative. This closes neither the
upgrade debt nor ADR-007: there is still no ratification, schema-2 route,
authorized operator package, real closed-library plan, external exclusive-write
proof, signed candidate, managed-device version-bump/rollback drill, or District
IT acceptance. No version, tag, signature, package, installation, distribution,
publication, live-library preparation, or protected-seat act occurred.

### Under-load evidence instruments — 31 August 2026

The three historical failures remain sightings, not diagnoses. The missing
instrument was the exact image case: `A_burned_region_destroys_the_pixels_beneath_it`
now has a bounded clean-tree harness that builds the Integration project once
and starts a fresh `dotnet test`/testhost process for that single assertion on
every repetition. It retains the exact commit and SDK, harness and test-assembly
SHA-256 values, per-run TRX/stdout/stderr, full assertion message, process exit,
timeout status, and elapsed time. Its summary says explicitly that a passing
batch is non-reproduction only and cannot recover the original message.

Both load instruments now retain the same unrestarted worker-job states before
and after each repetition. A missing or non-`Running` worker invalidates the
repetition and the batch; the state receipt deliberately does not claim measured
CPU utilization. The focused source-contract batch measured **12/12**, and both
PowerShell files parsed without errors. The exact burned-region test separately
passed **1/1** as an ordinary focused execution; that is not a source-bound load
batch. No probe or test timeout was increased.
Because the source tree is deliberately uncommitted pending the ADR-001 identity
decision, the clean-tree guard correctly prevents a source-bound image batch;
no non-reproduction count is invented from this implementation check.

### Upgrade and sighting-instrument closing evidence — 31 August 2026

The exact combined tree completed the ordered local close on SDK **10.0.302**.
The Release solution build measured **0 warnings and 0 errors**; full
`dotnet format` fix and `--verify-no-changes` both exited 0. A precision check
then found that the formatter had touched the upgrade sources after the first
build, so the two otherwise-green runs against those earlier binaries were not
counted as final evidence. The formatted tree was rebuilt at **0 warnings and 0
errors**, and two new independent complete runs each produced seven directly
read TRX files and measured **1,770/1,770 passed**, zero failed, skipped, or
other outcomes: Unit 737, Integration 267, UI automation 243, Contract 100,
Rendering 81, Instructional Evals 334, and Accessibility 8.

The retained final result roots are
`ocf-final-formatted-run1-632e61af55a643fa95af9e60d31282d8` and
`ocf-final-formatted-run2-c5cfd98a7c584105bf66c40022347c4a` under the local
temporary directory. The three historical under-load sightings did not recur
in either ordinary complete run; those are non-reproductions, not load-harness
evidence or diagnoses. The new exact image harness remains deliberately unrun
because its clean-tree precondition is false. Press definitions and sample
inputs did not change, so the conditional two-run SampleGenerator hash rite did
not apply. The unresolved ADR-001 identity decision still precedes staging,
commit, and push. No version, tag, signature, package, installation,
distribution, publication, filing, correspondence, live-library preparation,
or protected-seat act occurred.

### Completion audit: Forumwright mode cardinality — 31 August 2026

A fresh audit of the eight buildable studios found one reachable mechanical
hole in Forumwright. The builder treated a list count as a mode count. Three
case/whitespace variants of `Speak` therefore produced no blocker, and a caller
could submit the builder's reserved automatic wait/pass label as a third mode;
the output then printed that option twice. The production Module Studio line
parser preserved both forms. This contradicted the plan's “three participation
modes plus processing/pass” boundary without requiring any semantic judgment
about which modes are equivalent.

Two new direct-builder assertions first measured **4/6**: both adversarial
cases failed. Forumwright now counts only nonblank, trimmed,
ordinal-case-insensitive distinct teacher modes after separating the exact
reserved option. Fewer than three blocks; submitting the reserved option also
blocks with an instruction to remove it; the draft emits the automatic option
exactly once even on that refused path. The real catalog adapter carries both
regressions. The focused builder/catalog batch then passed **47/47**.

The structural corpus gained duplicate-mode and reserved-option refusal cases:
it now has **38** fixtures, **12** of them blocking cases, and measured
**39/39** including its inventory assertion. This closes neither Forumwright's
sensitive-topic nor available-time acceptance gaps and is not curriculum,
AAC/SLP, accessibility, language, or educator-usability evidence. Because it
changes the existing `talk-moves-studio` behavior under recipe `0.1.0`, schema
`schema.talk-moves.v1`, and evaluation `0.1`, it joins rather than bypasses the
same unresolved ADR-001 identity decision that already holds this tree.

### Completion audit: interactive chrome at the minimum floor — 31 August 2026

The strengthened minimum-floor assertion had measured rectangles and passive
chrome but did not measure fixed Button, CheckBox, or RadioButton captions, nor
the selected and dropdown reading paths for ComboBox items. Synthetic red and
green contracts now pin both boundaries: fixed interactive captions must fit
their visible control, and a compact combo must either display each item or
offer an explicitly wide enough dropdown.

The first production run exposed two real clipping paths. SequenceSlate symbol
selectors offered **159 px** for a **272 px** neutral choice and **203 px** for
the **295 px** pseudo form. After that repair, the pseudo Node Editor exposed
its “Middle” TextAnchor item at **65 px required vs 49 px available**. A shared
measured dropdown-width helper now gives every current item a visual reading
path in both selectors while leaving their collapsed layout compact. The floor
suite passed **10/10**; the related All Aboard, accessibility, and pseudo-locale
contracts passed **24/24**. This is still hermetic live-client geometry, not
physical 1366×768 hardware, `DeviceDpi`, non-client/shown-window, 200-percent
zoom, keyboard, screen-reader, contrast, or print evidence.

### Completion audit: source-bound load instruments — 31 August 2026

The clean-tree check alone did not prove that ignored `bin`/`obj` outputs came
from the recorded commit. The headed harness also exposed `-SkipBuild`, while
all child processes deliberately use `--no-build --no-restore`. Both the headed
UIA and exact burned-region image instruments now perform one repository-
configured locked restore followed by a forced non-incremental Release build
before starting contention. The headed escape is gone. Each summary binds the
clean commit to SHA-256 values for the exact harness and produced test assembly
before any repetition begins; child processes remain fresh and no-build.

The focused source contracts measured **12/12**, and both PowerShell files
parsed without errors. No timeout changed and no load batch ran: the required
clean-tree precondition remains false while this combined work is intentionally
uncommitted. These improvements strengthen future provenance only. They do not
diagnose or close any of the three historical under-load sightings.

The same audit added the canonical unrun Atlas priority packet and its
authority-order regression to release traceability. That link is documentation,
not a session, council recommendation, record freeze, priority selection,
product-owner disposition, ADR, or protected-seat act.

### Closing evidence and new local UI sighting — 31 August 2026

The first complete-solution invocation was green in its console output but used
one shared TRX filename for all seven projects; later projects overwrote earlier
receipts, so that run is not counted as final evidence. A subsequent sequential
run retained valid Unit **740/740** and Integration **267/267** receipts, then
the full UI Automation process produced no result or further output for more
than five minutes and was stopped. A second plain minimal full-UI invocation
did the same. Neither stalled invocation produced a TRX, active-test name,
stack, or dump.

The exact 243-test UI binary separately passed **243/243** with ordinary console
output, blame-hang enabled, and VSTest diagnostic logging. Its **234** nonheaded
tests passed together, and its **9** headed tests passed together. Those
timing-altered and partitioned passes narrow the observation but do not diagnose
it. The two stalls are recorded as one new timing-sensitive same-process suite
sighting. They are not evidence that either historical headed UIA timeout
recurred.

The audit did prove independent process-lifecycle defects in the harness. The
pseudo-locale headed test sent a tree kill without observing child exit;
`HeadedApp.Dispose` ignored an unconfirmed five-second wait; and a constructor-
time UIA failure occurred before fixture ownership reached `using`. A shared
bounded shutdown contract now requires every child exit to be observed,
constructor failure owns the same cleanup, and a simultaneous assertion and
cleanup failure retains both exceptions. Two direct tests cover forced and
already-complete child termination. This is justified harness hardening, not a
claim that those defects caused the stalls. The UIA polling stopwatch also
does not make a synchronous UIA/COM call process-bounded; a diagnosis still
requires a stalled-run process inventory and native/full dumps. No timeout was
increased.

After that guard, the exact plain minimal UI command completed **245/245** in
45 seconds. The formatted combined tree was rebuilt in Release with **0 warnings
and 0 errors**. Full `dotnet format` fix and `--verify-no-changes` both exited
0. Two independent sequential closing runs then each produced exactly seven
TRX files read directly and measured **1,779/1,779 passed**, with zero failed,
error, timeout, aborted, or not-executed outcomes: Unit 740, Integration 267,
UI automation 245, Contract 100, Rendering 81, Instructional Evals 336, and
Accessibility 10. The retained result roots are
`C:\Users\gbaco\AppData\Local\Temp\ocf-forge-close-run1-20260831` and
`C:\Users\gbaco\AppData\Local\Temp\ocf-forge-close-run2-20260831`.

Press definitions and sample inputs did not change, so the conditional
SampleGenerator two-run hash rite did not apply. The closing passes are
non-reproductions, not diagnoses or closure of any recorded sighting. The
unresolved ADR-001 recipe/schema/evaluation identity choice still precedes
staging, commit, and push. No version, tag, signature, package, installation,
distribution, publication, filing, correspondence, live-library preparation,
or protected-seat act occurred.

### Completion audit: pseudo inventory, Board intake, and the floor — 31 August 2026

The accented trailing `ẋ` marks shown in the typist's screenshot are the
intentional pseudo-locale padding. They are expected only while the exact
`--pseudo-locale` switch or inherited `OCF_PSEUDO_LOCALE=1` setting is active;
they are neither damaged text nor a second language. An ordinary launch that
shows them is a selector/configuration defect to investigate, not content to
ship.

That diagnostic exposed two measurable contract gaps. The runtime claimed
that every complete string expands by at least 40 percent, but its earlier
padding used only letter count: an exact projection measured **178/1,078**
catalog entries below 1.40×, with a minimum near 1.2063×. The pseudo surface
walk also covered eight of the nine shipped forms and omitted Board-to-Brief.
Catalog-wide and form-inventory red contracts pinned both findings. The runtime
now retains letter-derived stress padding while filling any complete-string
deficit; the surface walk constructs a wholly synthetic Green Board intake,
checks its grid-combo choices, and reflection-compares its deliberate scenarios
with every concrete shipped form. The focused pseudo plus reviewed-catalog
batch measured **34/34**.

The stronger padding then found one real floor failure rather than being
weakened to fit it. At pseudo 125 percent, a Node Editor vector-primitive row
required **635 px** in a list offering **623 px** with no horizontal reading
path. The production list now exposes its native horizontal scrollbar. The
complete floor matrix then measured **10/10**. This remains hermetic live-client
geometry, not physical 1366×768 hardware, actual `DeviceDpi`/non-client chrome,
keyboard, NVDA/Narrator, contrast, print, protected-language, or educator-
usability evidence.

### Completion audit: process-bounded sighting evidence and CI — 31 August 2026

Both under-load reproduction scripts formerly requested a forced process-tree
kill after their outer cap but discarded its result and continued without
observing exit. They now retain the termination request status, wait a further
bounded ten seconds for process exit, write `TerminationConfirmed` into the
per-run receipt, and abort the batch after retaining that receipt if exit is
unseen. The two new red contracts first measured **0/2** and the complete
harness-source batch then measured **14/14**; both scripts parsed cleanly.
This does not diagnose any historical flake.

The hosted CI test step also had only the six-hour job ceiling. It now keeps the
same single solution-wide process and ordinary scheduling—no project split,
filter, retry, blame mode, or test-level timeout—but adds a 15-minute outer
process cap. The runner retains unfiltered stdout/stderr, fixed command
identity, commit/SDK/process/timing/exit/termination fields, and positively
observes a timed-out process tree. A 20-minute Actions step limit is only the
backstop. An immediate `if: always()` upload retains the receipt, streams, and
any completed TRX/Cobertura files before downstream gates. Its three source
contracts passed and the PowerShell file parsed cleanly. No hosted run is
claimed for this uncommitted tree; workflow text alone is not a CI conclusion.

### Completion audit: identity-held Scaffold Smith finding — 31 August 2026

A direct current-builder probe found that the required task-entry
`fade-criterion` accepts blank or whitespace-only input, emits `fade when    .`,
and reports no initial or review blocker. The current Instructional Evals suite
still passed **336/336**, its task-entry corpus **41/41**, and its focused unit
path **47/47**, demonstrating the coverage gap rather than negating the
finding. Correcting acceptance changes the existing
`scaffold-smith.task-entry` recipe `0.1.0`, shared schema
`schema.scaffold-smith.v1`, and evaluation `0.1`. It is therefore
recorded and held behind the same unresolved ADR-001 first-freeze versus side-
by-side-identity decision; no silent in-place behavior change was made.

### Closing evidence after the pseudo and process-bound audit — 31 August 2026

The formatted exact tree rebuilt in Release on SDK **10.0.302** with **0
warnings and 0 errors**. Full `dotnet format` fix completed and
`--verify-no-changes` exited 0; the post-format Release rebuild again measured
0 warnings and 0 errors. All three PowerShell harnesses parsed cleanly and
`git diff --check` found no whitespace errors.

The new bounded CI runner's normal path then executed the unchanged single
solution-wide test selection twice with coverage. Each run produced seven TRX
files copied into its own evidence root and read directly: **1,786/1,786
passed**, zero failed, skipped, error, timeout, aborted, or not-executed. The
suite totals were Unit **745**, Integration **267**, UI automation **247**,
Contract **100**, Rendering **81**, Instructional Evals **336**, and
Accessibility **10**. Run one completed in **58,664 ms** with evidence at
`out/ci-test-run/20260831T224452Z-e96a5f77723b440fbd4772f04dba7462`;
run two completed in **57,080 ms** with evidence at
`out/ci-test-run/20260831T224612Z-f876368809f84359b0555c3b4bf22de9`.
Both receipts record exit 0, no outer timeout, and confirmed process exit.

Those passes are ordinary local non-reproductions, not diagnoses or closure of
any historical or same-process sighting. The clean-source precondition still
correctly prevents either under-load reproduction batch on this deliberately
uncommitted tree. Press definitions and sample inputs did not change, so the
conditional SampleGenerator two-run hash rite did not apply. The unresolved
ADR-001 identity decision still precedes staging, commit, and push; no version,
tag, signature, package, installation, distribution, publication, filing,
correspondence, live-library preparation, or protected-seat act occurred.

### Completion audit: action runtimes, pseudo activation, and Atlas authority — 31 August 2026

The current workflow audit measures **21** `uses` occurrences containing **9** exact
selectors. A machine-readable inventory now binds every selector to its
reviewed action, immutable commit, release tag, and JavaScript runtime; the
composite Pages upload action also records its pinned Node 24 child. The source
contract recognizes supported block-key spellings including whitespace before
the colon and fails closed on an unsupported mapping shape, unreviewed ref,
unexpected selector, mutable local action, comment drift, or runtime drift. Its
red counterexample was the exact Checkout
v4.2.2 commit: it satisfied the old 40-hex-only rule while still declaring
Node 20. The strengthened contract measured **8/8**. The hardening checklist and OP-05
traceability now point to the inventory rather than claiming that a SHA alone
proves runtime currency.

No pseudo-locale leak was found. The exact switch, inherited environment
selector, repository launch paths, and post-run process environment were all
measured; `OCF_PSEUDO_LOCALE`, `OCF_UI_CATALOG`, and the matching command-line
switch were absent outside the deliberate headed tests. A real containment gap
did exist: `UiLocale.Configure` and its test selector were public process-global
mutators. A reflection contract failed on both, they are now assembly-only, and
the complete reviewed-catalog class measured **27/27**. Thus the typist's
accented trailing `ẋ` marks remain expected pseudo padding during an intentional
pseudo run; seeing them in an ordinary launch remains a selector/configuration
defect, not acceptable production text.

Accepted ADR-008 had also overstated the council's authority by saying the
council “selects” the next Atlas priority. The corrected decision order is
council recommendation, participant-record freeze, engineering feasibility,
and written product-owner disposition. Its mechanical governance contract
measured **1/1**. No Atlas priority, council outcome, or protected-seat judgment
was invented.

### Live Board-to-Brief stall diagnosis and async-boundary repair — 31 August 2026

A later bounded normal run completed in **57,982 ms**. Its retained stdout
recorded all seven suites green at **1,790/1,790**: Unit 748, Integration 267,
UI automation 248, Contract 100, Rendering 81, Instructional Evals 336, and
Accessibility 10. Its per-project TRXs were not copied before the immediate
repetition overwrote six of them, so this is retained stdout and process-receipt
evidence rather than seven retained direct TRXs.

The immediate unchanged repetition reached the 900-second outer cap. Six
direct TRXs measured **1,542/1,542 passed**; UI automation reported 208 passing
completions but produced no TRX. The runner retained the streams and receipt,
returned exit 124, recorded `taskkill` exit 0, and confirmed process-tree exit
after **900,297 ms**. Those receipts are retained at
`out/ci-test-run/20260831T230648Z-60ca81249fca44efad141c60718669b4`
and
`out/ci-test-run/20260831T230752Z-1b7447dd6e6742a2a82a94c5c8c78cb1`.

A separately captured full dump of the live UI testhost, PID **33808**,
localized the stall to
`Unexpected_OCR_fault_during_terminal_cancel_cannot_block_purge`. The UI STA
was in `Application.ThreadContext.OnThreadException` → `Form.ShowDialog` →
`Application.DoEvents` → `BoardToBriefIntakeContractTests.PumpUntil`. Terminal
cancel and `CaptureAndRecognizeAsync` both awaited the same OCR task. Its
synthetic post-cancellation `KeyNotFoundException` was contained on the
terminal path but escaped the duplicate capture path, causing WinForms to open
a modal exception dialog while the unattended test waited behind it. The
488,046,792-byte full-memory dump and temporary diagnostic tool were deleted
after this content-free stack finding was retained; they are not recoverable
from the repository.

The repair retains the shown-form capture task, contains only the duplicate
fault after the owned terminal cancellation is already underway, and proves
purge before completion. It does not mask an ordinary unexpected fault: an
initial capture-surface `KeyNotFoundException` now completes terminal cleanup
and then reaches `Application.ThreadException`. Deterministic red/green probes
covered both paths, and the full Board intake contract measured **10/10**.

The same async-boundary audit found three exporters that mislabeled any
`OperationCanceledException` as a user cancellation and one live Module Studio
Cancel handler that observed, but did not surface, a throwing cancellation
callback. Press Room, SequenceSlate, and Module Studio now recognize
cancellation only when their own token was requested. The live Cancel handler
awaits its callback work so unexpected callback faults reach the UI fault
boundary; close and disposal retain their bounded fire-and-observe behavior.
Four red regressions first measured **0/4**, then **4/4** after repair; the
three affected form classes measured **73/73** twice. Their unchanged-existing-
destination, stage-cleanup, restored-control, and fault-observability assertions
all passed. The combined Board, export, Module Studio, and reviewed-catalog UI
batch measured **110/110**.

This diagnoses the live Board-to-Brief same-process stall only. It neither
relabels nor closes the three historical under-load sightings: the lost-message
burned-region image failure and the `Part3…` and `PilotDay…` headed UIA
timeouts. It also does not retroactively prove the cause of earlier undumped
same-process stalls.

### Final closing evidence after the diagnosed Board stall — 31 August 2026

The exact combined tree resolved SDK **10.0.302** and rebuilt in Release with
**0 warnings and 0 errors**. Full `dotnet format` fix completed; its first
verification usefully rejected two expression-bodied test event handlers with
IDE0061. After their explicit block-body correction, format fix and
`--verify-no-changes` both exited 0, and the post-format Release rebuild again
measured 0 warnings and 0 errors.

Two unchanged bounded full runs then each produced seven TRXs copied before the
next run and seven matching coverage files. Direct XML reads measured
**1,794/1,794 passed** in each run, with every other outcome counter zero: Unit
748, Integration 267, UI automation 252, Contract 100, Rendering 81,
Instructional Evals 336, and Accessibility 10. Run one completed in **59,474
ms** with evidence at
`out/ci-test-run/20260831T233547Z-83b864fec7e845d99ccbe8e064e854a8`;
run two completed in **59,177 ms** with evidence at
`out/ci-test-run/20260831T233753Z-7180ac3ed32e431eb1ffe75d7bffc3f4`.
Both receipts record exit 0, no outer timeout, confirmed process exit, and an
empty stderr stream.

Press definitions and sample inputs did not change, so the conditional
SampleGenerator two-run hash rite did not apply. The unresolved ADR-001
recipe/schema/evaluation identity choice still precedes staging, commit, and
push. No version, tag, signature, package, installation, distribution,
publication, filing, correspondence, live-library preparation, or
protected-seat act occurred.

### Final practical-floor and bounded-evidence continuation — 31 August 2026

The historical “nine” is not the current runtime door count. The measured
Built-in Studios catalog exposes **10 doors and 11 modes**, because Scaffold
Smith owns two modes. Eight modes are mechanically buildable; Access Remix,
ReteachSignal, and Rubric Relay remain visible and held. SequenceSlate is a
separate top-level authoring surface, while Module Zero and Symbol Commons sit
outside this count. No protected door was converted into keyboard authority.

This continuation supersedes the historical one-profile **8/8** floor account
above and the later **10/10** interactive-chrome continuation without rewriting
either historical measurement. The 1366×768 covenant is now explicit as a
**practical functional floor**: it is not a fixed canvas, design target, or
startup cutoff, and reflow, wrapping, and genuine scrolling are valid; however,
an unreadable, unreachable, or unrecoverable supported core path still fails the
floor. The final matrix exercises 1366×728 and 1366×720 client areas at neutral
100 percent and pseudo 125 percent. Its **12/12** cases cover unresolved and
completed Board review, reading-order movement, purge recovery, Capture
normalization refusal and purge recovery, and busy/refused export states for
Press Room, SequenceSlate, and Module Studio. The tighter profile exposed and
drove a height-only Loaded Project preflight repair. This remains hermetic live-
control-tree geometry, not physical 1366×768 hardware, actual `DeviceDpi` or
non-client/shown-window evidence, keyboard or NVDA/Narrator observation,
contrast/high-contrast, 200-percent zoom, or physical print inspection.

The second-chrome machinery is complete only on its seat-neutral side. The
final UI TRX contains **28/28** reviewed-catalog contracts and **10/10** pseudo-
locale contracts. Exact test-only pins admit complete synthetic LTR and RTL
catalogs through every shipped surface and mode, so the combined mechanical
path measured **38/38**. These tokens are fixtures, not translations or
qualified review. Production's compiled catalog SHA-256 allowlist remains
exactly empty; no real second chrome language, reviewer identity, provenance
record, or activation pin was invented.

The managed-upgrade boundary measured **50/50**: `OcfprojUpgradeTests` supplied
30 results and `ProjectUpgradeOperatorHostTests` supplied 20. The host binds
review and preparation to the executing build's exact sorted recipe-ID/version
inventory and digest, structurally carries `Ctrl+C` into cooperative
cancellation, refuses non-Windows operation before plan processing, and keeps
the published plan schema and parser in parity. The digest deliberately does
not identify schema, validator, renderer, warning, or evaluation behavior; no
real console-signal process ran. ADR-007 remains Proposed, and there is still no
archived outgoing release package, frozen outgoing-version renderer route,
schema-2 migration, authorized operator package, real closed-library plan, or
managed-device version-bump/rollback proof. The mid-pilot upgrade debt is
therefore narrowed, not closed.

Atlas remains council-first. The repository still contains no real session,
enacted decision procedure or quorum, recommendation, participant-frozen
record, engineering-feasibility appendix, written product-owner disposition,
or selected next priority. Rehearsal and mechanical governance checks do not
manufacture any of those acts.

The final CI runner keeps one unfiltered, no-retry, no-blame solution-wide
Release `dotnet test --no-build` process under a 900-second outer budget. It
holds a repository lock; durably records starting, running, or stranded state;
uses deadline-accounted 250-millisecond process polling; retains incremental
stdout/stderr; and, after a timeout or interrupted parent, records a bounded
`taskkill /T` attempt, parent-exit observation, and stream-drain result. Before
the child starts it derives the exact solution/test-tree inventory, requires one
Release target framework and a project-local `bin/Release/<TFM>` assembly for
each suite, and rejects duplicate normalized test DLLs. Its receipt binds
pre/post HEAD, status and tracked-plus-untracked-nonignored source-content
fingerprints, pre/post Release test-assembly hashes, and exactly one newly
produced validated TRX plus one direct Cobertura file per suite; every curated
copy is hash-compared. The workflow uploads this curated evidence immediately
under `if: always()`, reserves raw `TestResults` for failure diagnosis, and feeds
only curated coverage into the threshold gate.

Those controls do **not** prove source-to-binary or dependency-closure
provenance. Likewise, `taskkill /T` success plus parent exit and pipe EOF is not
a Job Object or adversarial descendant-exit proof; PID reuse, adversarial path
TOCTOU/hard-link substitution, abrupt power loss, and non-polling native calls
remain explicitly bounded residuals. No hosted workflow conclusion is claimed
for this uncommitted tree.

The exact formatted tree resolved SDK **10.0.302**. Full `dotnet format` fix and
`--verify-no-changes` exited 0, and the post-format Release solution build
measured **0 warnings and 0 errors**. Because the changed symbol manifest feeds
the SampleGenerator, it ran twice into
`out/closing-samples/20260901T022129Z-f2ee33e0b06a41d3b32be88849904c98`:
both inventories contained **40 files** and **773,759 bytes**, with zero
inventory or byte mismatch; the sorted filename-plus-hash manifest SHA-256 was
`DEF10A3258A2F2ABA922DF8F1BC38FC3A3209065B36F81F44C41B4FE047F4A90`.

Two unchanged bounded full runs then each produced seven curated TRXs and seven
matching direct coverage files. Direct XML reads measured **1,826/1,826 passed**
in each run, with every other counter and every non-passed result-record count
zero: Unit **772**, Integration **270**, UI automation **255**, Contract **100**,
Rendering **81**, Instructional Evals **336**, and Accessibility **12**. Run one
completed in **66,236 ms** at
`out/ci-test-run/20260901T022139Z-eac8913e296045bd954bdc9bc1750fd9`;
run two completed in **64,479 ms** at
`out/ci-test-run/20260901T022318Z-b3f630cbb4de44f08e8efcb7f7e1afac`.
Both receipts record runner and child exit 0, no timeout, observed parent exit,
complete output drain, safe subsequent-run state, stable source and all seven
test-assembly identities, zero snapshot/identity/completeness/copy errors, and
7/7 valid TRX plus 7/7 valid Cobertura files. This mechanically closes the
formerly empty `tests/Accessibility` suite debt and records a source-side Node
24 action inventory, but it is not a hosted Node-runtime observation.

The two historical headed UIA timeouts and one image failure remain exactly
three under-load **sightings, not diagnoses**. Both final runs are local non-
reproductions only; the separately diagnosed Board-to-Brief modal-stall defect
does not relabel them. ADR-001's first-freeze versus new-identity decision still
precedes staging, commit, and push. No version, tag, signature, package,
installation, distribution, publication, filing, correspondence, real-language
activation, Atlas decision, or protected-seat act occurred.

### Recipe identity disposition packet — 31 August 2026

A read-only audit converted the remaining recipe-identity ambiguity into an
explicit [disposition packet](../adr/recipe-identity-disposition-packet.md). It
does not reopen accepted ADR-001 and selects no option. It records the exact
existing tuples affected by behavior, output, shared-validator, or evaluation-
corpus changes; the same tuples' presence in the public unsigned
`v0.7.0-alpha` tag; the separate display-name decisions; and the required
choice among an explicit first-admission freeze, genuinely side-by-side new
identities, or an exhaustive per-identity split.

The audit also found that implementation-plan §6.5 and ADR-003 require a
`recipeHash`, while strict project schema 1 and the candidate-recipe inventory
carry only recipe ID/version. That gap cannot be repaired by adding a property
under the old schema label. It remains an explicit legacy/forward-route or
schema-2 decision, not a closed migration claim. The packet leaves every field
blank for the accountable product-owner/typist act. Staging, commit, push, and
all versioning, deployment, outward, and protected-seat acts remain held.

### Real console-cancellation exercise — 31 August 2026

The remaining console evidence gap is now closed at the local Windows process
boundary without changing a recipe, schema, evaluation identity, or live
library. `ProjectUpgradeOperatorHostTests` starts the referenced
`ProjectUpgradeHost` apphost in its own hidden console and supplies a closed,
temporary inventory of 512 copies of the synthetic frozen upgrade fixture. It
starts a separate Windows-only test-support process immediately after launch.
That sender attaches to the isolated console, checks target liveness, and waits
until the real batch-lock path refuses a second read with a sharing violation;
it then calls `GenerateConsoleCtrlEvent(CTRL_C_EVENT, 0)` without the parent
test's polling delay. This is a bounded observation followed immediately by
delivery, not a deterministic held rendezvous: target exit, loss of the
exclusive-lock observation, or observation timeout fails the exercise. The
test does not invoke the handler or cancel its token directly.

The launcher now uses `STARTUPINFOEX` and
`PROC_THREAD_ATTRIBUTE_HANDLE_LIST` so the host inherits exactly its NUL input
and redirected output/error handles rather than every inheritable handle in the
testhost. An unrelated inheritable-file probe must remain deletable while the
launched host is proved alive. Forced teardown verifies termination and a
bounded five-second exit observation before releasing its sole process handle;
sender timeout handling likewise observes exit after a forced kill.

Before that inheritance/rendezvous hardening, the launched host exited **3**,
printed the stable content-free
`upgrade.canceled` refusal, disclosed neither synthetic path nor fixture text,
left the candidate root empty, and left all **512/512** source packages at the
frozen SHA-256. The exact case passed once during implementation and then in
five independent immediate reruns. The combined managed-upgrade boundary now
measured **51/51** at that point: `OcfprojUpgradeTests` 30 and
`ProjectUpgradeOperatorHostTests` 21. Release compilation of the focused
Integration project measured **0 warnings and 0 errors**.

After the hardening, a fresh Release build of that Integration project again
measured **0 warnings and 0 errors**, and targeted format verification exited
0. Five independent exact-process runs then passed **5/5** in 572–609 ms by
direct TRX result duration, each with zero failed or other outcomes. Every
receipt preserved the same content-free `StdOut` facts: `CTRL_C_EVENT`, sender
exit 0, host exit 3, `upgrade.canceled`, zero candidate entries, 512 source
packages, and the frozen source SHA-256. The five receipts are retained under
`out/upgrade-console-cancel-review/20260901T031208Z-81382279fb904366a2152e2cb96cae6b/`.
The prior 51-case combined count is not relabeled as a post-hardening run; the
exact changed boundary is what was rerun here.

The retained focused receipt is
`out/upgrade-console-cancel/20260901T025456Z-dc6a098a3b1f4959880625253b42a572/console-cancel.trx`.
Direct XML inspection measured **1/1 passed**, zero failed, and preserved the
content-free signal, sender/host exits, refusal code, candidate count, source
count, and frozen source hash in `StdOut`.

This is a real Windows console-control event delivered to a real child process,
not proof of a physical keyboard chord, a particular terminal emulator, a
remote-management service, Intune, or managed-device cancellation. Non-Windows
operation remains fail-closed before command or plan processing. ADR-007 is
still Proposed, and every package, deployment, real-library, rollback,
identity, typist, district, and protected-seat hold remains unchanged.

### Under-load evidence-boundary hardening — 31 August 2026

The two headed and one burned-region image failure remain exactly three
historical under-load **sightings, not diagnoses**. Their two reproduction
instruments now share a cooperative lock and checked no-reparse evidence-root
creator; perform a locked restore and forced non-incremental Release build; and
compare pre/post HEAD, status, tracked-plus-untracked-nonignored source content,
and every file under the exact Release TFM output root. Drift, missing/failed
results, or invalid load-worker liveness refuses the batch, while a completed
batch retains a create-new, write-through summary before releasing the lock.

Timeout cleanup now uses a Process-based bounded `taskkill /T /F` helper. A
subsequent repetition requires a started, non-timed-out, exit-0 request,
observed helper settlement, and observed launcher exit. This is not
compiler/source correspondence, transient mutate-and-restore detection,
arbitrary-builder exclusion, complete SDK/NuGet process closure, a Job Object,
or descendant-exit proof. This note supersedes this handover's earlier
`TerminationConfirmed` and “confirmed process-tree exit” wording: those records
observed a launcher/parent boundary, not every descendant. The deliberately
dirty tree prevented current reproduction batches; the only new execution
evidence is **3/3** clean PowerShell parses and focused helper/source-contract
tests at **20/20 passed**. No broad-suite result, causal diagnosis, or sighting
closure is claimed by this continuation.

### Final unchanged-tree closing evidence — 31 August 2026

The final closing pass used SDK **10.0.302**. A Release solution build after
formatting measured **0 warnings and 0 errors**. Full `dotnet format` fix and a
separate `--verify-no-changes` invocation exited 0. The PowerShell parser found
zero errors in both evidence modules and all three runner scripts, and no
active-or-stranded CI marker remained.

Two unchanged bounded solution-wide runs then each produced exactly seven
curated TRXs and seven direct Cobertura files. Direct XML reads of every TRX
measured **1,833/1,833 passed** in each run, with zero failed, not-executed, or
other result records: Accessibility **12**, Contract **100**, Instructional
Evals **336**, Integration **271**, Rendering **81**, UI automation **255**, and
Unit **778**. Run one completed in **68,803 ms** at
`out/ci-test-run/20260901T031521Z-01987f1b039b4810883f75bb3af73905`;
run two completed in **62,798 ms** at
`out/ci-test-run/20260901T031840Z-e6070ed1e428437880a66cb11405f229`.

Both receipts record runner and test-process exit 0, no timeout, observed
parent exit, complete output drain, safe subsequent-run state, zero identity
or evidence-completeness errors, and 14 hash-checked evidence copies. Before,
after, and across both runs, the source-content SHA-256 remained
`F2F7A2C42840C28AA7485492F880D00CC3339C88728EBA7FE79EA08AC43B1794`;
the canonicalized seven-test-assembly manifest SHA-256 remained
`8662DF4C61B90B8D68231BE597BF5184A52168F754704268A3089D870DD5D836`.
Those are content and selected-output identity observations, not a compiler or
dependency-closure provenance claim.

Direct reads of the second run's Integration TRX measured the complete upgrade
boundary at **51/51 passed**: `OcfprojUpgradeTests` **30/30** and
`ProjectUpgradeOperatorHostTests` **21/21**. Its Unit TRX measured the load-
evidence contracts at **20/20 passed**: shared evidence helpers **6/6**, headed
UIA harness contracts **7/7**, and image harness contracts **7/7**. No headed
or image reproduction batch was run on the dirty tree, so the original three
under-load sightings remain sightings and no cause is assigned.

No press or sample input changed after the prior two-way SampleGenerator proof,
so it was not rerun merely to create a newer timestamp. The prior 40-file,
773,759-byte, zero-mismatch receipt and manifest hash remain the applicable
evidence for the unchanged press inputs.

The recipe-identity disposition packet remains deliberately undecided. No
staging, commit, push, version, tag, signature, package, installation,
distribution, publication, filing, correspondence, real-language activation,
Atlas decision, or protected-seat act occurred.

### Atlas dated-record mechanics guard — 31 August 2026

The canonical Atlas packet remains unchanged and **UNRUN**, and the council
directory still contains no dated `atlas-priority-session-YYYY-MM-DD.md` record.
A new non-UI, source-only validator now watches any future dated copy without
running or interpreting a council session. It accepts only the packet's existing
`UNRUN`, `SESSION HELD — REVIEW PENDING`, and `COUNCIL RECORD FROZEN` vocabulary;
requires the existing headings in lifecycle order; requires held status to carry
the existing session-occurrence record; and checks canonical table widths with
escape-aware Markdown parsing. It retains the seat-and-count structure without
trying to decide whether a human-authorized public-credit identity may appear,
requires an explicit recommendation identity plus absent-seat and per-
recommendation holds, and refuses feasibility until recommendation and record
freeze precede it or product-owner disposition until feasibility precedes it.
Refusal messages do not echo untrusted record values.

The boundary is deliberately narrower than governance. The validator does not
infer quorum, score needs, rank or recommend possibilities, authenticate
participants, select a priority, or perform a protected-seat act. A recorded
“not enacted; no quorum; unranked consultation” therefore passes when the
documentary fields are complete; its meaning and authority remain wholly human.
The future-file repository test finds every `atlas-priority-session*.md`
candidate other than the canonical template, so malformed dated-copy names are
also refused; the synthetic fixtures are the only records exercised in this
continuation.

The focused Release build completed with **0 warnings and 0 errors**. Targeted
format fix and two separate `--verify-no-changes` checks exited 0. The validator
and Atlas governance batch passed **21/21**, with zero failed or skipped. A
follow-up traceability batch passed **23/23**, and the focused architecture and
repository-hygiene batch passed **21/21**. The solution parser also resolved the
new tooling project from the checked-in `.slnx`. No broad suite or real council
activity is claimed, and no Atlas priority, feasibility judgment, product-owner
disposition, participant evidence, or protected-seat review was created.

### Practical-floor and upgrade close preparation — 1 September 2026

The 1366×768 requirement is now consistently implemented and described as a
**practical functional floor**, not a fixed canvas, design target, or startup
cutoff. Reflow, wrapping, and genuine scrolling are valid; essential controls,
status, and recovery remain required to be readable and reachable. The hermetic
matrix now covers 1366×728 and 1366×720 working areas at neutral 100 percent,
pseudo 125 percent, and neutral 200 percent, plus exact-hash test-only synthetic
LTR and RTL reviewed-catalog paths at 1366×720. Production keeps no fixed 1366
canvas constant and no second-language activation.

Independent review found two oracle-evidence gaps after the first expanded run:
an oversized zero-area control could pass, and nested nonzero scroll restoration
was not guarded. Both now have exact synthetic regressions. Export cleanup has a
separate test that preserves the original failure while settling the operation.
Board-to-Brief's capture task is observed without an `async void` duplicate fault
path; initial capture, cancellation-time OCR, and active OCR faults fail closed,
purge transient bytes, and close and dispose the form without provider detail or
`Application.ThreadException`. Board's neutral default no longer manufactures a
body scrollbar, and Loaded Project preflight gives surplus height to the exact-
document row rather than its action row. Fresh post-format focused runs passed
the floor **17/17** and Board intake **11/11**. This remains hermetic client-tree
and in-process evidence, not physical DPI/device, keyboard, NVDA/Narrator,
contrast, print, language-seat, or accessibility-seat evidence.

The managed-upgrade candidate now binds review and preparation to the exact plan
digest, sorted recipe ID/version inventory digest, and a separately length-
framed digest of every declarative `RecipeManifest` field. A cross-runtime known-
answer vector pins that framing, reflection discovers the current static manifest
carriers, conflicting contracts under one ID/version fail closed, and the
portable-semantic-editor remains explicitly identity-only. These digests do not
bind executable builder, editor, renderer, schema, validator, or evaluation
implementations or bytes and are not the missing project `recipeHash`.

The product-owner packet now freezes the exact 23 outgoing alpha-tag identities,
all 15 candidate-only identities, both outgoing and current
`recipe-contract-fingerprint.v1` values for the shared identities, and the exact
11 direct manifest deltas. It remains **UNDECIDED**: no A, B, or evidence-backed
U route was inferred. The upgrade boundary passed **55/55**; the combined CI-
runner, fingerprint, packet, and Atlas guard batch passed **54/54**; the action-
runtime inventory passed **8/8**; and the post-document architecture,
traceability, hygiene, and packet batch passed **24/24**. All five PowerShell
runner/module sources parsed without errors. A Release solution build before
formatting and another after formatting each completed with **0 warnings and 0
errors**; full format fix plus independent no-change verification exited 0.

This note deliberately precedes the exact unchanged-tree solution-wide stability
runs because adding a receipt to this tracked file would itself change the source
identity being proved. The machine-readable summaries, TRXs, coverage files, and
hash-checked copies under `out/ci-test-run/` are the authoritative final receipts
for the tree containing this note; their conclusions and direct TRX counts must
be read rather than inferred from this preparation record. The existing two-run
SampleGenerator proof remains applicable: no generator, deterministic-press,
rendering, or manifest input changed after its 40-file, 773,759-byte,
zero-mismatch receipt and sorted-manifest SHA-256
`DEF10A3258A2F2ABA922DF8F1BC38FC3A3209065B36F81F44C41B4FE047F4A90`.

No staging, commit, push, version, tag, signature, package, installation,
distribution, publication, filing, correspondence, real-language activation,
Atlas decision, or protected-seat act occurred. Commit and push remain held
until the product owner completes the recipe-identity disposition and its chosen
route is implemented and reclosed.

### Post-close completion-audit corrections — 1 September 2026

A fresh read-only audit found four bounded evidence/mechanics gaps that did not
require a recipe route or protected judgment. The Atlas validator previously
protected heading order without parsing the need-card and mapping tables. It now
pins the canonical prompt set and order, table shapes and separators, repeated
complete need cards, exact preceding need IDs for every substantive mapping, and
the mapping prerequisite for recommendation content. Its expanded synthetic,
mechanics-only batch passed **24/24**. The canonical packet remains **UNRUN**;
the validator still cannot determine needs, participants, quorum, mappings,
recommendations, feasibility, disposition, or protected-seat findings.

Managed-upgrade `review` now prints every sorted, content-free constituent—recipe
ID, version, manifest fingerprint, and explicit identity-only status—without
changing the plan, source packages, or candidate root. Its ID/version digest now
uses explicit `candidate-recipe-identity-inventory.v1` strict-UTF-8 and big-endian
count/length framing rather than default JSON serialization; a cross-runtime
known-answer vector pins SHA-256
`067538B22611516220E7DEDC8D8685B3ADC501BE02909CD27AB6396E5784A826`.
The focused upgrade boundary passed **56/56**. Constituents are now inspectable,
not approved.

The recipe-disposition packet now exposes a clearly non-dispositive appendix of
all 23 exact outgoing and measured current-tree
`recipe-contract-fingerprint.v1` values, and its source contract parses and
compares every row. The route, candidate freeze, post-disposition versions,
schemas, evaluation identities, rationale, owner, and decision instant remain
blank. The packet and traceability also now state the independent §6.6 model
gap: compiled `RecipeManifest` does not yet represent local preprocessing,
localization resources, or migration IDs. Adding those fields changes identity
and is held for the row-by-row route. The packet contract passed **1/1**.

The same audit confirmed 10 visible module doors/11 modes, eight buildable
Built-in modes, and the separate SequenceSlate surface: nine buildable authoring
corpora. It also confirmed that production still has no real second-language
catalog or allowlisted digest while the deterministic neutral packet and exact-
hash synthetic LTR/RTL path remain complete. It did not erase the recorded
identity-changing acceptance gaps: SequenceSlate reorder and bilingual authoring
UI; Board perspective/glare, multi-page/PDF/slide intake, DOCX/ODT, and native
print-PDF; Scaffold source comparison and checkpoint card; Forumwright available-
time fit; and Inquirywright excerpt-bound verification, role-bound exact-fact
validation, and unresolved-rights sink enforcement. Those changes require the
product owner to choose the applicable recipe route before they can be enacted
under honest identities. Sensitive-topic, language, AAC/SLP, rights, physical-
device, AT, council, district, and release judgments remain with their seats.

This note again precedes the exact unchanged-tree ordered close. No staging,
commit, push, version, tag, signature, package, installation, distribution,
publication, filing, correspondence, real-language activation, Atlas session,
recipe disposition, or protected-seat act occurred.

### Provider, project-provenance, and shipped-catalog hardening — 1 September 2026

A final machine-owned audit found three bounded gaps that could be closed without
choosing a recipe route or exercising a protected seat. The shared provider
capability kit had asserted structured-output support but sent only text and
accepted any nonblank payload as structured. It now sends separate confirmed
text and valid one-pixel-PNG probes, requires every claimed success to parse as
the one-property `schema.capability-probe.v1` object under explicit 256-byte and
depth-4 bounds, retains cancellation proof, and supports a caller-supplied exact
request whose expected outcome is refusal. Wrong, extra, duplicate, non-object,
and oversized results fail. The source-compatible overload without that refusal
request always records conformance as unproved; it cannot report a clean provider.
The common kit deliberately invents no model-policy trigger: a real configured
deployment still needs its reviewer-supplied harmless refusal probe at Gate 3.
Mixed refusal-plus-output records fail the contract. Independent review caught
and closed both the formerly optional-refusal and any-object weaknesses before
the final ordered close. A schema/validator parity proof pins the published
schema to the same one-property contract. The focused capability batch passed
**20/20**.

The schema-1 `.ocfproj` reader formerly applied weaker text rules to embedded
asset provenance than the loose catalog and teacher shelf applied to the same
record type. It now retains the exact package ID, leaf-path, NFC, digest, and
private non-redistributable rights rules while also applying the shared required
and optional provenance policy. Invisible, directional, punctuation-only,
oversized, and control-bearing hostile values fail closed; safe optional values
survive load and exact resave. The integrated affected package/catalog batch
passed **101/101**.

The current local 13-symbol deployment is now fenced to exact manifest SHA-256
`103D117E37090A6CBB9968AE9485D7C1CD9C7D3EEEC65B13DFC1054609D1B2A4`.
That digest binds the exact bounded manifest bytes; ordinary integrity then
recomputes every manifest-declared asset digest, and the app-only closed-root
check refuses every unlisted top-level file or directory (so a nested tree is
refused at its root) without echoing its name or path. Generic exported packs
remain free to carry their governed `ATTRIBUTIONS.md` sidecar. Raw whitespace
drift, a semantically valid record change, top-level orphan, and nested orphan
all have direct regressions; the shipped-catalog/UI boundary batch passed
**26/26**.

The compiled catalog digest is a current-build identity fence only. It is not
AAC/SLP, accessibility, or rights approval; it admits no Mulberry, OpenMoji,
OpenSymbols, mapping, fallback, or learner-vocabulary behavior. Those candidate
sources and interactions remain held exactly where the council packet places
them. No generator, deterministic-press, rendering, or manifest input for the
SampleGenerator changed, so the prior 40-file byte-identical proof remains the
applicable press receipt.

This section precedes the exact unchanged-tree ordered close. No staging,
commit, push, version, tag, signature, package, installation, distribution,
publication, filing, correspondence, real-language activation, Atlas session,
recipe disposition, or protected-seat act occurred.

### Provider transport and district-policy boundary hardening — 1 September 2026

A fresh machine-owned boundary audit found that the Azure adapter still trusted
too much of a remote response and accepted a deployment value as part of a URI
template without first proving that it was one identifier segment. The adapter
now reconstructs the target from the normalized allowlisted origin, one bounded
ASCII-unreserved deployment segment, and one bounded escaped API-version query
value. Its production operation has one 100-second deadline spanning bearer-token
acquisition, response headers, response body, and the returned validation outcome.
Recoverable token-acquisition faults and invalid token shapes remain typed rather
than escaping the provider boundary. Automatic redirects remain
disabled; redirects, authorization failures, request timeout, rate limiting,
server failures, and all other body-independent non-success statuses are mapped
without opening their bodies. A response body that must be inspected is capped
at 1 MiB even when `Content-Length` is absent or false, and the pooled buffer is
zeroed before return.

Success now requires `application/json` with absent or UTF-8 charset, valid
UTF-8 bytes, JSON depth at most 16, no duplicate property at any depth, exactly
one choice, a supported finish reason, and one `assistant` message without a
tool or function call. Its content must itself be a strict JSON object; a typed,
nonblank refusal must carry no content and maps separately. Truncation, content
filtering, malformed output, transport failure, deadline expiry, and caller
cancellation remain distinct. Independent review of the first implementation
found that the assistant role and tool/function exclusions were not yet enforced.
A second review then measured cancellation expiring during synchronous response
validation while success was still returned, recoverable bearer-factory faults
escaping the taxonomy, and five duplicate-name cases failing for an unrelated
missing role. The contract now rechecks cancellation after validation, types the
authentication failures, and makes every duplicate fixture otherwise valid. The
follow-up also gives cancellation/deadline precedence when a token factory
returns after cancellation or the cap-plus-one body read reaches its terminal
outcome. The complete provider contract batch passed **152/152** with names and
outcomes preserved.

The district-policy reader now admits at most 64 KiB of raw bytes, parses only
strict UTF-8 JSON to depth 8, and requires an exact case-sensitive root inventory.
Unknown, missing, duplicate, case-confusable, malformed, empty, oversized, or
incomplete grants fail closed to `DistrictPolicy.Offline`; every endpoint must
itself be an absolute HTTPS URI without user information, lane values must be
string-only camel-case `green` or `amber`, and the transient byte buffer is
zeroed. The second independent review found that invalid and null endpoint
entries had previously survived the first structural pass; those grants now fail
closed. Teacher preferences retain their separate compatibility parser. The
focused policy-store batch passed **21/21**.

These are local parser and transport-boundary proofs, not a captured network
trace, configured-deployment attestation, retention statement, or Gate 3 review.
They choose no recipe identity and exercise no district, AAC/SLP, multilingual,
rights, accessibility, council, or product-owner authority. No generator,
deterministic-press, rendering, symbol-manifest, or symbol-asset input changed,
so the prior 40-file byte-identical SampleGenerator receipt remains applicable.

This note precedes the exact unchanged-tree ordered close. No staging, commit,
push, version, tag, signature, package, installation, distribution, publication,
filing, correspondence, real-language activation, Atlas session, recipe
disposition, or protected-seat act occurred.
