# Seven-recipe compatibility continuation

**Opened:** 6 September 2026 UTC.
**Branch:** `codex/seven-recipe-compatibility`.
**Starting source:** `7cb257e7df5dcc55a7414ef482bed035ee2b4fd8`.

**Current cutoff:** the implementation is committed and pushed as `630f707` in
draft PR25, with its own successful hosted evidence in the ledger. The final
record-only closure must earn its own exact-head checks; its successful report
ends the typist-selected engineering goal, not the held human/release work.

This supersedes the [fixture lifecycle continuation](2026-09-06-fixture-lifecycle-isolation.md)
for current navigation only. Its frozen record and all earlier failures remain
unchanged. The typist requested that earlier repairs be perfected first.

The fixture-isolation baseline now has its own successful hosted CI and
CodeQL conclusions in the [evidence ledger](../evidence/evidence-ledger.json).
The exact PR24 result is not an exact-main or later-candidate result. Earlier
sightings remain open. PR15's advisory-only merge is separately recorded;
the later production stack remains compatibility-held.

The [implementation record](../governance/seven-recipe-compatibility.md)
tracks the authorized engine `0.8.0-alpha` and seven recipe/evaluation
replacements, exact outgoing selection, fresh-approval boundaries and
compatibility proof. The first UI group failed five cases; their corrections
then passed the fresh 24-case UI group. A subsequent full preflight failed
eight 200%-scale accessibility cases (2,984/2,992 overall). The measured
version-row repair passed a fresh zero-warning build, format verification
and the full 26-case Accessibility assembly. Exact failure messages and all
cutoffs remain in the record. A later test-only extension selecting every
historical/replacement version passed a fresh complete 26-case floor run.
An additional real-modal probe reproduced an escaping unknown-asset refusal
twice without changing the reviewed draft or assets. Its initial instrument
cancellation error is retained separately. The narrow UI status/error-handling
repair passed its first modal check and then the complete 300-case UI assembly;
both final formatting-edge-case/status-persistence cases also passed a separate
stability run. ~~Full solution closing is not complete.~~ **Struck 6 September,
12:43 UTC:** the later local closing below completed those executable gates;
compatibility disposition and hosted verification remain separate.
Fresh exact-C1 semantic captures completed twice; 48 original press-default
captures also settled safely and retain both returned files and unsupported
SVG outcomes. They preserve original defects/refusals and do not certify the
replacement engine. Two actual candidate sample generations are byte-equal;
39 original files are unchanged and only the package's engine stamp differs.

Later 6 September measurements completed the original 506-case press boundary
capture and the four candidate semantic runs with separate historical/replacement
dispositions. The detailed record retains all raw changes and failures. Candidate
press defaults are underway; the first batch stopped on a PowerShell receipt-shape
error after a safely completed child. A separately reviewed correction continues
at 13 without retrying that child. The genuine C1 package instrument has built,
but its packages and the new 36-row backward-compatibility consumer's runtime
results do not yet exist at this cutoff. The consumer's two analyzer errors and
subsequent zero-warning build are recorded, not relabeled as passing tests.

Later readback completed all candidate defaults and sealed their raw evidence;
the first full comparer stopped on a controller-manifest property-name mismatch.
The original C1 package collector produced its first two packages, but their
outer audit stopped on the distinct PascalCase/camelCase representation
mismatch. Both failures remain preserved while narrow instrument corrections
are prepared. The first candidate boundary capture completed safely, retaining
the historical zero-chart block with no output; the other 1,011 remain unrun
at that cutoff. The detailed implementation record holds exact native receipts,
hashes and readback scopes. Collection success is not compatibility acceptance.
The replacement's default Letter calibration PDF clipping repair has also been
visually inspected in ordinary and low-ink forms; A4/browser, physical-print
and real-AT acceptance remain separate and incomplete.

The subsequent corrected full Defaults comparison completed with actual
native zero. Outgoing document, PDF and returned-SVG bytes match C1; HTML
retains the measured asset-CSS insertion and is not called byte-preserved or
browser-verified. Only calibration changes the measured same-engine default
document/render bytes. All A/B document and returned-file pairs match. The
complete report, all unsupported SVG/refusal records and 10,619 consumed-file
pins are retained and freshly verified in the detailed implementation record.
This comparison does not dispose of the unrun boundaries or package tests.

At the subsequent 12:25 UTC cutoff, all eighteen original package children and
their actual A/B comparisons are complete. Eighteen unchanged original fixture
variants and their measured index were installed after twice-derived encoding
and fresh source/copy checks. The thirty-six-case backward-open/selection/source
preservation/late-rollback consumer passed twice after Release build 25. Its
fixture provenance and detailed implementation record retain exact hashes,
full results and the earlier failed instruments without relabeling them.
The fixed archive-bound candidate boundary continuation 2–1012 is now running;
its completion and full comparison remain unclaimed. The candidate sample
manifest also now has its required explicit LF checkout rule, supported by
the retained red/green and 24-case stability regression.

Continue with actual candidate tests, archived/current comparisons, corrected
calibration layout evidence, backward-open/source-preservation/rollback, and
the narrowly measured current-writer sample relation. Preserve the original
40-member C1 baseline. Record failures before fixes and never transfer a
passing earlier build to new source.

The later 12:43 UTC local executable closing passed in order: Release build 26,
format-fix and zero-change verification 08, post-format Rebuild 27, then full
**3,049/3,049** solution tests twice with stable 606-file source and seven-assembly
identities. Both fresh forty-file sample generations are directly byte-equal;
the unchanged narrow baseline guard passed for both. The complete local
receipts, actual TRXs, sample relation and sealed original-package evidence are
bound in the detailed working record. These results do not complete the
still-running candidate boundaries, final shared-engine/schema disposition,
A4/browser or human/physical acceptance. Any later executable source change
requires fresh closing. Candidate commitment still awaits the compatibility
audit; exact-head CI and CodeQL remain required after a push.

The subsequent local CI checks at 12:48–12:49 UTC measured **93%** core-and-module
line coverage separately from each exact full run's retained reports, above
the unchanged 80% floor. Generated-source-path warnings remain retained.
The original C1/C2 ratification guard passed; it does not ratify the candidate.
The direct-and-transitive NuGet audit returned zero and reported no known
vulnerable dependencies across its 27 projects. These are dated local results,
not a complete hosted CI result or a guarantee that no vulnerability exists.

At 13:15 UTC, an additional test-only proof extension passes eighteen
edited-reopen/resave cases twice across both versions of all seven families,
including each learner-held definition. It verifies the actual library door's
fresh preflight/review, edit invalidation, portable-identity resave and original
byte preservation. The initial compile and assertion-instrument failures remain
in the detailed record; no product behavior was changed. ~~Fresh full closing for
this extended source is pending~~ (completed below); the earlier 3,049-result pair
is not silently transferred to it. ADR-010 also gains a historical-context clarification,
without changing its Proposed status or unfilled authority fields.

**Later 6 September, 13:39 UTC:** fresh ordered closing for the test extension
passed Release build 31, format-fix/verify 09, post-format Rebuild 32 and full
**3,067/3,067** tests twice. Root and a separate read-only committee audit checked
the actual results, source identity and seven assembly identities, rather than
borrowing the previous cutoff. All 28 original/retained TRX and coverage pairs
are directly byte-equal. Two fresh sample generations again match all forty
files and the unchanged narrow historical/candidate guard; each exact full run
separately measures 93% core-and-module line coverage. Generated-source-path
warnings and all preceding failed instruments remain retained. The detailed
record binds the exact receipts and hashes. Later documentation recording these
results is not represented as part of the frozen executable-test source.
The boundary capture and its eventual comparison remain unfinished; none of
these local results is own-head hosted evidence or final compatibility admission.

**Later 6 September, 14:14 UTC:** the updated neutral catalog was exported twice
with matching 1,084-entry bytes and an independently recomputed source digest.
Only the four new chrome strings were added; the packet remains draft and the
production allowlist empty. Bounded visual inspection now includes Letter and
A4 at 5/25 mm margins in ordinary/low-ink mode: eight source PDFs, sixteen
rasters, all eight raster pairs directly equal. The visible instructions and
calibration marks fit, with Poppler's two missing-font warnings retained.
The detailed record binds the exact files, gate results and limits. This closes
that generated-PDF inspection gap, not browser/AT or physical-print acceptance,
and not the still-running boundary capture's complete comparison/disposition.

The [seven-recipe authorization](../governance/2026-09-05-recipe-replacement-authorization.md)
is implementation authority only. The additional booklet-guide disposition
is still held. Schema 1's release stop, Proposed ADR-007/010, scope-specific
rights and CC BY-SA proposal limits, actual consent/custody/withdrawal,
appointments/quorum/recusal and H0–H7 reviews remain unresolved. No synthetic
committee supplies human seat authority. No publication, release, signing,
installation, distribution, correspondence or real-library upgrade is enacted.

**Later 6 September, 16:23–16:24 UTC:** the fixed candidate boundary collection,
its final 80,264-file seal and both full raw-comparison derivations are complete.
The comparer covers 506 C1 and 1,012 candidate observations, with 1,012 C1-cross,
759 A/B and 506 same-engine old/new relations; all 104,324 consumed input pins
passed its final fresh rehash. Actual native exit is zero and source stayed
stable during the comparison. Root directly compared the two report and input
manifests; a separate audit freshly verified every member of the boundary seal.
The detailed working record binds these facts and the deliberately interrupted
v01 retainer without relabeling it as success. Collection/comparison completion
is not product acceptance or final schema disposition.

The first summary step then failed its completion preflight, native one,
before producing an output directory. Both source raw binding records match;
the instrument compared one raw path to a filesystem-normalized path. Its full
failure remains while a narrow raw-to-raw correction is reviewed. Final bounded
technical disposition, draft commitment and own-head hosted evidence remain
pending. The README now states the measured candidate engine `0.8.0-alpha` and
application file version `1.0.0.0`, without manufacturing release correspondence.
No historical record, admission or human/publication hold is removed.

**Later 6 September, finite engineering closeout:** the typist clarified that
already-made improvements may be included and pushed, and that this goal should
end at a definite verified milestone. The
[bounded closeout](../governance/2026-09-06-bounded-engineering-closeout.md)
now maps all opening-audit findings and the accumulated repairs to that boundary.
~~Bounded technical disposition remains pending.~~ **Struck 6 September:** the
[technical assessment](../governance/seven-recipe-technical-disposition.md)
is complete for the declared corpus, with raw differences and export limits
retained. A secondary summary was voluntarily stopped, not called successful;
the complete primary reports and independent audit remain authoritative.

The final reconciliation measured a remaining Press authoring gap using only
synthetic text: it reached Green review without confirmation. The parameter-
panel guard, fresh-confirmation invalidation, pending-review protection and
associated source claims are repaired, with a zero-warning Release build and
62/62 focused tests. Loaded-project preflight stays independent. Final formatted-
source full/stability/accessibility/sample gates and own-head hosted evidence
remain pending in the closeout, not borrowed from the earlier 3,067-test pair.

**Later 6 September, 18:09 UTC:** ~~final formatted-source local closing is
pending~~ — struck after actual ordered Release build41, format-fix/verify12,
post-format Rebuild42 and two **3,078/3,078** full passes. The closeout retains
all earlier failed layout attempts and their full messages; the final single-
scroll-owner layout passes all 26 floor cases in both full runs. Source and
seven-assembly identities agree across the pair, and all 28 TRX/coverage copies
are directly byte-equal to their originals. Paired forty-file samples pass the
unchanged strict historical/candidate relation; both coverage measurements are
93%. Two fresh 1,085-entry neutral exports match and remain draft/unapproved.
These final local results certify the final executable-source cutoff, not a
later record-only edit or the still-pending draft's own hosted run. Commit/push,
exact-head CI/CodeQL and the bounded completion record are next; final admission,
main merge, old-PR closure, publication and human/release work stay held.

**Later 6 September, implementation-head closeout:** ~~commit/push and that
implementation head's own hosted verification remain pending~~ — struck after
the reviewed normal commit/push and direct readback of its own CI and CodeQL.
The exact entries, checkout identity and retained evidence are in the
[ledger](../evidence/evidence-ledger.json); the
[bounded closeout](../governance/2026-09-06-bounded-engineering-closeout.md#reviewed-source-push-and-implementation-head-closure)
records public-source hygiene and the finite completion rule. This record-only
follow-up must earn its own conclusions, reported to the typist rather than
predeclared in its own text. Following the existing record-chain stopping rule,
that successful report completes the selected engineering commission. No main
merge, old-PR closure, site publication, release or human ratification follows.
