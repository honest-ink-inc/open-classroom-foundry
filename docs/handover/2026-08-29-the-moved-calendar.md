# The moved calendar — the long window, and what it is for

> **Superseded for repository state by [the public turn](2026-08-30-the-public-turn.md) (30 Aug 2026).** The calendar reasoning, the counsel questions, and the re-ordered work below all still stand; the state of the repository moved on the same night.

**Date:** 29 August 2026, after the fourth menu was exhausted and the last CI exclusion deleted · **Audience:** the typist, the second maintainer when one is seated, whichever hand next opens this repository, and — for §II and §VI — counsel.

The pilot does not open on 8 September 2026. This document records why, what the critical path now is, what the delay unfreezes, and what must still not be built. It supersedes the September calendar in the [coordination plan](../pilots/human-gates-coordination-plan.md) and the [hardening checklist](../release/hardening-checklist.md); those documents' *substance* stands and only their dates are stale, which is itself a task in §V.

## I. What changed

Three things arrived at once, and they agree with each other.

1. **The entity track is longer than the pilot date.** Maryland must accept the articles; the EIN (Form SS-4) is filed after acceptance so line 1 matches the approved legal name exactly; Form 1023-EZ cannot precede the EIN. The typist has sequenced this correctly and it simply does not fit inside a fortnight.
2. **An educator wrote agreeing to join, and said 8 September does not fit them.** A council seat accepted on a date the seat-holder cannot make is not a seat.
3. **The typist reached the same conclusion independently.** 8 September was always the earliest honest date, not a comfortable one.

The new pilot date is **not yet set**, and this document deliberately does not invent one. It is downstream of Maryland's acceptance, which is downstream of the state's own queue. Setting a date before SDAT answers would be the kind of paper fact this project exists to refuse.

## II. The critical path is now legal, not technical

The forge is not the bottleneck and has not been since the fourth menu closed. The chain is:

> Maryland acceptance → EIN → 1023-EZ → (exemption recognized)

with three things hanging off it: the OV code-signing certificate (needs completed incorporation, EIN, and verifiable presence), the bank account (needs EIN), and whatever the counsel/accountant conversation concludes about tax-exemption path and copyright stewardship.

**Four questions belong to counsel, not to the keyboard.** They are recorded here so they are asked once, deliberately, rather than assumed:

1. **Is Form 1023-EZ actually available to Honest Ink, Inc.?** The whole schedule rests on it. The form has an eligibility worksheet with receipt and asset thresholds and category exclusions; failing it means the full Form 1023, a materially longer and more expensive path. This should be confirmed before the calendar is rebuilt on top of it.
2. **Is the pilot genuinely gated on exemption at all?** Incorporation → EIN → 1023-EZ → hosting is a clean chain, but only the last link was ever load-bearing for the *pilot*, and §III argues that link is optional. A six-week staff pilot of a locally installed, offline-capable, Green-lane-only tool may be independent of tax-exempt status. It is certainly gated on **Gate 3 district approval**, which is a separate instrument already drafted and ready. If these are two independent tracks that got braided together, the typist has a much freer hand than the calendar suggests.
3. **Does ADR-006's pre-release checkpoint reach a website?** [ADR-006](../adr/ADR-006-public-name-honest-ink.md) §4 requires counsel and formal screening to confirm the name "before any public distribution." A site is not distribution of software, but it is unmistakably public use of the name. See §III.
4. **Who are the directors, and where are the bylaws?** `GOVERNANCE.md` governs the *project* — product owner, council, maintainers, decision records. Nothing in this repository describes the *corporation's* governance. Maryland will want directors; the exemption filing will ask about organizational structure. This is the one entity-track item the coordination plan does not already name, and it is cheap to settle now and awkward to settle under time pressure.

## III. The website: the dependency was partly self-imposed

`honest-ink.org` is purchased. `tools/SiteGenerator` already builds the whole site — README, Governance, Contributing, Security, Notices, the Deterministic Press specification, and the samples gallery carrying the presses' own portraits — and its determinism is CI-tested. Nothing about publishing that requires an EIN or a recognized exemption.

**The 501(c)(3) promotional pricing and the host decision are orthogonal.** Nonprofit programs discount *paid infrastructure* — compute, seat licenses, ad grants. A generated static site needs none of it and is free at this scale regardless of status; a discount on free is nothing. The decision also forecloses nothing: a static site is a directory of files, regenerated deterministically from this repository, and changing hosts is a DNS record. Nonprofit pricing becomes genuinely relevant only when something dynamic is needed — donation processing, a symbol-commons upload endpoint, forms — and that conversation is better had against real requirements than hypothetical ones.

**Decision recorded: choose a static host now; do not buy a paid tier or sign an annual contract.** The recommendation is GitHub Pages, on grounds specific to this repository rather than general preference — the repo, the CI, and the generator are already here, so publishing is one workflow step with no new vendor, account, or secret, and custom domain plus HTTPS are included. Keep the site generated from the repository so the host stays interchangeable.

**But standing up the host and switching DNS are two decisions, not one.** The infrastructure carries no name risk and can proceed immediately. Pointing `honest-ink.org` at it is public use of the Honest Ink name and touches ADR-006's checkpoint (§II question 3). The delay has bought exactly the time to get counsel's read first, which is the cheapest available ordering. The cost of getting this wrong is not the HTML — that regenerates — it is that every educator, council member, and district contact who learns the name makes a later rename more painful, and ADR-006's fallback order (Schoolhouse Foundry, then Inkwright) exists precisely because that possibility is real.

What the wait is currently costing, recorded plainly because it lands on the very things the delay is meant to buy: the educator who accepted has nothing to look at; second-maintainer recruitment (R2-13, the only *standing* council finding) is much harder without a public face, and "a well-lit house, not a rescue" is a poor pitch when there is no house to see.

**Done 29 Aug 2026:** the deploy workflow exists at `.github/workflows/site.yml`. It is `workflow_dispatch` only — no push trigger, no schedule — so nothing publishes until a human presses the button. Its build step is proven locally (seven pages: index, governance, contributing, security, notices, the Deterministic Press spec, and the samples gallery); its *deploy* step has never run, because Pages is not enabled, and that first run is its own first test. Three human acts remain, in increasing order of loudness, and none belongs to a keyboard: **(1)** enable Pages with source = GitHub Actions; **(2)** run the workflow; **(3)** point `honest-ink.org` at it. Note that the split is not as clean as it looks — the name appears in the page content of three of the seven generated pages, so even step 2 to a default `*.github.io` URL is public use of the name, not merely a technical rehearsal. Only step 3 is *loud*, but step 2 is not silent.

### Where the project should live: an organization, not a second account

The repository currently sits under the director's personal account (`Spacejunk-io`), which is also the account the `gh` CLI is authenticated as. The question of whether to move it is real, and the answer is **a GitHub Organization, and not a second user account** — the two are frequently confused and only one of them is right.

A second *personal* account for the nonprofit is the anti-pattern: it means a shared login handed to the second maintainer when one is seated, constant account switching, and a `gh` CLI that stops being seamless. An **organization** solves the actual problems without any of that, because an org is *owned by* personal accounts rather than being one. The director's account stays the login, `gh` keeps working unchanged, pushes stay authenticated as before — and:

- **It is the direct cure for R2-13.** An org takes multiple owners. A repository under a personal account is a bus factor expressed as infrastructure; the standing council finding is currently true of the hosting as well as the maintainership.
- **It matches the stewardship question already before counsel** (§II, question 4). If Honest Ink, Inc. becomes copyright steward, code living under an individual's personal account contradicts that on its face.
- **It reads correctly.** An organization looks like an organization to an educator council and a school district; a personal account looks like a side project.

Two cautions. First, naming the org is the same ADR-006 decision as everything else in this section — though creating it also *reserves the handle*, which is a legitimate protective move, and an org can be created without moving the repository into it yet. Second, a repository transfer redirects old URLs and preserves history, issues, and stars, but **secrets and Pages settings do not travel** — they are reconfigured on the far side, and CI should be re-run to prove it before anything depends on it.

Creating the organization, transferring the repository, enabling Pages, and accepting any terms are all the typist's acts. Git *authorship* is a separate matter from hosting and needs no change: commits are authored by a person, and that is correct.

**Done 29 Aug 2026.** The organization is **`honest-ink-inc`**, and the reason is worth stating precisely, because "the name was taken" would mislead counsel. Two handles were unavailable, for two unrelated reasons: **`honest-ink-edu` is the typist's own personal account** — self-inflicted, not evidence of anything — and **`honest-ink` is held by a party who is not the typist and whose repository does not appear to be an educational company.** So the namespace is not contested by anyone in this field. Since relatedness of goods and services is central to trademark analysis, an unrelated holder weakens a conflict concern rather than strengthening it — but that judgment is counsel's, and the fact should reach them as stated here rather than as a bare "taken." The repository now lives at `honest-ink-inc/open-classroom-foundry`; the old URL redirects, history, and the git remote is updated. `SiteBuilder.RepositoryBase` was repointed so the generated site links to the new home rather than the director's personal account. **The secrets warning above proved unfounded on inspection** — the only secret referenced anywhere is the auto-provided `GITHUB_TOKEN`, so nothing needed migrating.

Three things remain open on this move, and the first two are the typist's:

1. **`Spacejunk-io` is an org *member*, not an *owner*.** Verified after the transfer. Until that changes, the organization has a single owner and **R2-13 is not cured — it is merely relocated.** An org with one owner is the same bus factor wearing a different hat, which was the whole reason for preferring an org to a second account.
2. ~~**The repository is PRIVATE.** This was discovered during the transfer, not assumed.~~ *Resolved 30 Aug 2026: the repository is **public**.* Before the flip, the whole history was scanned rather than the working tree alone, because publishing publishes every commit ever made: **no credential-shaped string has ever been committed** (no private keys, `gh*_`/`sk-`/`AKIA` tokens, connection strings, or hardcoded passwords), the only email addresses in any commit are the deliberately-public contacts, the `example.org` fixtures, and commit authorship, and exactly three files have ever been deleted — `FACILITATOR-KEY.md` and the two retired `Form1` placeholders. So the only sensitive object in history is the seeded-error key, **which had already been public in this same repository for roughly 8½ hours on 29 Aug** (14:13–~23:55 UTC): re-publishing exposed nothing new, and the remedy — regenerate the study seeds — was owed either way. The original finding, for the record: It has two consequences nobody had written down: **GitHub Pages cannot publish from a private repository on a free plan**, so `.github/workflows/site.yml` will fail on its first run until either the repository is made public or the organization pays; and the hardening checklist's claim that "source archive is the GitHub repository" is not currently true for anyone outside the org. GPL-3.0-or-later does not require publishing source to the world — only to those you distribute binaries to — so nothing is out of compliance today. But the project's stated posture and its actual visibility disagree, and that gap should close deliberately rather than by accident.
3. The `-inc` in the handle asserts corporate status that Maryland has not yet granted. Defensible — incorporation is filed and pending — but worth a conscious decision rather than a default, in a project whose whole ethos is not asserting what is not yet established.

## IV. What the delay unfreezes, in order

The pilot freeze argued for on 29 August is void — it was a five-day argument, not a principle. The window is now weeks, and the following is the honest order. Per the amended divination rite, each item names its source.

**1. The doorless modules — nine of them.** *(Source: atlas entries 11, 31, 41, 51, 61, 101, 141, 148, 201, 202, and the composition the Press Room affords.)* Board to Brief, Access Remix, Directions Duet, Scaffold Smith, Talk Moves Studio, Lesson Loom, Exit Lens, Rubric Relay, and Source Lens all exist as tested, deterministic builders in `Foundry.Modules.BuiltIn`. Only All Aboard has a form; the sole app reference to the module namespace is `AllAboardForm.cs`. A teacher cannot reach any of the others from the running executable, though the atlas build order lists five of them as shipped 0.2 and 0.3 features. **This is the largest gap in the product.** The Press Room is already fully generic over typed parameters (`PressDefinition` → generated UI → Gate B → print/export/library/tile), so the work is generalizing that machinery into a shared module door, not writing nine bespoke forms. Five of these are pure-Green atlas entries that would otherwise be "enacted" by building something new when they are already built.

*Struck 30 Aug 2026, with the inventory corrected rather than repeated.* The count was ten doors, not nine: Family Bridge was present in the source list behind the paragraph but absent from its prose. `ModuleStudioForm` now generates all ten doors and eleven modes from one typed catalog. Eight modes have synthetic Green starters; Access accepts only an exact current in-memory classroom-support approval; Exit Lens and Rubric Relay are visible, speak the written district gate, and have no build delegate. A door exists for every builder, while protected territory remains unavailable.

**2. A real second UI language.** *(Source: the composition the pseudo-locale affords.)* `UiLocaleMode` has exactly two members, Neutral and Pseudo; the chrome is English-only. For a product whose flagship differentiators are Directions Duet, Glossary Garden, and Family Bridge, an English-only shell around bilingual content is an asymmetry the multilingual seat will notice at once. The pseudo-locale already proved every string is externalized — 81 of them in one catalog — and `UiStrings`' own comment says real translations arrive as additional catalogs and the mechanism is ready. It is ready and unscheduled. **The typography and the translations themselves answer to the multilingual seat; that blessing is not ours to give from a keyboard.**

*Forge-owned preparation exhausted 30 Aug 2026; the item is deliberately not struck.* The real inventory is now 984 stable chrome ids, including dynamic press and module labels. The neutral packet exports deterministically; the strict loader checks exact keys, neutral digest, placeholders, mnemonics, canonical locale, direction, provenance, and review assertion; production additionally requires an exact-file SHA-256 compiled into the build. The allowlist is empty. No authorized seat-supplied catalog was available through the user-provided sources, and no protected material was inspected. No actual second language exists until the multilingual seat supplies and reviews one.

**3. The minimum-hardware floor, correctly framed.** *(Source: atlas, "One engine, not 227 codebases" — the hardware covenant.)* **1366×768 is a floor that must not break, not a design target.** Honest Ink is a teacher's tool on a teacher's machine, and 1920×1080 is the sensible design surface; most student and Chromebook devices are lower, but they are not this product's target. The test at the floor asserts that nothing is unreachable, clipped, or off-screen — not that the layout is optimized there. That honors the covenant correctly: the donated machine still works, the ordinary machine gets the good experience. Nothing in the repository currently references that resolution at all.

*Struck 30 Aug 2026.* `tests/Accessibility/MinimumHardwareFloorTests.cs` lays out every shipped surface at the 1366×768 working area in neutral 100% and stretched pseudo-locale 125% configurations, walks every built-in node editor type and every vector primitive layout, and requires each control and speaking status to remain reachable either directly or through an owning scroll container. A negative control proves the assertion catches an off-screen non-scrollable control. The ordinary design surface remains larger.

**4. Then the atlas, council-first.** *(Source: atlas.)* Seventy-four pure-Green entries remain unenacted of the 111 that carry no specialist gate, no Amber lane, and no Restricted escalation. But with a council seated and a pilot ahead, the order is educators first and register second: build what they ask for, not what the numbering happens to offer next.

*Prepared, not struck, 30 Aug 2026.* The [needs-first priority-session packet](../council/atlas-priority-session.md) is ready: need capture precedes candidate names, absent seats create holds, feasibility is separate, and product-owner disposition remains required. No session has run, no rank has been selected, and no rehearsal persona or keyboard has been counted as council evidence.

**5. Seat the second maintainer.** *(Source: R2-13, standing; implementation plan Definition of Done.)* The Smith noted dryly that they cannot review their own succession. This window is the best chance the project will get, and items 1–3 make the house worth joining.

## V. Debts and hygiene the window should also pay

- **An upgrade path exists nowhere.** The only reference is a single line in the plan about managed deployments installing alongside. During a six-week pilot the project *will* ship fixes — that is what a pilot is for — and a longer runway means more versions before it even opens. How a pilot teacher moves from 0.7.0-alpha to the next build without losing their library is undefined. The reversibility guarantee covers `.ocfproj` projects across schema versions; it does not cover the executable.

  *Engineering debt struck 30 Aug 2026; deployment remains human-gated.* Proposed ADR-007, the advisory runbook, strict operator-plan schema, and `Foundry.Tools.ProjectUpgradeHost` now define review and prepare operations for an exact side-by-side, copy-on-write candidate library. The host holds each source open read-only, binds roots, versions, schema, relative paths and SHA-256, validates the entire package before and after, flushes and atomically promotes only completed copies, cleans bounded partial state, and leaves the prior library unchanged for rollback. It never versions, downloads, signs, installs, uninstalls, distributes, launches, or replaces an application. Ratification and every real device action remain with the product owner, typist, and District IT.
- ~~**The date is baked into sixteen places**, including the hardening checklist's "scheduled 8 Sep–16 Oct," the coordination plan's six-week calendar, and the default press seed `20260908`. Stale dates in governing documents are how documents stop being trusted. The seed is harmless and deterministic either way; the prose is not.~~

  *Swept 29 Aug 2026.* The live documents now carry the six-week **structure** with no absolute anchor: the coordination plan's calendar runs "Opening week", "Week 1 + 1", … so every dependency and every "Needs" column survives intact and only the anchor is missing. Hardening checklist, educator council status, and the district packet say the date is pending and point here. **Three things were deliberately NOT changed:** the seeded defaults (`20260908` in `PressRoom.Seed()`, the SampleGenerator, and the word-search test) are functional constants whose change would alter every seeded sample's bytes for no benefit; `LockedFieldValidatorTests`' "Friday, October 16" is an unrelated fixture; and the menu handovers one through four keep their dates untouched, because a dated record of what was true when written is not stale — it is the record. The §V pointer in the [enactment handover](2026-08-29-forge-enactment-handover.md) is struck and redirected here rather than rewritten.

  The sweep's real find was not tidiness. **Letter 1 in the unsent [council correspondence](../council/correspondence/2026-09-invitation-updates.md) asked the accepting educator for "your availability for the week of 8 September"** — the exact week that educator had written to say did not fit them. The three letters now state the move honestly, ask for the *shape* of availability rather than dates, drop the lapsed 4 September reply deadline, and correct a stale "361 automated tests" to 657. They remain drafts and remain the typist's words: **read them before sending, and do not re-insert any date until Maryland has answered and the seats have confirmed.**
- **`tests/Accessibility` builds but contains no tests** — `dotnet test` reports "No test is available" for it. The real coverage lives in `tests/UiAutomation`, so nothing is unguarded, but a suite named Accessibility holding nothing reads as coverage that is not there. Either give it the tests its name promises or fold it into the suite that has them.

  *Struck 30 Aug 2026.* The suite now contains three executable minimum-floor tests and passed 3/3 in both local closing runs. The first hosted run then exposed a harness premise rather than a product failure: the runner's narrower desktop silently clamped the nominal floor form to 1044×728. That exact geometry and failure reproduced locally, while 1366×728 and 1071×728 passed. The harness now hosts each real framed Form inside an exact 1366×728 child-window canvas and asserts that geometry, so the floor no longer changes with the machine running the test.
- **Under-load flakes, sighted 29 Aug 2026 and recorded as sightings, not diagnoses.** Three failures across roughly six full-solution runs, each passing in isolation immediately after, each in a different test:
  - `Integration.ImageNormalizerTests.A_burned_region_destroys_the_pixels_beneath_it` — **message lost to the output filter**, which is the very trap the [enactment handover](2026-08-29-forge-enactment-handover.md) §III warns about, sprung a second way.
  - `UiAutomation.HeadedUiaWalkTests.Part3_Steps9to12_move_edit_and_approve_operate_through_uia_patterns` — failed at 22 s, passed alone in 685 ms.
  - `UiAutomation.HeadedUiaWalkTests.PilotDay_dress_rehearsal_cold_start_to_reopened_booklet_and_low_ink_over_real_uia` — **message captured this time**: `System.TimeoutException : The UI Automation element did not appear in time`, thrown from `WaitFor` via `AwaitStatus` ([PilotRehearsal.cs:39](../../tests/UiAutomation/HeadedUiaWalkTests.PilotRehearsal.cs)).

  The two headed failures share a class — a UIA element wait timing out while the machine is contended — and differ in which test draws the short straw, which is what a load-sensitive wait looks like rather than a defect in any one test. **No cause is claimed and no fix was attempted**, because raising a timeout treats the symptom: the last flake in this repository was not a test problem at all but a real seam, and the cure was the `libraryPicker` ctor seam, not a better wait (traceability finding 9). Whoever takes this should ask first whether the rehearsal's waits should be seams too. CI has not reproduced any of the three.

  *Measured 30 Aug 2026.* The image sighting is now diagnosed: concurrent first use of the .NET 10 GDI+ encoder inventory could observe its published array before initialization completed, producing `ArgumentNullException (encoder)`. It reproduced in 2 of 20 fresh-process races before the fix and 0 of 50 after a thread-safe explicit-codec cache replaced format-only `Image.Save` calls in production and fixtures. The two historical headed UIA timeouts remain sightings, not diagnoses; neither reproduced in the two 884-test closing runs or in CI. New harness findings discovered this day were separate and deterministic—unsafe root inheritance, malformed-mode fallthrough, and an unconstrained direct export path—and were fixed with disposable-root and path-confinement tests.
- **The transfer broke the secret-scan job, and it went unnoticed for four commits.** `gitleaks-action` requires a paid licence for **organization**-owned repositories, so from the move commit onward every run failed with *"missing gitleaks license."* `build-and-test` — tests, format gate, coverage, determinism — passed throughout; only the scan job was red. **It went unnoticed because `gh run watch --exit-status` was trusted for a verdict instead of the run's own conclusion being read.** The lesson is the session's own, sprung a third way: *read the measurement, do not infer it from a wrapper's exit code.* *Resolved 30 Aug 2026.* Going public did **not** restore the free tier — measured, not assumed: the licence attaches to the organization regardless of visibility. The Action wrapper is gone and the **gitleaks CLI** runs directly, pinned to v8.30.1 with its published checksum verified *before* the binary executes (a security gate that ran an unverified download would be its own counterexample). The gate was then checked for being real rather than merely green: the runner log reads `gitleaks_8.30.1_linux_x64.tar.gz: OK` and **`67 commits scanned`**, which independently corroborates the pre-publication history scan. A paid dependency no longer stands in front of a GPL project's security gate.
- **CI actions are on deprecated Node 20.** `actions/checkout@v4` and `gitleaks/gitleaks-action@v2` are being force-migrated to Node 24 by the runner. Not urgent, worth a version bump next time the workflow is touched.

  *Struck 30 Aug 2026.* Every third-party workflow action is exact-commit pinned to its Node 24 generation. The licensed gitleaks wrapper remains absent; the checksum-verified pinned CLI continues to scan full history.

## VI. What has not changed

The standing laws of all four menus remain binding, verbatim and without exception:

- Presses take parameters, never prose. Nothing renders, prints, exports, or persists without a typed `ApprovedArtifact`.
- Lanes escalate only; Amber never persists. Model self-ratings are not release evidence.
- The AAC/SLP seat and the district instrument are not waivable from the keyboard, and no additional time makes them so. **A longer runway is not a licence to build into the seats' territory while they are unseated** — it is the opposite, because there is now no schedule pressure to excuse it.
- No printing without approval. No auto-approving CLI.
- **Version bumps, tags, installs, signing, and distribution remain the typist's acts.** The forge builds; the typist ships.
- Every human gate in the [coordination plan](../pilots/human-gates-coordination-plan.md) survives the schedule change intact. Only the dates moved.

## VII. Resuming

The closing rites in the [enactment handover](2026-08-29-forge-enactment-handover.md) §III are unchanged and apply to every item: Release build with warnings as errors; `dotnet format` fix then `--verify-no-changes` at exit 0; the full suite plus at least one stability re-run with failure names surviving the filter; the SampleGenerator twice with every byte hash-compared if presses changed; strike with a dated note; commit and push. The determinism gate now runs with **no exclusions** — the `.ocfproj` writer was made a pure function of its inputs on 29 August and the last standing exception was deleted with it.

The typist says **"Proceed with …"** and names an item; that adopts it. Items completed here are struck with dated notes, never deleted.

One caution earned the same day this was written, and it outranks the craft notes: **a defect note records a hypothesis, not a finding.** The `.ocfproj` nondeterminism was recorded for a day as a timestamp bug; it was measured before it was fixed and turned out to be a random `Guid`, with the timestamps a real second cause that a fast measurement had nearly hidden. Measure before fixing, and check whether the cause you recorded is the cause you have. The same discipline applies to a calendar: this document records what is known on 29 August 2026 and no date beyond what Maryland has actually answered.
