# Seven-recipe bounded technical disposition

**6 September 2026. Engineering assessment, not final admission or human ratification.**
This completes classification of the fixed captured corpus. Final current-source
and hosted closing are recorded separately in the
[bounded closeout](2026-09-06-bounded-engineering-closeout.md).
The [working evidence record](seven-recipe-compatibility.md) retains actual
commands, failed instruments, raw reports, complete exceptions and exact pins.

## Authority and compatibility meaning

The [exact authorization](2026-09-05-recipe-replacement-authorization.md) permits
engine `0.8.0-alpha`, seven replacement recipes `0.2.0` and evaluations `0.2`.
Outgoing `0.1.0` recipes, evaluations `0.1`, manifest fingerprints and literal
minimum engine `0.7.0-alpha` remain available by exact selection. C1 is
`5cae09dcb40628265d51912aea98304557abfda6`; record-only C2 is
`94f128cddd5cdbd00a6f7097b470e4defdebaa47`, with C1 its sole parent.
Their ancestry and forty original sample rows must not be rewritten.

| Replacement identity | Output label present in the held candidate |
|---|---|
| `lesson-loom@0.2.0` | `schema.lesson-loom.v1` |
| `source-lens@0.2.0` | `schema.source-lens.v1` |
| `board-to-brief@0.2.0` | `schema.board-to-brief.v1` |
| `press.charts@0.2.0` | `schema.deterministic-press.v1` |
| `press.learner-held@0.2.0` | `schema.deterministic-press.v1` |
| `press.calibration@0.2.0` | `schema.deterministic-press.v1` |
| `press.flashcards@0.2.0` | `schema.deterministic-press.v1` |

The narrow technical proposal is to retain these labels for unchanged typed
serialization vocabulary, units and field/audience meanings, while assigning
the changed acceptance, arithmetic, association, layout and notice behavior to
the exact replacement tuples. Labels are already present in candidate code;
their final admission is not granted by this assessment. These deterministic
paths resolve C# ArtifactDocument contracts and validators, not a separate formal
JSON Schema for each manifest label. The cloud schema registry proves no such
deterministic-path claim. Neither label retention nor unchanged default bytes
means universal semantic or behavioral equivalence.

Nine named grammar/package sources have the same raw lengths and SHA-256 values
as both retained C1 source inventories: ArtifactDocument, VectorGraphics,
DocumentValidator, LockedFields, RecipeManifest, RecipeContractFingerprint,
ProjectPersistence, JsonAssetCatalog and OcfprojPackageValidator. Each inventory
is 80,405 bytes / 466 rows, SHA-256
`65B286FE051FE37590FBF8D465AE82FFEBF1D504BF71B34A11C3475C47ADDA3D`.
This was a file-pin comparison, not an independent source-to-binary proof or
exhaustive proof of every recipe predicate.

## Completed fixed-corpus comparison

The authoritative boundary report covers **506 C1 and 1,012 candidate captures**,
1,012 C1/candidate relations, 759 A/B relations and 506 same-engine
outgoing/replacement relations. Both independently produced 521,214,404-byte
reports are directly byte-equal, SHA-256
`2F5B2A3F8DA50AFA3CFDABBB2750204C682F9A86046D304BA410A876C44F81B0`.
The comparer completed with native zero, 104,324 consumed-file pins and zero
final rehash mismatches. Its receipt SHA-256 is
`57F76AE228FBF190BA9673BF9D57D5976E09E0BBC3B0B4DFD669B1814E6E5F3F`.

The independent full-report readback also directly compared and freshly pinned
8,176 equal document/returned-output pairs. It found no additional actionable
mismatch in this bounded scope. Its retained `AUDIT.md` is 12,095 bytes,
SHA-256 `758257929804682A166E7081A17CD548160D2E82CBA0E9EF0723129AEB134717`.
All relation input-token records match. Absence is never equality.

| Family | Measured replacement differences and limits |
|---|---|
| Lesson Loom | In the separate 43-case semantic corpus, three formerly approved counterexamples now refuse; the old total-overflow exception becomes a blocked document with sum 4,294,967,341. Missing/whitespace learning evidence refuses. Existing document bytes remain unchanged. |
| SourceLens | Fourteen formerly approved association/ambiguity counterexamples now refuse. Title/card/excerpt/citation association and exactly seven metadata rows are checked; allowed notes, row reordering and intact-section movement retain their observed results. This does not authenticate sources or rights. |
| Board-to-Brief | Three former approvals that lost visible locked facts now refuse with `Outside teacher-only notes:`. Complete-deletion refusals remain; permitted movement and unlocked prose edits retain their measured results. No universal fact-association claim follows. |
| Charts | Sixteen zero/maximum catalog observations change: zero rectangles are omitted but labels remain; maximum geometry no longer has the observed negative height. Six of ten scalar observations change at the three largest tested values: grid step becomes 500,000,000. The other four scalar observations do not change. Sixteen old blocks become synthetic approvals; these are explicit acceptance changes. |
| Learner-held | Portfolio's 16 and strategy's 100 builder-document observations remain equal. Goal Post has 80 equal, 24 changed and 24 old-only builder-document observations. Twenty-four cases paginate one sheet to two without shrinking prompt text; 24 invalid/no-capacity margin cases newly refuse with ArgumentException. Eight of those refusals remove formerly approved outputs. |
| Calibration | All 48 surviving catalog observations change layout; four old NaN blocked documents become early refusals. Instructions wrap and the instrument group moves without ruler rescaling. Low ink retains the solid density endpoint. Eight Letter/A4, margin 5/25, ordinary/low-ink PDF inspections fit the instructions; font warnings and the untagged PDF limitation remain. This is not physical scale, duplex, browser or AT acceptance. |
| Flashcards | All 128 builder-document observations and returned HTML/PDF bytes remain equal. Twenty-four catalog observations gain the existing `flashcard.overflow` Warning at the same 40-character heuristic, preserving full text and `RequiresAcknowledgement=false`. This is warning propagation, not repaired typography or a new blocking rule. |

The separate semantic corpus observes 39 approvals / 3 blocks / 1 builder
exception on C1 and current historical routes, versus 19 approvals / 24 blocks /
0 builder exceptions on replacement routes. All 42 previously existing builder
documents and 42 previously existing acknowledged documents are byte-equal.
The newly blocked overflow document is not an approved output. These capture
outcomes are synthetic; declared evaluation suites have their separate tests.

Boundary outcome census per version is historical **436 approvals / 36 blocks /
24 builder exceptions / 10 scalar observations**, replacement **444 / 0 / 52 / 10**.
All 76 builder exceptions are ArgumentException; twenty separate historical
fingerprint-serialization exceptions are not additional builder crashes.

**Export availability is not silently preserved.** Each version has forty
unsupported native-PDF observations: 32 flashcard Unicode observations refuse
`Character U+D83D has no WinAnsi encoding; this document needs the HTML print path.`
and eight strategy observations refuse U+6F22 with the same complete suffix.
Standalone SVG has 416 historical versus 464 replacement unsupported observations.
The additional 48 are Goal Post's newly multi-sheet documents: standalone SVG
requires exactly one vector sheet and no other nodes; print HTML is the
multi-sheet route. Separately, early Goal Post refusals remove sixteen previously
returned SVGs. These are distinct reasons for absence. Native PDF observations
do not expand the historical manifest's declared exports.

The 48-C1/96-candidate default comparison and full boundary comparison preserve
historical document/PDF/returned-SVG bytes in their inspected corpus. **HTML is
not C1-byte-equal:** all 1,744 historical boundary HTML pairs retain an exact
132-byte image-CSS insertion at character 864. The image-free source strings
do not match those selectors, but no browser-equivalence proof was obtained.
The difference remains raw and unexempted. A/B document and returned-output
bytes match; raw identities, clocks, stacks, instrumentation and changed
decisions are not normalized into equality.

The optional derivative summary is not the authoritative comparison. Its first
preflight failed on a raw/canonical-path comparison. The narrow correction
passed 38/38 focused controls twice, preserving its initial test-vector failure.
Root voluntarily stopped the subsequent whole summary at 16:55 UTC to bound
the closeout: native -1, outer 1, source stable, partial output retained, no
summary completion. No comparer or producer was rerun or weakened.

## Persistence and shared-engine classification

Eighteen genuine C1 fixture variants cover nine definitions, each contextless
and exact-context. The [fixture record](../../tests/Integration/Fixtures/upgrade/c1-seven-recipes/README.md)
binds original bytes and provenance. Thirty-six current-loader, exact-selection,
source-preservation and late-rollback controls passed twice. Contextless
enrichment adds context while preserving original members/timestamps;
exact-context copies preserve whole ZIP bytes. Both loader entry points share
a validator, so they are not independent implementations. No real library was
migrated, and hash-less package selectors do not authenticate origin.

Seventeen current-writer tests and eighteen twice-run library-door
edited-reopen/resave controls cover the declared tuples. Edits invalidate
acknowledgement; deliberate resave uses engine-owned portable identity, not
fabricated continuation provenance. A later reopen needs fresh preflight/review.
All forty candidate sample files match across repeated generation. The strict
sample guard retains thirty-nine exact original files and only the measured
writer-stamp-only ZIP relation for the canonical task-strip package; all forty
original manifest rows remain unchanged.

Shared authority, capture/transcript mutation, exact asset/revision binding,
Restricted refusal, inert Amber authorization, strict cloud validation,
print/save failure precedence and fixture lifecycle controls are engine or
test-infrastructure hardening, not a reason to recreate unsafe bypasses in
historical recipes. The later Press authoring Green confirmation is likewise
an upstream UI classification repair; unchanged builder outputs do not prove
that boundary. It receives its own fresh verification in the closeout record.
The captured producer DLLs remain bound to their archived build; later UI tests
do not retroactively change those captures.

## Explicit admission and human holds

Schema 1 remains closed, hash-less and release-blocked. ADR-010's schema-2 writer
and ADR-007's managed rollout/retention disposition remain unratified. The
booklet-guide LONG-to-SHORT change predates this stack at
`576e19148862df0540fbdf6235e7896832674d7e`, already on main. Its separate C1
gap remains held; this assessment neither authorizes an eighth replacement nor
restores or waives that change.

CC BY-SA 4.0 remains a selected scope proposal, not an operative grant. Rights,
assent, consent/withdrawal/custody, appointments/quorum/recusal, real H0–H7,
AAC/SLP, curriculum, multilingual, accessibility, print and teacher-pilot evidence
remain separately required. No synthetic committee, corpus equality or successful
test enacts them. This technical assessment grants no merge, publication, tag,
signing, installation, distribution or release authority. **Note, 6 September 2026:** that remains exactly true — this assessment granted none. The merge that later landed this work on `main` drew its authority from a separate session-specific typist authorization, recorded in [the authorization record](2026-09-06-stack-integration-authorization.md), and conferred no recipe admission. Publication, tagging, signing, installation, distribution and release authority remain ungranted.
