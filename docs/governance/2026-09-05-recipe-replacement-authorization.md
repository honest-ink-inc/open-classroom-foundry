# Exact authorization to prepare seven versioned replacements

**Authority:** product owner / typist, in this Codex task.
**Record date:** 5 September 2026 (America/New_York).
**Response observation:** `2026-09-06T02:22:45Z`; this is the recording clock
observation, not a separately supplied message timestamp.
**Status:** preparation and implementation AUTHORIZED; final admission NOT GRANTED.

## Exact question and response

The assistant asked:

> For the seven held fixes—Lesson Loom, charts, learner-held pages, calibration, flashcards, SourceLens, and Board-to-Brief—do you authorize preparing engine 0.8.0-alpha, replacement recipes 0.2.0, and evaluation suites 0.2, including the calibration instruction-layout repair? Existing 0.1.0 contracts would remain available unchanged. Existing output-schema labels would be retained only where equivalence is proven; schema 1's release stop, real-review requirements, and publication holds remain. This authorizes compatibility implementation, not final admission, tagging, or release.

The typist responded:

> Authorized.

The response authorizes the exact bounded question above. It does not convert
the entire advisory proposal, an unmentioned alternative, a future proof result,
or another person's authority into a decision.

## Exact implementation choices

| Recipe ID | Outgoing version retained | Replacement version authorized | Outgoing evaluation retained | Replacement evaluation authorized | Existing output schema, conditional on equivalence proof |
|---|---|---|---|---|---|
| `lesson-loom` | `0.1.0` | `0.2.0` | `0.1` | `0.2` | `schema.lesson-loom.v1` |
| `press.charts` | `0.1.0` | `0.2.0` | `0.1` | `0.2` | `schema.deterministic-press.v1` |
| `press.learner-held` | `0.1.0` | `0.2.0` | `0.1` | `0.2` | `schema.deterministic-press.v1` |
| `press.calibration` | `0.1.0` | `0.2.0` | `0.1` | `0.2` | `schema.deterministic-press.v1` |
| `press.flashcards` | `0.1.0` | `0.2.0` | `0.1` | `0.2` | `schema.deterministic-press.v1` |
| `source-lens` | `0.1.0` | `0.2.0` | `0.1` | `0.2` | `schema.source-lens.v1` |
| `board-to-brief` | `0.1.0` | `0.2.0` | `0.1` | `0.2` | `schema.board-to-brief.v1` |

The authorized candidate engine is **`0.8.0-alpha`**. Existing first-admission
manifest values, including their `0.7.0-alpha` minimum engine, remain frozen.
The governing outgoing freeze is C1
`5cae09dcb40628265d51912aea98304557abfda6`, retained with its sole-parent
record-only C2 `94f128cddd5cdbd00a6f7097b470e4defdebaa47` and original samples.
The scoped unadmitted production fixes inspected for this proposal are at
`db40e9b673bceb65cd60d0e0fb88653b02ce66c6`; no production source changes are
included in the contemporaneous asynchronous-fixture repair.

The calibration replacement may repair instruction layout. The original text
meaning, exact ruler scale and outgoing contract remain protected by the
question's preservation requirement. Mechanical layout proof is not physical
printer, accessibility, AAC/SLP, multilingual or classroom acceptance.

## Required work and effects not granted

Authorization starts implementation; it does not assert that side-by-side
execution, exact selection, schema equivalence, versioned evaluations,
backward-open, source preservation, rollback or final candidate verification
already exist. Those are still required by the
[ratified compatibility record](../adr/recipe-identity-disposition-packet.md)
and ADR-001/003. A changed schema meaning requires another exact schema
decision, not an in-place `v1` rewrite. The seven rows are a scoped direct-change
inventory, not proof that all shared consumers are unaffected.

Preserving an outgoing manifest while substituting new executable behavior is
not retention. Old and new builders, editors, validators, evaluation routes
and required render behavior must be resolved exactly. A single global engine
bump must not alter outgoing manifest hashes or remove the historical
`0.7.0-alpha` snapshot reader. Shared transcript and failure semantics need
their own explicit compatibility classification, not automatic version
assignments to every downstream consumer.

Schema 1 stays closed and hash-less, with its release stop. ADR-010 is not
ratified; `0.8.0-alpha` here does not select its separately deferred first
schema-2 writer. ADR-007 and every real district/retention plan remain separate.
No existing tag is moved; no tag, package, release, signing, installation,
distribution, site dispatch, filing or correspondence is authorized here.
Actual rights, consent, withdrawal, custody, appointments, quorum/recusal and
H0–H7 records remain separate. The selected CC BY-SA 4.0 scope proposal is not
made operative by a recipe-version decision.

The [current handover](../handover/2026-09-05-pr-stack-resumption.md) tracks
implementation. The ignored proposal at
`out/pr-stack-resumption/compatibility-decision-proposal.md`, SHA-256
`982698AC8DF949DCF6F7A4F41B584CC5363703745464B094161E4B983CD82DF8`,
remains supporting analysis rather than an independently ratified document.
