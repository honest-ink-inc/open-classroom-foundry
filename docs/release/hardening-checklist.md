# 1.0 hardening checklist

**Date opened:** 29 August 2026 · **Engine version:** 0.7.0-alpha · Maps implementation plan Release 1.0 to evidence. "Done" means the bounded artifact exists and its stated machine checks have run; it never closes named human, district, protected-seat, or exact-release evidence. **0.7.0-alpha is not 1.0**: every open human row below is why.

| Item | Status | Notes |
|---|---|---|
| Signed package and checksum | **Partial** | `tools/publish.ps1` is constrained to clean-commit, Release-only **unsigned pre-sign** output: locked `win-x64` restore from explicit `NuGet.config`, publish with `--no-restore`, engine/product/dependency and SourceLink checks, PDB segregation, exact signing-role inventory, unique staging, cooperative lock, pre-sign hashes, and rollback/recovery reporting. `tools/finalize-signed-package.ps1` is a separate typist-invoked, non-signing verifier: exact clean signed tag/HEAD, authorized timestamped Authenticode on signable first-party files, byte-identical unchanged files, regenerated signed checksums, and final-zip hash. Their 11 contracts passed; neither script was executed. The current engine `0.7.0-alpha`, assembly `1.0.0.0`, and earlier unsigned tag do not identify one releasable source state, so packaging fails closed until the typist performs a coherent version/tag act. Consumers must also wait for successful script completion because an unlocked reader cannot observe three paths as one atomic generation. **Authenticode signing needs the district's certificate or Honest Ink, Inc.'s own OV certificate** (entity track: `docs/pilots/human-gates-coordination-plan.md`). Unsigned pre-sign inputs are not a release |
| Installer / Intune package | **Pending** | `.intunewin` wrapping via Microsoft Win32 Content Prep over the publish output; documented here, executed with district IT |
| Staged rollout / rollback | **Partial** | Git tags + immutable releases give rollback-by-version; the runtime kill switch exists (`DistrictPolicy.CloudInferenceEnabled=false` disables all egress; removing the policy file fails closed to Offline). Rollout staging is a deployment-time procedure with IT |
| Migration and compatibility tests | **Partial — preparation seam built** | Schema 1 save/reopen, one constructed legacy fixture and one package emitted by a prior-main build, hostile validation, and proposed ADR-007's side-by-side compatibility host have focused regression coverage. The current upgrade boundary passed **55/55**, the full-manifest fingerprint contract passed **5/5**, and the exact hardened Windows console-event exercise earlier passed **5/5** independent runs. Review and preparation require three confirmations: the exact plan, the executing build's sorted recipe ID/version inventory, and a separately length-framed inventory of every manifest-backed recipe's full declarative-contract fingerprint; conflicting contracts under one identity fail closed and the manifest-less portable-semantic-editor remains explicitly identity-only. A cross-runtime known-answer vector pins UTF-8, big-endian framing, enum values, and empty/ordered collections, while an independent reflection inventory guards the two current production recipe assemblies against catalog omission. The host carries cooperative cancellation through the console seam, fails closed on non-Windows hosts before plan processing, and keeps the published plan schema and parser in parity. The contract digest binds declared schema, validators, editor, renderer, exports, the ordered §6.6 warning/required-confirmation declaration, and evaluation version, but not executable bytes. The single `Warnings` list is deliberate: every entry becomes a fresh `RequiresAcknowledgement=true` warning in each review through both supported paths, so no duplicate confirmation-text field is inferred and acknowledgement is not approval. Neither recipe digest supplies the project `recipeHash` missing from strict schema 1. The three echoed digests prove review/prepare consistency, not external approval; no independently approved candidate contract-inventory artifact exists. Internalizing the formerly public storage upgrade types is an intentional application-implementation boundary and a source/binary break, not an external-SDK compatibility claim. The host remains a source-tree evaluation tool, not part of the application publish. No archived outgoing release package, frozen outgoing-version renderer route, schema-2 migration, authorized operator package, real closed-library plan, or managed-device version-bump/rollback proof exists; ADR ratification, packaging, deployment, installation, and rollback remain human work |
| Documentation, contribution, localization workflows | **Workflow and changed-tree neutral packet measured / human catalog pending** | README, GOVERNANCE, CONTRIBUTING, council charter, data inventory, and Gate C design exist. The deterministic catalog workflow is fail-closed. The current 1,078-string neutral packet was byte-identical twice at 164,925 bytes and SHA-256 `ADC01D0323AD6BBF766B1CC305A409165AD3D82118FF642A9B6576FE8CFF20C1`, bound to source digest `13cab42111782b29bc1d048b75d66a45a5a195210fe284072d3323b2d63d0e3e`; the focused catalog contract passed 28/28. The six entries beyond crop commit `96e37e9` are the shared source-inventory status, two module-specific confirmation labels, and three Family Bridge target-role labels. ADR-009's display-only StrandPlan correction changed the current packet bytes without changing that inventory count. Synthetic test-pinned LTR and RTL catalogs now traverse every shipped surface and mode through the reviewed-catalog runtime path, while a checked-in-schema/template/runtime guard binds their structural contract. This is mechanical test evidence only. No second language is claimed until the multilingual seat reviews an actual catalog and its exact bytes are pinned |
| SBOM, source archive, asset ledger | **Partial** | [CI run 33390149083](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33390149083) concluded `success` for crop commit `96e37e9` and retained build-evidence artifact `9757271221` with both unambiguously scoped CycloneDX 1.7 JSON SBOMs, both dependency inventories, and the vulnerability report. The repository/build SBOM measured 34 dependency components, all 34 with license evidence, and an exact restored-assets graph of 35 nodes with all 34 components root-reachable. The separate `Foundry.App.WinForms` `win-x64` project-dependency SBOM measured 18 NuGet components, all 18 with license evidence and root-reachable through the exact package-only projection of the restored application graph. It is explicitly labeled **not an exact release-artifact correspondence claim**: engine `0.7.0-alpha` differs from assembly `1.0.0.0`, and no promoted release artifact was examined. The public repository exposes the source history, but the exact immutable source archive and digest still belong to release evidence. Current-file integrity is verified for 13 pack assets plus teacher-shelf provenance. The application additionally pins the exact current manifest SHA-256 and refuses any unlisted file or directory in that shipped deployment root; this is a build-identity fence, not protected semantic or rights approval. Open export requires Unicode-substantive attribution and modification dispositions and emits source plus both dispositions as raw one-line values inside exactly one fenced literal `text` block. The current 13-asset catalog and open-license teacher submissions remain blocked because no protected reviewer text is fabricated. The durable schema still lacks license-text, commercial-use, and consent/release fields; a versioned topology decision, exact-release evidence, and rights review remain open |
| SPDX per-file notices (R2-10) | **Done** | Every admitted source file is headed; an architecture test enforces the inventory from this commit forward |
| CI gates (R2-11) | **Automated policy; implementation commit hosted green** | [CI run 33390149083](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33390149083) concluded `success` for commit `96e37e9854861cc2a6098a9fbf05add708732e03`: warnings-as-errors build, format, **1,361/1,361** tests, **92.8%** core-and-module line coverage, byte-identical 40-file Windows double run, 40-file Ubuntu double run, and 40/40 cross-platform comparison, full-history secret scan, separate dependency inventories/SBOMs, and direct/transitive vulnerability audit all passed. The Ubuntu result proves the bounded headless producer on that hosted Linux runtime, not a second UI. At that hosted commit, all 24 projects retained portable lock files; the application graph additionally retained 11 separate `win-x64` lock files so the release restore could not rewrite the portable graph. The current working tree has 27 solution projects, each with one portable lock file, and the current application graph has 12 separate `win-x64` lock files after admitting `Foundry.ReviewPreview`; that inventory is local source inspection until the next unchanged-tree close and hosted run. Repository source mapping clears inherited feeds and selects only NuGet.org. Workflow actions and repository tools are exact-version pinned. The [workflow-action runtime inventory](workflow-action-runtime-inventory.json) records the tag-to-commit and immutable-manifest review for all nine direct selectors plus the sole composite action's external child; its fail-closed unit contract rejects an unknown selector, a different exact pin or version comment, and every unreviewed local action. This is offline drift evidence, not a hosted execution claim. Gitleaks `8.30.1` uses a repository-pinned archive SHA-256 copied from the official checksum and independently matched against the archive before any extraction or execution; on this run it scanned 101 commits / about 8.74 MB and found no leaks. This is implementation-commit evidence, not branch-protection enforcement or a promoted-release claim |
| Static analysis / SARIF | **Automated policy; implementation commit hosted clean** | The separate read-only CodeQL workflow is pinned for the C# `security-extended` suite and the linked CLI, fails its own job on any SARIF result, forbids SARIF and database upload, and retains ordinary SARIF evidence for 30 days without changing repository security settings. [CodeQL run 33390149028](https://github.com/honest-ink-inc/open-classroom-foundry/actions/runs/33390149028) concluded `success` for commit `96e37e9854861cc2a6098a9fbf05add708732e03`, scanned 236/236 C# files, and retained artifact `9757301294`; direct inspection found one SARIF 2.1.0 CodeQL run and zero results. The 62,441-byte artifact's SHA-256 was `1b21b0c93300324df18218c4bcfa43e0419e9884583e395603341d1b32aefece`. Neither workflow configures or proves branch-protection enforcement. A release must record its own exact-run conclusions and retain that commit's SARIF artifact; current-source evidence is not promoted-release evidence |
| Hostile-package suite | **Done (bounded contract)** | Entry/count/inflation ceilings; normalized closed inventory; duplicate/traversal inertness (nothing is extracted); strict JSON; semantic, snapshot, context, asset, provenance and exact-renderer correspondence; atomic staged save and upgrade cleanup. Project provenance now shares the loose-catalog required/optional Unicode and length policy while retaining safe private records and exact resave. Loose symbol manifests add 2 MiB/512-record/depth/property bounds, duplicate/case-confusable/unknown-field rejection, bounded asset reads, inert Markdown attribution, and required/optional provenance limits; the current shipped deployment additionally uses an exact manifest-byte identity and closed-root check. Fuzzing remains welcome, not a substitute for this contract |
| Six-week staff pilot and defect burn-down | **HUMAN — participant use held; no opening date authorized** | The coordination structure and draft instruments exist, but the NVDA, print, think-aloud, seeded-error, and pilot-kit materials are **not ready for participant or reviewer use**. Exact consent, withdrawal-right, credit, contribution, private-record, quorum, conflict/recusal, and seat-authority terms require the recorded human dispositions; each participant's applicable choices must be recorded and each must separately acknowledge the withdrawal right and route before any session. Every dependent review and any release consideration also require the [bounded-commission ledger's](../council/bounded-commission-review-ledger.md) fresh full-chain audit: all later correction, withdrawal, credit-change, and supersession events must be bound to the exact candidate revision and one current effective disposition, with uncertainty or an unresolved event held. Maryland acceptance and the ordered needs-first/protected-seat reviews remain downstream human gates; see [the coordination plan](../pilots/human-gates-coordination-plan.md) |
| District readiness (Gate 3) | **HUMAN — packet ready, date pending** | `docs/district/gate-3-readiness-packet.md` includes the written-approval instrument; delivery follows Maryland and the moved calendar, with no replacement date inferred |
| Second maintainer | **HUMAN** | The sustainability DoD item; recruitment plan in the coordination plan; still the bus factor's only cure |

> **Migration-evidence continuation — 1 September 2026:** the current focused
> upgrade boundary is **56/56**, superseding the 55/55 count above. Read-only
> review now emits every ordinal-sorted, content-free recipe constituent, and the
> ID/version digest uses pinned `candidate-recipe-identity-inventory.v1` strict-
> UTF-8 and big-endian count/length framing instead of default JSON
> serialization. The output is inspectable evidence, not approval. The compiled
> manifest type now includes plan §6.6 local-preprocessing, recipe-owned
> localization-resource, and migration identity lists. The selected Option-A
> first-admission tree represents all three honestly as exact empty lists and
> binds them under `recipe-contract-fingerprint.v2`; the surrounding inventory
> binds that framing under `candidate-recipe-contract-inventory.v2`. This does
> not omit §6.6 confirmations: the already-bound ordered warning list is the
> combined declaration, and every warning is freshly materialized as a required-
> acknowledgement review issue by both supported review paths. This mechanical
> confirmation is not specialist approval. The fingerprint values therefore do
> not change for a duplicate field that is neither required nor admitted. This
> does not supply project `recipeHash`: schema 1 remains deliberately legacy and
> hash-less, and release remains stopped pending a separately authorized
> schema-2 route. ADR-007 remains Proposed; packaging, installation, and managed
> rollback remain unratified or human-held.

> **Schema-disposition preparation — 3 September 2026:** proposed ADR-010 now
> makes a bounded forward route reviewable: immutable hash-less schema 1;
> mandatory declarative-contract hash in schema 2; an explicitly
> manifest-backed portable-semantic identity; plural locale and exact
> asset/provenance-reference shapes; no inferred or in-place backfill; and
> copy-on-write re-admission only after current validation and a fresh Gate B
> review. Its four authority decision fields are intentionally blank; its three
> fixed deferred/non-effect rows defer the exact engine version and preserve
> district/records and release holds. No
> schema-2 code, exact engine version, tag move, ADR-007 ratification, real
> library plan, or release authority follows from this documentation artifact.

## Pre-publication check (run before anything becomes newly visible)

**Added 30 Aug 2026, because its absence cost something.** The repository was made public on 29 August with the seeded-error study’s answer key inside it. No forks, stars, or watchers were observed, and no participant was known to have the URL; the unavailable traffic interval cannot be reconstructed, so “nothing was lost” is not claimed. Nothing had *prevented* the exposure either. Every other rule in this project governs what **enters** the repository; that failure was a **visibility change**, and content that is harmless while private can be harmful the instant it is public.

Run this before a visibility change, a release, a site publish, or a public filing. It is short on purpose.

| # | Check | Why |
|---|---|---|
| 1 | **Scan the whole history, not the working tree.** `gitleaks git .` over full history, using the repository-pinned archive SHA-256 rather than a checksum downloaded beside the payload, plus a manual pass for emails and deleted files (`git log --all --diff-filter=D --name-only`). | Publishing publishes every commit ever made. Removing a file today does not unpublish it tomorrow. The pinned gitleaks digest was copied from the official `8.30.1` release checksum and independently matched before extraction or execution. |
| 2 | **List what becomes readable that was not.** Ask of each: would this harm anyone, or invalidate anything, if a participant, a district, or a stranger read it? | The key was not a credential and no scanner would ever have flagged it. Only a person asking this question finds that class of thing. |
| 3 | **Check the claims, not just the contents.** Does the README, the site index, and every status line say something true *today*? | The front page said "No application code exists yet" while 665 tests were passing. A false claim published is worse than an unpublished truth. |
| 4 | **Name what the act cannot be undone.** Write down what becomes permanent — an indexed page, a public filing, an archived clone. | ADR-006 holds a counsel checkpoint in front of public use of the name; a USPTO filing is public and permanent in a way a website is not. |
| 5 | **Confirm every gate by reading its conclusion**, not a wrapper’s exit code. Read both CI and CodeQL for the exact commit, and retain CodeQL's SARIF artifact with the release evidence. | CI was twice reported green while red, because `gh run watch --exit-status` was trusted instead of the run’s own `conclusion`; a missing SARIF file is missing security evidence. |

A finding at step 2 is not automatically a stop. Historically, the typist chose to publish after the facilitator replaced the external study instrument and regenerated the packets. Rendering is one command, but designing and reviewing a new instrument and key is protected human work. That historical choice is not standing authority for any future publication. The point of the check is that an authorized decision is *made*, not stumbled into.
## Rollback drill (documented procedure)

1. Every release is a signed tag; `git checkout <previous-tag>` + `tools/publish.ps1` rebuilds that pinned source for the explicit `win-x64` runtime. The SDK, repository source map, portable project locks, and separate application RID locks are pinned, but byte-identical packaging has not been measured and is not claimed.
2. The kill switch is policy, not code: IT sets `CloudInferenceEnabled=false` (or deletes `policy.json` — absence fails closed) and every device refuses egress at the provider boundary on next launch, no redeploy needed.
3. Retain the prior application and source library unchanged. The current engine reads and verifies the recorded frozen schema-1 packages and can prepare a separately validated candidate copy under proposed ADR-007. Reverse-opening a context-enriched current package in an older binary is **not** claimed; rollback returns to the retained prior library until a real managed-device drill proves more.
