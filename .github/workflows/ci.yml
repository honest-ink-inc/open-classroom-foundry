# Pull-request CI per implementation plan §12. This workflow activates when the
# repository gains a GitHub remote; until then it is reviewed as configuration.
#
# Coverage of the plan's CI list, honestly stated:
#   [x] Build on pinned .NET 10 with warnings as errors (global.json + Directory.Build.props)
#   [x] Unit, contract, schema, and migration tests (dotnet test — suites grow with the code)
#   [x] Secret scan (gitleaks)
#   [x] Dependency inventory artifact (SBOM-lite via `dotnet list package`)
#   [x] Coverage collected (Cobertura via coverlet collector) and published as an artifact
#   [x] Coverage THRESHOLD: Domain+Application+Contracts line coverage >= 80% (measured 88.6% at enablement)
#   [x] Formatting gate: whitespace verification (EOL/indentation/final-newline). Style-rule
#       enforcement stays out deliberately — the blanket analyzer severity would drag
#       info-level suggestions into the gate; revisit with a curated .editorconfig pass.
#   [ ] Full SBOM (CycloneDX) and license audit tooling — CycloneDX blocked on .slnx support
#   [x] Deterministic rendering comparisons (byte-identical render tests in the Rendering suite)
#   [x] UIA accessibility harness (tests/UiAutomation): in-process contract tests always run;
#       headed UIA tests run where the runner offers an interactive desktop and are VISIBLY
#       skipped otherwise (or with OCF_SKIP_HEADED=1) — a skip is an honest gap, never a pass

name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore OpenClassroomFoundry.slnx

      - name: Build (warnings are errors)
        run: dotnet build OpenClassroomFoundry.slnx --no-restore --configuration Release

      - name: Whitespace format gate
        run: dotnet format whitespace OpenClassroomFoundry.slnx --verify-no-changes

      - name: Test with coverage
        run: dotnet test OpenClassroomFoundry.slnx --no-build --configuration Release --logger "trx;LogFileName=test-results.trx" --collect:"XPlat Code Coverage"

      - name: Coverage threshold (core assemblies >= 80%)
        shell: pwsh
        run: |
          dotnet tool install --global dotnet-reportgenerator-globaltool
          reportgenerator -reports:"**/TestResults/**/coverage.cobertura.xml" -targetdir:coverage-report -reporttypes:JsonSummary "-assemblyfilters:+Foundry.Domain;+Foundry.Application;+Foundry.Contracts"
          $summary = Get-Content coverage-report/Summary.json | ConvertFrom-Json
          $line = $summary.summary.linecoverage
          Write-Host "Core line coverage: $line%"
          if ($line -lt 80) { throw "Line coverage $line% is below the 80% threshold." }

      - name: Dependency inventory (SBOM-lite)
        run: dotnet list OpenClassroomFoundry.slnx package --include-transitive > dependency-inventory.txt
        shell: bash

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: build-evidence
          path: |
            dependency-inventory.txt
            **/TestResults/*.trx
            **/TestResults/**/coverage.cobertura.xml

  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
